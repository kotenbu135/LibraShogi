# 手法の裏取り（2026-09-15）

採用済みの手法と設定値について、出所（計画書・決定の記録・計測）と文献での扱いを並べる。**変更の案ではない**（変えるかどうかはユーザーが決める。CLAUDE.md 作業の進め方 5・10）。

区分:
- **裏取りあり**: 原典（論文・開発元の文書）と一致を確かめた、計測で決めた、またはユーザーの決定。
- **一部**: 計画書や記録に出所はあるが、値や形の根拠が無い。または文献から変えていて理由の記録が無い。
- **記録なし**: 出所も理由も記録が無い（実装時に Claude が決めた値を含む）。
- **未実装**: 計画書にあるが実装が無く、やめた記録も無い。

---

## 1. 布石の価値目標（λ·z＋(1−λ)·V̂41）の文献での扱い

### 1.1 経緯

decisions.md 2026-09-15 の同名の行。claude.ai の設計案の 1 行「分散を下げる」が出所で、論文の引用は無い。λ 0.5 は 9/11 の実装時に理由の記録なく決めた。Gemini の回答の分離学習案（40 手目の価値を本将棋のネットから取る）を z で縛った形と読める（推測）。

### 1.2 文献

| 出典 | 価値の目標 | 探索値の使い方 | 確認 |
|---|---|---|---|
| AlphaZero [Silver18] | 結果 z（勝 +1・分 0・負 −1）、損失 (z−v)² | 使わない | 原典（arXiv 版） |
| KataGo 論文 [Wu19] | 結果 z。全読みの手だけを学習に使う（価値も） | 使わない。補助は陣地・得点（結果から作る） | 原典 |
| KataGoMethods [KGM] | 本体は結果 z | 将来の探索値の指数平均（約 6・16・50 手）を**別の補助ヘッド**で学ぶ。本体の損失の重みを少し下げて補助を足すと、学習が少し速く本体の損失も少し良い | 原典 |
| MuZero [Schrittwieser20] | 盤上ゲームは終局まで（z） | Atari だけ n 手先の探索値で補う（Reanalyze は n=5） | 原典 |
| Lc0 のブログ [Lc0-18] | — | 探索値だけでは値が自己強化してずれるおそれがあるので、z との平均を提案 | 原典 |
| Lc0 のブログ [Lc0-19] | — | q_ratio を入れた test52 は test51 とほぼ同じ強さ | 原典 |
| lczero-training [Lc0-code] | 既定は z（`q_ratio` 既定 0）。混ぜるときは `q·q_ratio + z·(1−q_ratio)` | 局面ごとに探索の q と引き分け確率 d を別々に持ち、WDL にするとき探索の d を使う | 原典（コード） |
| elmo・dlshogi [Yamaoka17][Yamaoka20][dlshogi-WCSC29] | 損失を (1−λ)H(v, z) ＋ λH(v, q) の**和**にする（勝率の 2 値の交差エントロピー） | 各局面自身の根の探索値。「経験的にブートストラップ手法は、非ブートストラップ手法より性能が良い」。2017 年の比較では価値の一致率がわずかに良い（0.4439 → 0.4444） | 原典（ブログ・アピール文書）。λ の値は未確認 |
| Oracle の Connect Four [Oracle] | 平均、または z から探索値へ世代で移すと、z・探索値の単独より学習が速い | 探索値の欠点は水平線効果と、学習初期に意味が無いこと | 原典 |
| Willemsen ほか [Willemsen22] | 探索値を使う目標（soft-Z、A0C、A0GB）が z より良い（Connect Four、Breakthrough） | — | **要旨のみ。本文は未確認**（PDF を読めなかった） |

### 1.3 論点ごとの所見

| 論点 | 文献 | Libra | 所見 |
|---|---|---|---|
| (a) 探索値を混ぜるか | 混ぜない: AlphaZero、KataGo（本体）、MuZero（盤上）、Lc0 の既定。混ぜる: elmo・dlshogi、Connect Four の例。Lc0 の試行は中立 | 布石で混ぜる | どちらにも実績があり、文献から一意には決まらない |
| (b) 混ぜ方 | dlshogi は損失の和。Lc0 は q と d を別々に混ぜる。KataGo は別ヘッド | 1 つのスカラー t に混ぜてから `soft_wdl` で (max(t,0), 1−\|t\|, max(−t,0)) に戻す | **同じ形の先例は見つからなかった**。引き分けの確率は結果にも探索にも無い 1−\|t\| になる（§2 の計測: 布石の局面でネットの D は 34〜37%、実際の引き分けは 0.7%）。探索と表示は W−L だけを使うので、推論では期待値を変えない（学習への影響は未確認） |
| (c) どの局面の探索値か | elmo・dlshogi・Lc0 は各局面自身の根の値。KataGo の補助は将来の探索値の平均。MuZero（Atari）は n 手先 | 布石の全局面に、41 手目の局面 1 か所の `root_q` を使う | **同じ形（固定の境界 1 点の値を手前の全局面に配る）の先例は見つからなかった**。計測済みの後手寄りのずれ（v41 が実際より約 0.04 低い。measurements.md 2026-09-15 18:40・19:10）は、この 1 点の値に集まる |
| (d) 係数の決め方 | Lc0 は既定 0。Connect Four は世代で移す。dlshogi は値が未確認 | λ 0.5 固定 | 根拠の記録なし |
| (e) 分散を下げる他の手段 | KataGo は補助ヘッドと本体の重みの調整 | V̂41 の補助ヘッドは実装済み（重み 0.5）。設計の「41 手目から本将棋を複数回分岐」は未実装 | — |

**まとめ**: 「混ぜるか・混ぜないか」は文献でも分かれていて、どちらかをバッドプラクティスとする資料は見つからなかった。一方、混ぜる先例（elmo・dlshogi・Lc0）に共通する次の 3 点では、Libra はいずれも先例と違う形になっている。
1. 探索値は各局面自身のもの。
2. 引き分けは実際の結果か探索の値から取り、スカラーから作らない。
3. 係数は既定 0 から、または計測で決める。

---

## 2. 洗い出し

「出所」の decisions は docs/decisions.md、measurements は docs/measurements.md。

### 2.1 学習の目標と損失

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| 布石の価値目標 | 0.5·z ＋ 0.5·v41 | 設計 §4.1 の 1 行、λ は実装時 | §1 | 記録なし |
| 目標の 3 値への戻し方 `soft_wdl` | 引き分け = 1−\|t\| | 9/11 の実装 | 先例なし（§1.3 (b)）。v0.1 の D: 2・10・20・30・39 手目で 0.338・0.343・0.361・0.366・0.352、45・80 手目で 0.007。実際の引き分け 0.7%（measurements 同日） | 記録なし |
| v41 の値 | 41 手目の局面の `root_q`（根の訪問の平均） | 設計は「探索値」とだけ | Lc0 は根の平均（root_q）と最善手の値（best_q）を両方持つ。v41 は実際より約 0.04 低い（measurements 18:40・19:10） | 記録なし |
| 価値を学ぶ局面 | 全局面（速読みの局面も） | decisions 9/11（値だけ） | KataGo は全読みの手だけを学習に使う（価値も） | 記録なし |
| 方策の目標 | 改善方策の上位 32 手で切って正規化し直す（`policy_topk`） | 9/11 の実装 | Gumbel 論文・AlphaZero・KataGo は全手の分布 | 記録なし |
| 改善方策の未訪問の手の値 | 根の `root_q` で補う | 9/11 の実装 | Gumbel 論文は v_mix（ネットの値と訪問した手の値の混合） | 一部（論文から変えて理由なし） |
| 損失の重み | 方策 1.0・価値 1.0・V̂41 0.5 | 9/11 の実装 | AlphaZero は等重み、KataGo は価値 1.5。V̂41 の 0.5 は根拠なし | 一部 |
| V̂41 ヘッドの目標 | `soft_wdl(v41)` の交差エントロピー | 9/11 の実装 | 上の `soft_wdl` と同じ | 記録なし |

### 2.2 学習の設定

| 項目 | 今の値 | 出所 | 文献との対応 | 区分 |
|---|---|---|---|---|
| 学習率 | 2e-4、1,000 step の立ち上げの後は一定 | 9/11 の実装 | AlphaZero は 3 回、KataGo も途中で下げる | 記録なし |
| 最適化 | AdamW (0.9, 0.98)、重み減衰 1e-4、勾配の切り詰め 1.0 | 9/11 の実装 | AlphaZero・KataGo は SGD（慣性 0.9） | 記録なし |
| バッチ・学習量 | 1,024、replay_ratio 4、256 局ごと | decisions 9/11（値だけ） | — | 記録なし |
| リプレイの窓 | 100,000 局で固定（lx 20,000）＝約 920 万局面。replay_ratio 4、AdamW 2e-4 一定、dropout 0 | 9/11 の実装（fdc65e7。値の理由の記録なし。計画 libra-local.md §1 は RAM の見積もりとして 1〜1.3×10^8 局面を挙げる） | KataGo は総数に応じて広げる（25 万から約 2,200 万局面、SGD）。**2026-09-17 の計測: 保存済みの重みは窓の中の局面（本将棋は一局ごとに全部違う）だけに当てはまり（価値の相関 0.93〜0.94）、窓の外では直後の世代の局でも 0.41〜0.50（二乗誤差 0.8〜0.9、常に 0 の予測で 1.0）。9/14 の 274,739 から同じ（measurements.md 同日 16:30）** | 記録なし |
| ワーカーの局の古さの上限 | 2,000 step | decisions 9/14（手元と同じ鮮度にする理由あり） | — | 一部 |
| 鏡映増強 | 50% | 設計 §3.1・§3.2 | — | 裏取りあり（計画書） |
| ネットの形 | d 320・8 層（約 10M） | 計画 §3.2 の 10M | 形の選び方の記録なし | 一部 |
| lx の学習設定 | バッチ 512、学習率 3e-4 | lx の config | — | 記録なし |
| 学習の torch.compile | max-autotune（CUDA Graphs なし）。損失の式・最適化・ステップ数は同じ | decisions 9/16（出典なし、速度のため） | compile の有無で損失の差 3e-5 以下（measurements 9/16 20:10）、`test_trainer.py` | 裏取りあり（計測） |

### 2.3 自己対局の探索

| 項目 | 今の値・形 | 出所 | 文献との対応 | 区分 |
|---|---|---|---|---|
| 読みの回数の抽選 | 全読み 96 回・速読み 24 回、全読み 25% | 計画 §3.1（平均 50〜100 回、全読み 25%） | KataGo は 600・100 回、25%（のち 1,000・200 回） | 一部 |
| Gumbel の候補数と σ | m 16（速読み 8）、c_visit 50、c_scale 1.0 | 9/11 の実装 | Gumbel 論文の Go・チェスの値と一致（m 16、c_visit 50、c_scale 1.0）。速読みの 8 は根拠なし | 裏取りあり（速読みの m を除く） |
| σ に掛ける値の範囲 | [−1, 1] のまま、c_scale 1.0、未訪問は根の値 | 9/11 の実装 | 著者の実装 mctx（`qtransform_completed_by_mix_value`、2026-09-17 に原典のコードで確認）は、未訪問を v_mix で補ってから子の間で [0, 1] に最小・最大で正規化し、`value_scale` の既定は **0.1**（σ = (50 + max N) · 0.1 · q̂）。Libra は正規化せず係数 1.0 なので、子の値の差 0.05 が約 7 nat になり目標がほぼ one-hot になる（measurements.md 2026-09-17 16:20: 最大確率 0.99 超が全読みの手の 0.36〜0.41）。論文本文の値は PDF を読めず未確認。**2026-09-17 に mctx 形を `[search] gumbel_rescale = true`（＋ `c_scale` 0.1）で選べるようにした（既定 false）。やり直しの煙テストで比べる** | 一部（論文から変えて理由なし） |
| 根以外の手の選び方 | PUCT、cpuct 1.5 | 設計 §3.3「PUCT＋Gumbel ルート」 | Gumbel 論文は根以外も改善方策に合わせる決定的な選び方。1.5 は根拠なし | 一部 |
| 未訪問の手の値 | 親の平均で補う | 9/11 の実装 | — | 記録なし |
| 引き分けの値 | 0 | 9/11 の実装 | AlphaZero と同じ | 裏取りあり |
| 詰み・証明の上限 | 根の詰み 200 節点、布石の証明 1,000 節点・36 手目から | 計画 §3.1「ノード上限を小さく」。200 は 9/14 に効果を測った上で変えない決定 | — | 一部 |
| 詰みの証明手の選び方 | 根で即座に勝つ手（1 手詰）があればそれ。本将棋で証明できたら 3 手・5 手の最短を総当たり（2 万節点まで）、無ければ df-pn が最初に証明した子（2026-09-24 から） | decisions 9/24（出典なし。df-pn は最短の詰みを返さないという既知の性質から） | — | 一部（効果は計測待ち） |
| 自己対局の玉配置 | 36×36 一様 | decisions 9/11「当面」 | 設計の 3 種の混合のうち一様だけ | 一部 |
| 搾取者・リーグ・openings | opponent_prior、PFSP、recent 30（保存している全部。2026-09-17 に 5 から）、openings 10% など | docs/exploiter-literature.md、decisions 9/14・9/17 | AlphaStar の本体はリーグ全体に PFSP（[Han20] と検索の要約。原典の割合は二次資料で食い違う）。recent 5 だった理由の記録はなく、9/17 の 200 局で本体が 79% 負ける古い lx が選ばれない状態だった | 一部 |
| 搾取者を過去の自分から始め直す | 2026-09-17 に 1 回、lx を step 116,230 の重みから（`restart-exploiter`） | decisions 9/17（ユーザーの決定「2′ これでやる」） | Tseng+ 2025 の gift-adversary（早いチェックポイントから始めて別の攻撃）、Wang+ 2023 の cp63。初期化する／続けるの統制比較は文献に無い | 一部（効果は計測待ち） |

### 2.4 計画にあって未実装（やめた記録なし）

| 項目 | 計画書 |
|---|---|
| 41 手目の局面から本将棋を複数回分岐（同一布石の勝率推定を安定化） | 設計 §4.1 |
| 補助ヘッド (ii) k 手後の駒損得・生存、(iii) 相手の次手予測、(iv) 後手玉に当たる利きの本数・遮断の可否 | 設計 §3.2。計画 §3.1 は V̂41 と次手予測を「残す」 |
| 1↔9 筋の鏡映で正規化した局面キーによる学習データの重複除去（鍵 `Position::norm_key` は libra-sim にあるが、使っているのはテストだけで、リプレイも探索も重複除去に使っていない） | 設計 §3.1 |

「後で」「当面」と記録があるもの（ペアサンプラーの重点、検証対局の逐次棄却、League exploiter）と、計画で「捨てる」としたもの（Reanalyze）は除いた。

### 2.5 玉配置表（libra-scale）とエンジン

| 項目 | 今の値 | 出所 | 対応 | 区分 |
|---|---|---|---|---|
| build の読み | 1,600 回 | decisions 9/11（値だけ） | 設計 §5 は 10^7〜10^8 ノード | 一部 |
| verify の組数・局数 | 上位 48 組 × 100 局、同数 | decisions 9/11（v0） | 計画 §3.1 は 100 組 × 1,000〜2,000 局、設計は逐次棄却・トンプソン | 一部 |
| 検証前の釣り合いの幅 | margin 0.02 | decisions 9/11（値だけ） | — | 記録なし |
| 検証後の釣り合い | 区間が最小値と重なる組 | 設計 §5 | — | 裏取りあり |
| 全組の逐次の検証（`seq`、2026-09-15〜）の打ち切り | 95% で有意か半幅 0.02。100 局から 50 局ごとに見る | 規則はユーザーの決定、見る間隔は出典なし（decisions 9/15） | 確かめた計測: V̂ を真の得点とした模擬（得点 0.5 で有意に止まる 29%、0.52 で逆を選ぶ 5.1%）。実データでは確かめていない | 一部 |
| 対称な組のアラート | 途中は 先手の得点 + 3.0·se < 0.5（早期）、止まった後は 95% 区間の上限 < 0.5（確定） | 出典なし（ユーザーの要望から Claude が値を決めた） | 模擬（2026-09-15、取り込みのたびに見る）: 真の得点 0.5 で早期の誤報は組 1 つ 2.5%、群の合算 1.7〜2.6%（27 組と 4 群がすべて 0.5 なら、どこかで 1 回出る確率は約 5 割）。0.49 の検出は 27 組の合算 99.5%・線対称 15 組 88%・組 1 つ 9%、0.47 は組 1 つ 63%。早期は注意、判断は確定で行う | 一部 |
| 置く側の集合（`seq`） | 95% 区間が 0.5 を含んだまま止まった組 | 設計 §5 を逐次のデータ向けに変えた案（ユーザーの了承待ち） | 模擬で置く側の損の最大 0.03（旧規則は 0.10〜0.15） | 一部 |
| エンジンの読みの既定 | Sims_Fuseki 400、Sims_Normal 800、Mate_Nodes 2000 | 実装 | — | 記録なし |
| 持ち時間の配分 | (秒読み＋加算＋残り/30) × 0.9 | 実装 | — | 記録なし |
| 選ぶ側の判断 | 2 手目の後の winrate の符号 | 計画 §4 | ネットの後手寄りのずれ（measurements 18:40）がそのまま効く | 裏取りあり（計画書） |

### 2.6 文書と実装の食い違い

| 項目 | 内容 |
|---|---|
| `libra-net/libra_net/losses.py` | どこからも読まれていない。書いてある損失（価値は z の 1 点、V̂41 は期待値の二乗誤差）と、実際に学習で使う `trainer.py` の損失（`soft_wdl` の交差エントロピー）が違う。**2026-09-15 に削除した（§4.4 (d)）** |
| docs/protocol.md の Sims_Normal | 既定 400 と書いてあるが、エンジンの既定は 800（Sims_Fuseki も 200 と書いてあり、実装は 400）。**2026-09-15 に実装に合わせた（§4.4 (d)）** |

### 2.7 計測（2026-09-17 に追加）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| 同じネットの自己対局の較正（`libra calib`、`calib.jsonl`） | 全読みの手の `root_q` を得点の予測にして結果と 10 区間で比べる。3 手目・布石／本将棋 × 手番、ECE・MCE・偏り・Brier。証明済み（\|q\|=1）とリーグの対局は除く。最新 20,000 局、60 分ごと | decisions 9/17（Jev [TypeSafe] の「確率を当たる頻度に合わせる」考え方から。手法の出典なし、ユーザーの了承「足す」） | 定義は [Guo17] §2 の信頼度曲線・ECE・MCE を、最上位の選択肢の確信度ではなく手番側の得点の予測に当てはめた（区間は論文の 15 でなく `libra eval` と同じ 10）。確かめる計測はこの曲線そのもの（measurements 9/17 08:40） | 一部（区間 10・20,000 局・60 分は Claude が決めた値） |

### 2.8 伸びの曲線（2026-09-18 に追加）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| 伸びを「2 倍あたりの Elo」で見る（`libra scaling`） | 固定の参照との Elo を log2(総局数) の直線に当てはめ、傾きを「局を 2 倍にしたときの Elo」とする。100 万局あたりの値も区間ごとに併記する | 自分の案（scaling-2026-09-18 §4 の問い「100 万局あたり何 Elo」に答えるため） | [Wu19] の図 4（1600 visits の Elo の推移）は横軸の自己対局の費用を対数で取っている（原典で確認。「Note the log-scale」）が、**Elo が局数の対数に直線で伸びるという主張は論文にはない**（原典で確認）ので、当てはめの形は自分の案。実測では、同じ伸びでも 100 万局あたりの値は総局数が増えるほど下がる（12 万→20 万局の区間 +1,770、40 万→80 万局 +523。2 倍あたりでは +192 と +209 でほぼ同じ）。確かめる計測: 120 万局・160 万局の点を足したときに、2 倍あたりの傾きが今の +185 ± 残差 23 に収まるか（次の 2 つの節目） | 一部（当てはめの形は自分の案。値は実測） |
| Elo が縮まない得点の帯 0.2〜0.8（`scaling.BAND`） | 参照との得点がこの外の点は傾きの計算から外す | 自分の判断（出典なし） | 実測で確かめた: 2 つの参照の差は、両方が帯の中にある 20 万・30 万局の点では +274・+306 Elo なのに、片方が 0.83・0.90 になる 40 万・80 万局では +244・+146 に潰れる。ls2-settings §5 の「win1m は旧 ls に +297」と合うのは帯の中の値のほう。200 局で得点 0.8 のときの区間は既に ±60 Elo | 一部（0.2 と 0.8 という切りのよい値は Claude が決めた。潰れることは実測） |


### 2.9 全部の対局から一度に Elo を出す（2026-09-18 に追加）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| Bradley-Terry の最尤推定（`libra rating`） | 記録した対局（最強比・基準比・固定の参照・外部計測）を全部まとめ、点ごとに 1 つの強さを当てはめる。Elo = (400/ln10)·ln λ | Bradley & Terry (1952) の模型を、Hunter (2004) の MM 反復で解く | 反復の式 `λ_i ← w_i / Σ_{j≠i} n_ij/(λ_i+λ_j)` は Caron & Doucet (2012) が Hunter の MM として書いている式をそのまま使った（a=1・b=0 が最尤）。**Hunter (2004) の原典（Annals of Statistics 32(1)）は本文を開けず未確認**（Incapsula）。確かめる計測: 2 点だけのときに Elo の式 400·log10(s/(1−s)) と一致すること（`test_rating.py` で確認済み、誤差 0.05 未満） | 一部（式は二次資料で確認、原典は未確認） |
| 引き分けを半勝として数える | 得点をそのまま小数の勝ち数にする | Elo の慣習（自分の判断） | Hunter の模型は勝ち負けだけで、引き分けを正しく扱うには Davidson (1970) の追加の母数が要る（**Davidson は未確認**）。半勝にしたのは引き分けが全体の 0.3% ほど（1,000 局で千日手 2・320 手 1）で、母数を 1 つ増やすだけの情報が無いため。確かめる計測: 引き分けの割合が 5% を超えたら Davidson 形を検討する | 一部（模型の簡略化。割合は実測） |
| 非推移性を χ²/自由度で測る | z_ij = (w_ij − n_ij p_ij)/√(n_ij p_ij p_ji) の二乗和 ÷（組の数 − (点の数 − 1)）。1 なら Elo の 1 本の目盛りで説明できる | 一般的な適合度の検定（自分の案） | 当てはめの残差が偶然だけなら z は標準正規なので二乗和は自由度どおりになる。2026-09-18 の実データでは 42.6 で、Elo 1 本では説明できない（measurements.md 同日の「非推移性」の行と整合）。確かめる計測: じゃんけんの人工データで大きく、推移的な人工データで 0 になること（`test_rating.py` で確認済み） | 一部（統計の定石。しきい値の読み方は自分の案） |
| 参照の自動入れ替え（`reference_rotate`） | 参照に 80% 以上勝ったら外し、そのときの archive を参照にする | 自分の案（出典なし。2026-09-18 のユーザーの依頼「じゃんけんに気をつけつつ物差しを自動で調整して」） | 入れ替え先をこの run 自身の archive にするのは、足した時点で自分自身（＝互角）なので目盛りが必ずつながるため（scaling-2026-09-18 §6.5）。80% は上の帯の上限と同じ値。確かめる計測: 160 万局の節目で `libra rating` の `dropped` が空で、曲線に点が足されること | 一部（値は帯と同じ、仕組みは自分の案） |

### 2.10 借りるホストの選び方（2026-09-18 に追加）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| 候補を**見込みの 100 万局あたりの費用**の安い順に借りる（`hosts.rank`） | 過去の実測から見込みの局/日を当て、$/h × 24 ÷ 局/日 × 10^6 の安い順。これまでは実効単価（$/h）の安い順 | 自分の案（出典なし） | 実測が根拠: 借りた 7 セッションで 100 万局あたり $5.98〜$9.32（1.56 倍）で、同じ 5070 Ti でも CPU の違いで 566k / 576k / 675k 局/日、5080 は局/日 1.3 倍でも $/h が 1.6 倍で 100 万局あたりは負ける（measurements.md 2026-09-16）。**$/h の安い順ではこの差を一切見ていなかった**。確かめる計測: この後の 5 セッションの実測の $/100 万局の中央値が、それまでの 7 セッションの中央値（$7.17）より下がること | 一部（並べ方は自分の案。差があることは実測） |
| 速さは「最初の 10 分を除いた回収の傾き」で測る（`hosts.WARMUP_S` 600 秒、`MIN_SPAN_S` 900 秒、`MIN_GAMES` 500） | セッションの `bridge.log` の `placed … (total N` の行から、暖機を除いた区間の局数 ÷ 秒 | measurements.md 2026-09-16 が同じ測り方（「最初の 10 分を除いた回収の傾き」）でホストを並べ直しているので、それに合わせた | 暖機が要るのは 512 局を同時に打ち始めるため（最初の終局がまとまって遅れる。measurements.md 2026-09-14 の `games_per_day` の warmup 300 秒も同じ理由）。900 秒・500 局は短すぎる区間を落とすための自分の判断（出典なし）。確かめる計測: 同じ機械の 2 セッションの見込みと実測の差が 10% 以内に収まるか（既にある 2 セッションでは 664,615 と 674,932 で 1.6%） | 一部（測り方は既存の計測に合わせた。しきい値は自分の判断） |
| 当てる順は 同じ機械 → 同じ GPU と CPU → 同じ GPU、どれも**中央値** | `hosts.keys_of` / `speed_table` | 自分の案（出典なし） | 実測で、同じ機械の 2 セッションは 1.6% しか違わないのに、同じ GPU で CPU が違うと 19% 違う（566k → 675k）ので、確かな順に当てる。中央値は 1 回の外れ（借り直しの直後、学習側の停止と重なった回など）に引きずられないため。確かめる計測: 上と同じ | 一部 |
| 実測の無いホストには**候補の中央値**を当てる | `hosts.annotate` | 自分の案（出典なし） | 未知を前に出せば外れを引き、後ろに回せば良いホストを試さないので、真ん中の扱いにした。確かめる計測: 実測の無いホストを借りた回で、実測が見込み（中央値）から大きく外れた割合を `libra-vast history` で見る | 自分の案 |

---

### 2.11 自己対局の投了（2026-09-19 に追加。**実装したが既定は無効**）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| 投了の規則: 手番側の探索後の値 `root_q` が −`resign_threshold` 以下の状態がその側の連続 `resign_runs` 手続いたら、その手を指した後にその側が投了する（`config/ls.toml` の `[search]`。**既定 `resign_threshold = 0.0` ＝無効**） | 未採用（実装だけ。入れる時期は**ユーザーが調整中**。2026-09-19 のユーザーの「投了は採用するが時期は要調整とする」） | AlphaGo Zero [Silver17] Methods「Self-play」の投了。**原典と 3 つ違う**（下） | 原典は「根の値**と最善の子の値**の両方が −v_resign を下回ったら投了」。Libra の棋譜には候補ごとの値を残していないので**根の値だけ**で見る（そのぶん投了しやすく、節約率も誤投了率も上限になる）。原典は連続の手数を数えず 1 手で投了するので `resign_runs = 1` が原典の形（2 以上は自分の案。出典なし）。原典は v_resign を「誤投了 5% 未満」になるよう自動で調整するが、Libra は固定値にして `bin/libra resign` で棋譜から測り直す（自分の案）。**布石では投了しない**（`resign_min_ply = 40`。布石は 40 手で必ず終わり、そこでの値は 41 手目の裁定の予想なので、投了で切っても得が無い。自分の案） | 一部（原典の形から 3 点変えていて、理由はここに書いた） |
| 10 局に 1 局は投了させず最後まで打つ（`resign_disable_prob = 0.1`） | 同上 | AlphaGo Zero [Silver17] Methods。原典と同じ 10% | 原典と同じ目的（誤投了の割合を測り続けるため）。この見本があると `bin/libra resign` を入れた後も同じ形で測り直せる | 裏取りあり（原典と一致） |
| 計測の対局（基準比・最強比・固定の参照・A-B）では投了しない（`evaluate.play_match` が `resign_threshold` を 0 にする） | 同上 | 自分の判断（出典なし） | 誤投了が 2.6% あるので、そのまま Elo に乗ると物差しが狂う（`libra rating` は全部の対局をまとめるので、自己対局の誤投了だけなら学習の信号の問題で済むが、計測の対局に混ざると強さの推定そのものがずれる）。原典は評価対局でも投了する。確かめる計測: 入れた後の最強比 1,000 局の Elo が、入れる前の点から曲線の見込みどおりに伸びること | 自分の判断 |
| 効き目の見積もり（0.90・1 手 → 評価の節約 19.1%、局/日 1.24 倍、誤投了 2.61%） | `bin/libra resign`（2026-09-19、ls の最新 20,000 局） | 打ち終わった棋譜の上で数え直したもの（measurements.md 2026-09-19） | **どの設定も原典の基準（誤投了 5% 未満）の内側**。ただし**1 日に学習へ入る局面の数は増えない**（手数の節約 20.3% と局/日 1.24 倍が打ち消し合って 0.98 倍）。増えるのは 1 日に取れる勝ち負けの結果の数で、価値の学習には効くが方策には効かない。曲線（+186.5 Elo/2 倍）から出る「+57 Elo」は**上限**であって見込みではない。確かめる計測: 入れた前後で最強比 1,000 局・`gen` の held-out との差・`libra rating` の目盛りを比べる | 裏取りあり（自分の計測） |

### 2.12 外部計測のハンデ（2026-09-19 に追加。**実装だけ。既定は今までどおり両者同じ `go`**）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| 勝ちすぎると物差しが効かなくなる（1 局あたりの情報は得点 p の p(1−p) に比例し、全勝すると Elo が発散する） | 40 局の 95% 区間は得点 0.5 で ±55 Elo、0.9 で ±92、0.975 で ±176。全勝した点は `rating.fit` の強連結の条件から外れて目盛りに載らない | 二項分布の分散（標準的な統計）と Hunter (2004) の最尤推定の存在条件（`libra rating` に実装済み。§2.9） | 実測: 2026-09-19 の 40 局は 36 点（0.90、+382 Elo [249, 860]）と 35 点（0.875）。曲線の +209 Elo/2 倍と今の局/日（190 万局/日）では、あと 1〜2 回の倍加で 0.975 に届く | 裏取りあり（式は標準、値は実データ） |
| 相手は変えず、**Libra 側の読む量を減らして得点を 5 割に近づける**（`match_go` = `nodes N`、`match_go_opp` = 相手の `go`） | 実装だけ（既定は `match_go_opp` 空＝両者同じ）。入れる値と時期はユーザーの判断 | 自分の案（**出典なし**）。持ち時間・読む量のハンデ自体はコンピュータ将棋・チェスで広く使われる測り方だが、原典は当たっていない（**未確認**） | 相手を強くする案（`Threads` を 2 → 8）や局数を増やす案は一度きりで、曲線の伸び（+209 Elo/2 倍）に数日で追い抜かれる。ハンデなら段を下げ続けられる。`nodes` にすると GPU の混み具合で読む回数が変わらなくなる副産物もある（2026-09-15 の実測で `movetime 1000` の読む回数は 2,320〜4,774 と 2 倍ぶれていた）。**確かめる計測**: 入れた最初の節目で得点が 0.4〜0.6 に入るか、入らなければ段を 1 つ動かす | 自分の案（未確認） |
| 条件（相手の `setoption` と両者の `go`）が違う結果は Elo の別の点にする（`rating.match_nodes`） | 実装済み（条件を記録していない古い結果は素の名前のまま） | 自分の判断（出典なし） | 相手のスレッド数や持ち時間を変えると強さが変わるのに `id name` は同じなので、1 点にまとめると強弱の違う相手が混ざって目盛りが狂う。ハンデを付けた Libra も全読みの自分とは別人なので別の点にする。確かめる計測: `test_rating.py` の 3 つ（設定違いで点が分かれる・ハンデで点が分かれる・古い記録は素の名前） | 自分の判断 |

### 2.13 外部計測の総取り替え（2026-09-20。布石は Libra 同士、41 手目から やねうら王／水匠5）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| 布石を Libra 同士にし、41 手目から相手（やねうら王＋水匠5 の評価）と指す | `match_fuseki = "self"`、相手は布石を指さないふつうの将棋エンジン | **ユーザーの決定**（2026-09-20「方策ネット＋やねうら王/水匠5 での対外対局には限界があるのでやめる。40 手目までは Elo 測定で測った最強 Libra 同士で行い、41 手目から 最強 Libra vs やねうら王/水匠5」） | 前の相手は布石が弱く、読む量のハンデを 8 回（ほぼ方策ネットだけ）まで下げても得点 0.875 のままだった（measurements.md 2026-09-20）。**この形では布石の強さは測らない**（両陣とも Libra が作るので打ち消し合う）。測るのは 41 手目以降の将棋 | ユーザーの決定 |
| 布石は計測する重みが両陣とも作り、**同じ布石を先後入れ替えて 2 局ずつ**打つ | `Match.make_opening`。41 手目の裁定で終わった布石は捨てて打ち直す | 自分の判断（**出典なし**）。同じ開始局面を色を入れ替えて 2 局打つのはエンジンの比較で広く使われる形だが、原典には当たっていない（**未確認**） | 布石が局ごとに散るのは USI エンジンが根の Gumbel ノイズを入れたまま指すため（`SearchConfig.gumbel_noise` の既定 true。`libra-engine/src/engine.cpp` は false にしていない）。先後を入れ替えて 2 局打てば、Libra が片方の陣を良く作ってしまっても打ち消し合う。**確かめる計測**: 最初の 1 回で 41 手目の局面が 5 つとも違うこと（棋譜の `sfen41`）と、同じ局面の 2 局で先後が入れ替わっていること（`test_harness.py::test_libra_builds_both_camps_and_then_plays_from_move_41`） | 自分の判断（未確認） |
| Libra 側は自己評価と同じ読み（`nodes 96`・`Mate_Nodes=200`）にして `match_libra_standard` を立てる | 実装済み（`rating.match_nodes`） | 自分の判断（出典なし）。`rating.fit` の強連結の条件（Hunter 2004 の存在条件、§2.9）から出る要請 | Libra 側の読む量を変えると「その Libra と相手」だけで閉じた塊になり、目盛りから丸ごと落ちる。2026-09-19〜20 のハンデ付きの計測が実際にそうで、得点は残っても `libra rating` には効いていなかった。逆に、ハンデを付けたまま素の `step N` にすると、強さの違う 2 人が 1 点に混ざって目盛りが狂う。**確かめる計測**: `test_rating.py::test_libra_standard_stays_on_the_main_scale`、および最初の 1 回の `libra rating` で相手の点が `dropped` に入らないこと | 自分の判断 |
| 段（相手の読む節点数）を 1 回の節目で 4 つに分けて挟む | `match_go_opp = "nodes 1000, nodes 10000, nodes 100000, nodes 1000000"`（40 局を 10 局ずつ） | 自分の判断（**出典なし**） | Libra が 96 回読みで、水匠5 の評価を積んだアルファベータ探索の何節点と釣り合うかは**まったくの未知**。1 段ずつ下ろすと節目（40 万局＝半日〜1 日）を何度も使う（読む量のハンデで実際に 2 回使った）。アルファベータは節点 2 倍で +50〜70 Elo と言われる（**未確認**）ので、この 4 段で 500〜700 Elo ぶんを一度に挟める。**確かめる計測**: 最初の 1 回で得点が段ごとに単調に下がり、0.4〜0.6 に入る段が 1 つ以上あること。無ければ上か下に伸ばす | 自分の判断（未確認） |

---

## 3. 読めなかった資料

- Willemsen ほか 2022 の本文（PDF を読めなかった）。要旨だけを使った。
- dlshogi の λ の値（ブログとアピール文書に数値が無い）。
- Gumbel 論文の σ の式は、読み取りで c_scale が指数に見えた。著者の実装 mctx は積（(c_visit ＋ max N) · c_scale · q）なので、積として扱った。

---

## 4. 対応の選択肢と再発防止（2026-09-15、提案。決めるのはユーザー）

### 4.1 確かなことと未確認のこと

| 項目 | 値 | 出所 | 確認 |
|---|---|---|---|
| ネットは布石の学習目標どおりに学んでいる | 両玉の局面の V 0.459（v0.1）、目標 0.457、z 0.475 | measurements 18:40 | 確認済み。ずれは最適化ではなく目標にある |
| v41 は同じ局の結果より低い | 平均 約 −0.04（ls の全期間 −0.017〜−0.063） | 18:40 | 確認済み |
| 探索値（`root_q`）は手番側を低く見る | 本将棋の各手で手番側から −0.017〜−0.036、先後によらない | 18:40 | 確認済み。41 手目の局面はいつも先手の番なので、この低さが先手に不利な向きに固定される |
| v41 が低くなる理由 | 探索値の手番側の低さと、41 手目の局面のネット単体の低さ（v0.1 のころ 0.398、実際 0.493） | 19:10 | **未確認** |
| 釣り合う組での判断のずれ | 検証済み 48 組で V − 検証の勝率: ネット単体 −0.006、読み 128 回 −0.009、build（1,600 回）−0.008。標準誤差 約 0.006 | measurements 20:05 | 確認済み。95% では有意でない |
| サイトの選ぶ AI の損 | 読み 128 回（`KINGS_SIMS`）で得点 0.509、いつも先手 0.508。読み 1 回なら 0.502 | 20:05 | 確認済み |
| 布石の途中の表示（`JUDGE_SIMS` 1 ＝ネット単体） | 布石の途中の局面のネット単体は実際に近い（先手の番 +0.002、後手の番 +0.015） | 19:10 | 確認済み（直近の棋譜の 24,000 局面） |
| `soft_wdl` の引き分けの確率 | 布石で 34〜37%、実際 0.7%。目標の W−L は t と等しいので、目標の期待値は変わらない | 20:00、replay.py | 学習への影響は未確認 |

2 つの比べ方で大きさが違う。
- 同じ局の中の比較: 学習の目標は結果より 約 0.02 低い（v41 − z 約 −0.04 の半分）。
- 釣り合う組での検証対局との比較: ずれは有意でない（−0.006 ± 0.006）。

先後を選ぶ判断が効くのは釣り合う組の近くなので、目標のずれは今のところ利用者に見える判断まで測れる大きさで届いていない。2 つの比べ方で大きさが違う理由（読みの回数、組の選び方、世代の違い）は未確認。

### 4.2 推論側（学習に触らない。公開済みの v0.1 に効くのはここだけ）

| 案 | 所見 | 推奨 |
|---|---|---|
| 選ぶ AI の読みを増やす | 効く（読み 1 回 0.502 → 128 回 0.509、区間が 0.5 を含まない組での逆の選択 4 → 1 組）。サイトはすでに 128 回（`KINGS_SIMS`）。エンジンは持ち時間で読み、`movetime 1000` で中央値 約 3,000 ノードと 128 回より多い（measurements 同日） | すでに入っている。追加は要らない |
| 検証の結果から求めた補正値を V から引く | 釣り合う組でのずれは有意でない | 入れない |
| 布石の途中の表示（`JUDGE_SIMS` 1） | 布石の途中の局面のネット単体は実際に近い（19:10）。両玉の直後の局面は上の 48 組で −0.006 | 変えない |

### 4.3 学習側（v0.2 以降。学習則なのでユーザーが決める）

互いに独立な 4 つのつまみがある。

| つまみ | 今 | 候補 | 所見（計測・文献） |
|---|---|---|---|
| (1) 係数 `lambda_z` | 0.5（ls・lx とも） | 1.0（布石も z だけ）、または計測で決めた値 | 1.0 は AlphaZero・KataGo の本体・Lc0 の既定と同じ形。混ぜる先例（dlshogi、Connect Four）は学習が速い・わずかに良いと報告し、Lc0 の試行はほぼ同じ強さ。Libra で混ぜる効果は測っていない |
| (2) 混ぜる探索値 | 41 手目の 1 点の `root_q` | 各局面の `root_q`（elmo・dlshogi・Lc0 形）、最も訪問した手の値 | 各局面の `root_q` は布石で先手の番 −0.011〜−0.023、後手の番 +0.021〜+0.030 と、どちらも先手に不利（18:40）で、向きは直らない見込み。最も訪問した手の値は、実際との差が読み 24 回で −0.033、96 回で +0.036（19:10）と、読みの回数で変わる |
| (3) 3 値の目標の作り方 | `soft_wdl`（引き分け = 1−\|t\|） | 損失の和（dlshogi 形）、q と d を別々に持つ（Lc0 形。今の探索は d を記録していない） | (1) を 1.0 にすると本体の目標は z の 1 点になり、この問題は本体から消える。V̂41 ヘッドには残るが、推論では使っていない（`ort_infer.cpp` は policy と wdl だけを読む） |
| (4) 分散を下げる別の手段 | V̂41 の補助ヘッド（重み 0.5） | 設計 §4.1 の、41 手目から本将棋を複数回分岐（未実装） | 分岐した回数だけ本将棋の手数が増え、同じ計算量で布石の局数が減る |

**推奨: 学習則は今は変えない。4.4 の (a) で、学習の目標と実際の結果の差を毎回見えるようにする。**
- 変えない理由: 釣り合う組での判断のずれは有意でなく、サイトの選ぶ AI の得点もいつも先手と同じ水準（4.1）。測れる害の無い段階で学習則を変えると、効果を確かめられない。
- 変えるときの第一候補は、(1) を 1.0 にして V̂41 ヘッドを残す案。次のどれかが出たら、下の比べ方 i で比べてから決める:
  - (a) で目標と結果の差が広がる。
  - 検証対局で、釣り合う組の V − 勝率が有意になる。
  - 選ぶ AI の得点が、いつも先手より有意に低くなる。
- 第一候補にする理由:
  - 確かめたずれの経路（41 手目の `root_q` の手番側の低さ → 先手に不利な v41 → 布石の目標の半分）を目標から外せる。(2) の代わりの値は、どちらも自分の計測で向きか大きさが直らない。
  - 本体の目標が z の 1 点になるので、(3) の引き分けの作り方も本体から消える。
  - 形は KataGo（本体は z、探索値は補助ヘッドで学ぶ）と同じ。
  - 変える箇所は設定 1 行。目標は `sample` のたびに作るので、窓の 10 万局に次の学習から効き、戻すのも 1 行。
  - 見込み（計算、未確認）: 布石の目標の平均は 0.457 から 0.475（z）になり、ネットの V もそれに沿う。
- 失うかもしれないのは、混ぜることで得ている分散の低さ（設計が混ぜた理由）。大きさは測っていない。
- 変えるときに併せて決めること・影響:
  - lx も `lambda_z` 0.5 で同じ形なので、lx も合わせるかを決める。
  - ls のリーグの局は v41 を結果に置き換える（`league.py` の `tag_league_game`）ので、lx の探索値は ls の目標に入らない。lx の布石から始まる ls の自己対局（直近で 3.2% 以上）は、ls 自身の v41 を使う。
  - 基準比の Elo は、基準が固定のネットなので変えた後も続けて測れる。玉配置表は、手順どおり次の版で作り直す。

**比べ方**（どちらも GPU を使うので、ls・lx の停止か共有をユーザーに頼む）

| 案 | やり方 | 分かること・分からないこと |
|---|---|---|
| i（すすめる） | 同じチェックポイントから `lambda_z` 0.5 と 1.0 を、同じ窓で同じ step 数だけ学習する | 分かる: 両玉の局面の V と検証の勝率の差、両者の 200 局。分からない: 長い目で見た学習の速さ（自己対局の繰り返しが入らない） |
| ii | 変える前のチェックポイントを写してから、ls の設定を変える | 分かる: [auto] の基準比 Elo と (a) の差の推移。分からない: 変更の効果と普段の伸びの区別（1 日 +30 程度に対し、100 局の幅は ±70） |

### 4.4 再発防止

**なぜ起きて、なぜ 4 日気づかなかったか**
- 採用: 設計の 1 行（出典なし）を、係数・3 値の作り方・どの探索値かの理由を書かずに実装した。CLAUDE.md の 8 は論文の手法が対象で、出典の無い手法には働かなかった。
- 検出: 学習の目標を実際の結果と比べる物差しが無かった。
  - 計画の「較正」（`libra eval`）は、別の世代のネットとの対局の `root_q` を結果と比べるので、実力差で崩れる。9/15 05:06 の基準比では、弱い側が 0.5〜0.6 と見た手の実際は 0.08 だった。
  - 布石・本将棋や先後で分けておらず、9/11 に 1 回記録した後は追っていない。
  - v41 − z は ls の全期間で 約 −0.04 あったが、サイトの選ぶ AI の振る舞いから調べるまで見つからなかった。
- テスト: `test_soft_wdl` は式の形（t = ±1・0）だけを確かめ、目標が実際の結果と合うかは確かめていない。

**対策**

| | 内容 | 種類 |
|---|---|---|
| a | 学習のバッチを作るときに、布石・本将棋と手番ごとに「目標の平均 − z の平均」「v41 − z」「目標の引き分けの確率 − 実際の引き分け率」を `metrics.jsonl` に出し、管理コンソールの学習タブに描く | 仕組み（学習則は変えない） |
| b | runbook の玉配置表の手順に「verify の後、V̂ − 検証の勝率の平均を measurements.md に書く」を足す（verify の出力に 1 行） | 手順 |
| c | CLAUDE.md の 8 を広げる。論文以外（計画書の 1 行、会話、自分の案）から手法や値を入れるときも、decisions.md に出典（無ければ「出典なし」）と、正しさを確かめる計測（何を・いつ）を書き、この文書の表に行を足す | 規則（番号は足さない） |
| d | 使われていない `losses.py` を消し、protocol.md の Sims_Normal を 800 に直す（§2.6） | 片付け |

(a)〜(d) は 2026-09-15 に入れた（ユーザーの了承。decisions.md 同日）。(a) はコンソールの「学習目標」タブ、(b) は verify のログの `v_hat - verify winrate`。

§2 のほかの「記録なし」の項目は、この症状の原因ではない（ネットは目標どおりに学んでいるので、最適化側の設定では説明できない）。別に扱う。

## 参考文献

- [Guo17] Guo, Pleiss, Sun, Weinberger, On Calibration of Modern Neural Networks (ICML 2017, arXiv:1706.04599) §2. https://ar5iv.labs.arxiv.org/html/1706.04599
- [TypeSafe] TypeSafe AI, Introducing System One Models & Jev（2026-09-15、宣伝文。RLCD の方法は非公開）. https://typesafe.ai/blog/introducing-system-one-models-and-jev
- [Silver18] Silver et al., A general reinforcement learning algorithm that masters chess, shogi, and Go through self-play (arXiv:1712.01815). https://ar5iv.labs.arxiv.org/html/1712.01815
- [Wu19] Wu, Accelerating Self-Play Learning in Go (arXiv:1902.10565). https://ar5iv.labs.arxiv.org/html/1902.10565
- [KGM] KataGo Methods. https://github.com/lightvector/KataGo/blob/master/docs/KataGoMethods.md
- [Silver17] Silver et al., Mastering the game of Go without human knowledge (Nature 550, 354-359, 2017) Methods「Self-play」. 方法の節を読んだ（augmentingcognition.com の PDF。decisions.md 2026-09-19）. https://www.nature.com/articles/nature24270
- [Schrittwieser20] Schrittwieser et al., Mastering Atari, Go, chess and shogi by planning with a learned model (arXiv:1911.08265). https://ar5iv.labs.arxiv.org/html/1911.08265
- [Danihelka22] Danihelka et al., Policy improvement by planning with Gumbel (ICLR 2022). https://davidstarsilver.wordpress.com/wp-content/uploads/2025/04/gumbel-alphazero.pdf / mctx https://github.com/google-deepmind/mctx
- [Lc0-18] Understanding Training against Q as Knowledge Distillation. https://lczero.org/blog/2018/10/understanding-training-against-q-as/
- [Lc0-19] What's going on with training! https://lczero.org/blog/2019/06/whats-going-on-with-training/
- [Lc0-code] lczero-training tf/tfprocess.py・chunkparser.py. https://github.com/LeelaChessZero/lczero-training
- [Yamaoka17] 将棋でディープラーニングする その39（ブートストラップ）、その50（訂正）. https://tadaoyamaoka.hatenablog.com/entry/2017/06/28/080414 / https://tadaoyamaoka.hatenablog.com/entry/2017/12/05/210522
- [Yamaoka20] dlshogiの学習則. https://tadaoyamaoka.hatenablog.com/entry/2020/05/31/114435
- [dlshogi-WCSC29] 第29回世界コンピュータ将棋選手権 dlshogi アピール文章. https://www.apply.computer-shogi.org/wcsc29/appeal/dlshogi/dlshogi_appeal_wcsc29.pdf
- [Oracle] Lessons From AlphaZero (part 4): Improving the Training Target. https://medium.com/oracledevs/lessons-from-alphazero-part-4-improving-the-training-target-6efba2e71628
- [Willemsen22] Willemsen, Baier, Kaisers, Value targets in off-policy AlphaZero: a new greedy backup. https://research.tue.nl/en/publications/value-targets-in-off-policy-alphazero-a-new-greedy-backup/

### 2.13 頭打ちの引き金（2026-09-20。ユーザーの依頼「どこで伸びが鈍った、と判断したらいいか検討」）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| 「`rating --curve` の点が**続けて 2 つ以上**、目安の線（`fit_recent`）より下に落ち、95% 区間が線をまたがない」 | docs/release.md §0 の表。2026-09-20 に置いたまま | 自分の案（**出典なし**）。学習曲線の頭打ちを判定する標準的な手法は当たっていない（**未確認**） | 2026-09-20 に計算で効き目を数えた（`tools/plateau_trigger_power.py`、実データ 13 点＋節目 20 回を条件ごとに 400 回）: 誤報 3.5%／停止 100%（1 節目）／半減 96%（480 万局）／**25% の鈍りは 27% しか捕まえない**。v0.2 の判断に効くのは「1 Elo 約 8 円を払う価値があるか」なので、25% の鈍りは判断を変えないと見て**そのまま使う**と決めた | 裏取りあり（自分の計算。一次資料は未確認） |
| 目安の線は `fit_recent`（最後の点から 4 回の倍化ぶん、最低 4 点） | `libra-league/libra_league/rating.py` の `RECENT_DOUBLINGS = 4` | 自分の案（**出典なし**） | 2026-09-19 の実データ 11 点で、全部の点に当てはめると傾き +140.1・残差 87.9 で最新の点が +62 上に出て「加速している」と誤読させた。この範囲なら +203.1・残差 21.3 で、40 万局以降どの節目で当てはめても 203〜213 に収まる | 裏取りあり（自分の計測） |
| 自己評価の局数 `anchor_games`・`best_games` = 2,000（1,000 から） | `config/ls.toml`（2026-09-20） | 自分の計算（**出典なし**） | 1 点の 95% 区間が ±35 → ±25 Elo に縮み、25% の鈍りを捕まえる割合が **27% → 51%**。誤報は 3.2% で変わらない。余分な対局は節目あたり 2,000 局で 40 万局の 0.5%。**確かめる計測**: 320 万局の節目で目盛りの点の区間が ±25 Elo 前後に縮んでいるか | 裏取りあり（自分の計算） |
| 節目を等比（総局数 1.25 倍ごと）にする案 | **却下**（40 万局ごとのまま） | 自分の案 | 25% の鈍りは 27% → 39% に上がるが、誤報が 3.5% → 5.8% に上がり、鳴る総局数はむしろ遅くなる（560 万 → 684 万局） | 裏取りあり（自分の計算） |

### 2.14 搾取者の課程（2026-09-24。ユーザーの決定「今すぐ作る」）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| 凍結した本体の読みの回数を 1・2・4・8・16・32 回の段で上げ、直近 2,000 局で 75% 以上なら次へ（`config/lx.toml` の `curriculum_*`） | docs/lx-settings.md §5 | [Tseng25] Appendix E.2（最終の相手の少ない読みから倍に、閾値 75%）、[Timbers22] §4.2（読みの回数だけの課程）。局数 2,000 は自分の計算（**出典なし**） | [Tseng25] は 64 回からいきなり当てて 3% で伸びず、4 回から始め直して進んだ。[Timbers22] は課程なし 20〜40%・あり約 90%。**確かめる計測**: 段ごとに上がるまでの局数と、課程を終えた後の対本体勝率（本番の読み）。上げた直後に崩れて戻らなければ段を足す | 一部（形は原典、段の値と局数は仮） |

### 2.15 搾取者の `replay_ratio`（2026-09-24。ユーザーの決定「4 回にしちゃってもいいんじゃない？」）

| 項目 | 今の値・形 | 出所 | 文献・計測との対応 | 区分 |
|---|---|---|---|---|
| `config/lx.toml` の `[train] replay_ratio` を 1.0 → **4.0**（1 局面を平均 4 回学習する） | docs/lx-settings.md §6 | [Tseng25] Table C.1「Data Reuse Factor 4」（「KataGo と同程度」）、[Wang23] 1 epoch 約 250,000 局面で 100 万学習サンプル ＝ 1 局面約 4 回 | **1.0 のほうが出典なしだった**（2026-09-22 に本体 ls に倣って Claude が決めた値）。2026-09-17 のやり直しの原因は「1 局面 4 回」ではなく「ネットの容量に対して窓が小さいこと」で、当時の本体は 1 パラメータあたり 0.90 局面、今の lx は 6.4 局面。**確かめる計測**: M1 の差（窓の中 − held-out。今 −0.079）が +0.1 を超えたら 1.0 に戻す／課程 1 段目の勝率（今 3.9%）が上がるか／本体の 局/日 が落ちないか | 裏取りあり（原典 2 本が同じ値） |


出典（[Tseng25]・[Timbers22]・[Wang23]）は docs/exploiter-literature.md §4。
