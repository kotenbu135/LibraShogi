# やり直しの計画（2026-09-17、提案。決めるのはユーザー）

ユーザーの方針（2026-09-17）: 「初期検討時点での不具合なので最初からやり直しも検討する。1 週間の損失なら受け入れる。ただし再発防止・早期検出・初期検討をしっかりして、二度とスタート地点に戻らないようにする」。
本書は、その方針を実行するための案。原因の見立てと計測は docs/status-2026-09-17.md §10、設定の出所は docs/method-evidence.md。
「決定」と書いた項目以外は提案で、ユーザーの判断を待つ（CLAUDE.md 作業の進め方 5・10）。ライセンス: CC BY 4.0。

## 0. 確認実験の結果（2026-09-17 18:05〜19:20）

status §10.3 の確かめ方を実行した（measurements.md 同日 18:05 の行）。646,699 から、本番と同じ設定で 5,000 step だけ学習し、**変えたのはリプレイの窓の大きさだけ**（10 万局 → 100 万局）。

| 646,699 ＋ 5,000 step | 学習の損失（価値） | 本将棋の価値の相関: 窓の中 / 直後の世代の局 | 対 646,699 | 対 422,358（最強） | 対 10 万局版 |
|---|---|---|---|---|---|
| 窓 10 万局（本番と同じ、§6） | 0.36 → 0.32 | 0.95 / 0.39 | −38 | −65 | — |
| 窓 100 万局 | 0.62 → 0.60 | 0.75 / 0.58 | **+297**（+269〜+329） | **+286**（+258〜+317） | **+306**（+277〜+339） |

- 見立て（ネットが窓の中の局の結果を記憶して一般化していない）は支持された（事実）。13 分の学習で、5 日間の本番の伸び（基準比 +220）を超える。
- 100 万局の窓でも損失はほとんど下がらないので、これは「記憶をやめた」効果であって、100 万局の窓が最適という意味ではない（推測）。窓・学習量・学習率の値は §4 の候補として煙テストで決める。
- 200 万局面（20 チャンク）を読む記憶の輪郭（CPU、1 分）で、この差は対局の前に見えていた。§3 の M1 に組み込む。

## 1. 今回の失敗の根（何が起きたか、ではなく、なぜ防げなかったか）

| # | 根 | 事実 |
|---|---|---|
| R1 | **学習則の設定を出所なしで決めた** | method-evidence.md で「記録なし」「先例なし」が 20 か所。窓 10 万局・replay_ratio 4・AdamW 2e-4 一定・σ の係数・布石の価値目標は、9/11 の実装時に理由の記録なく決めた |
| R2 | **一般化を測る物差しが無かった** | 見ていたのは窓の中の損失だけ。窓の中の損失は記憶でも下がるので、記憶と一般化を区別できない。窓の外の局面で測れば 9/14 に分かった（status §10.1） |
| R3 | **強さの物差しが弱かった** | 100 局（±75 Elo）、比べる相手は直前と古い基準だけ、外部の計測は得点 0 に張り付き。50 Elo の後退が 2 日見えず、向きが逆に出た日もあった |
| R4 | **見直しの時点が遠かった** | 計画は「11 月末に頭打ちなら見直す」（libra-local.md §10）。1 週間で頭打ちになっても、それを判断する定例が無かった |
| R5 | **学習に関わる変更を、強さの計測なしで重ねた** | 9/14 の 1 日に 5 つ。「棋譜は同じ」を本番の大きさで確かめていない変更もある |
| R6 | **起動前の煙テストが無かった** | 小さな run で「一般化の物差しが正しい向きに動く」ことを確かめずに本番を起動した |

## 2. 初期検討（本番を起動する前に済ませること）

起動の条件: 下の C1〜C5 がすべて済み、結果が docs に書いてあること。

| # | 内容 | 出来上がり |
|---|---|---|
| C1 | **設定表の完成**: 学習則・探索・目標・ネットの形の全項目に「出所（一次資料）・理由・確かめる計測（何を・いつ・閾値）」を書く。「記録なし」が 1 つでも残っていたら起動しない | method-evidence.md の表を新しい run の設定で作り直す |
| C2 | **一次資料の照合**: AlphaZero・KataGo（論文と KataGoMethods）・Gumbel（論文と mctx）・Lc0 の学習設定を、値ごとに原典の文で引く。読めなかったものは「未確認」と書く | 同上の「文献」列 |
| C3 | **煙テスト**: 本番と同じコードで、小さい設定（例: 6〜12 時間、ネットは本番と同じ）を回し、§3 の物差しが (a) 窓の外の価値の相関が上がる、(b) 最強比の Elo が上がる、の両方を満たすことを確かめる。満たさなければ本番を起動しない | measurements.md に 1 行、決定は decisions.md |
| C4 | **テスト**: 学習目標の符号・視点（先手／後手、手番側）、鏡映と方策の添字の往復、`soft_wdl`／z の対応、窓の外の当てはまりを測るコマンド、の単体テスト | pytest |
| C5 | **変更の規則**: 学習データに関わる変更は 1 日 1 つまで、前後で §3 の物差しを比べてから次を入れる。「棋譜は同じ」は本番の大きさで確かめる | CLAUDE.md に 1 行 |

## 3. 早期検出（run に組み込む物差しと、止める・見直す閾値）

| # | 物差し | 頻度 | 閾値（案） | 実装 |
|---|---|---|---|---|
| M1 | **一般化**: 学習に使わない held-out の局（例: 50 チャンクに 1 つを窓に入れない）と窓の中の局で、価値の相関・二乗誤差・方策の交差エントロピーを出し、差を metrics.jsonl とコンソールに描く | 1 時間 | 窓の外の相関が 24 時間上がらない、または中との差が広がり続けたら注意。本将棋で外 < 0.6 が 2 日続いたら見直し | ランナーに `held_out_every_chunks` と計測を足す（今日の memprof.py の手順） |
| M2 | **最強比の Elo**: 毎日、その日の重みを「これまでで最強の保存済み」と 1,000 局（GPU 共有で約 10 分 × 1）。100 局の直前比はやめる | 24 時間 | 3 日続けて最強を更新しなければ注意、5 日で見直し | auto.py の対局数と相手の選び方 |
| M3 | **固定基準比**: 基準（run の初期の保存）との 1,000 局 | 24 時間 | 伸びが 1 週間で +30 未満なら見直し | 同上 |
| M4 | **絶対の物差し**: 変化が見える強さの固定相手（例: 読み 1 回のネット単体 v0.1、または詰み探索だけの相手）との 200 局。外部の強い AI との対局は得点が 0 に張り付くので、成長の物差しにしない | 24 時間 | 勝率が 1 週間上がらなければ見直し | `libra eval` に固定相手を足す |
| M5 | **人との対局**: ユーザーが desktop で 3 局 | 週 1 | 感想を decisions.md に 1 行 | — |
| M6 | **定例の見直し**: 週 1 回、M1〜M5 を 1 枚にまとめて「続ける／設定を変える／止める」を決める | 週 1 | — | docs/weekly/ に 1 ファイル |

## 4. 学習則の候補（ユーザーの決定。各行の「確かめる」を C3 の煙テストで先に見る）

| 項目 | 今 | 候補 | 出所 | 確かめる |
|---|---|---|---|---|
| リプレイの窓 | 10 万局固定（約 920 万局面） | 総局数に応じて広げる。最初 25 万局面相当、総局数の 30〜50%、上限は RAM（1 局 約 5 KB、100 万局で 5 GB） | KataGo [Wu19]: 25 万 → 2,200 万局面 | M1 の窓の外の相関 |
| 学習量 replay_ratio | 4 | 1〜2 | AlphaZero: 70 万 step × 4,096 ÷（2,400 万局 × 約 100 局面）≈ 1.2 回（計算、未確認）。KataGo: 記載なし | 同上 |
| 最適化・学習率 | AdamW 2e-4 一定 | SGD 慣性 0.9 と段階的な学習率（AlphaZero・KataGo 形）。AdamW を残すなら学習率を下げて途中で落とす | [Silver18][Wu19] | 同上、M2 |
| Gumbel の σ | 係数 1.0、正規化なし、未訪問は根の値 | mctx の既定: 子の間で [0,1] に正規化、係数 0.1、未訪問は v_mix | mctx `qtransform_completed_by_mix_value`（原典コード） | 目標の最大確率 0.99 超の割合、M2 |
| 布石の価値目標 | 0.5·z ＋ 0.5·v41（v41 と z の相関 0.2） | z だけ。V̂41 は補助ヘッドに残す（KataGo 形） | [Wu19][KGM]、method-evidence §4.3 | M1（布石）、玉配置表の V̂ − 検証勝率 |
| 学習する局面 | 全局面 | 全読みの手だけ（価値も）か、今のまま | [Wu19] | M1 |
| 読みの回数 | 96 / 24、全読み 25% | 今のまま（計算量の制約） | 計画 §3.1 | — |
| ネットの形 | d 320・8 層・10M | 今のまま | 計画 §3.2 | — |

## 4.1 実装の状況（2026-09-17 夜）

| 項目 | 状況 |
|---|---|
| 窓の拡大（`window_frac`・`window_games_max`）、held-out、一般化の物差し `gen`・`libra genprof` | 済（bb7fc07） |
| 最強比 `best_games`、固定の参照 `reference_ckpts` | 済（88ede39）。コンソールの Elo タブに参照の線、要約に最強比 |
| σ の mctx 形 `gumbel_rescale` | 済（5c0bcc8、既定 off） |
| 設定表（C1・C2）と設定の案 | docs/ls2-settings.md、docs/ls2-config.toml |
| 目標の符号のテスト（C4） | test_targets_are_from_the_side_to_move |
| 煙テスト（C3） | 起動待ち（§5 の 2） |

## 5. やり直しの手順（案）

1. 今の ls・lx は止めたまま `~/libra-run/ls-v1`・`lx-v1` に改名して残す（再開しない。ls-v0 と同じ扱い。棋譜（CC0）は残す）。管理コンソール・bat・タスク スケジューラは run-id `ls`・`lx` を見るので、新しい系列も `ls`・`lx` の名前で作る（改名はユーザーの了承を得てから）。
2. 新しい `ls` を docs/ls2-config.toml で乱数初期化から始める（`bin/libra run --config docs/ls2-config.toml`。起動はユーザーがコンソールから）。最初の 8 時間が煙テスト（docs/ls2-settings.md §5）。合格ならそのまま本番として続け、`[auto] every_hours` を 24 に戻す。搾取者 lx は本体が M2 で伸び始めてから足す。
3. 起動の前に §2 の C1〜C5（C1・C2・C4 は済。C5 は CLAUDE.md に足す）。
4. 起動後は §3 の物差しと週 1 の見直し。1 週間で M2 が伸びなければ止めて設定を見直す（11 月末まで待たない）。

## 6. 段階と受け入れ基準（日程ではなく基準で進める。2026-09-17 のユーザーの指示）

| 段階 | 状態 | 次へ進む条件 |
|---|---|---|
| 準備（C1〜C5） | 済（9/17） | — |
| 煙テスト | 9/17 19:54 起動、総局数 30 万局まで | docs/ls2-settings.md §5.1 の合格（review が「続ける」、M2 が 2 回続けて更新、M4 が上がる） |
| 本番 | every_games 400,000 | 100 万局以上で最強比が 3 回続けて「続ける」→ 仮の設定値の比較と lx の追加（§5.2） |
| 見直し | review の「見直し」か 200 万局ごと | M1〜M4 の値で決める |

計測はすべて局数で区切る（`[auto] every_games`、`[run] gen_games`、review の窓 `gen_games`・`reference_games`）。PC の利用状況で局/日が変わっても、判断に要る局数がたまったときに測る。

## 7. 別のセッションからの提案（2026-09-17 夜、TypeSafe/Jev の文書の読み込みから。Jev 自体は使わない）

採用して §3 の続きの実装に入れるもの:

| # | 内容 | 対応 |
|---|---|---|
| P1 | 較正（偏り）と判別（相関）は別物。calib.jsonl の偏り −0.02 は「価値が効いている」証拠にならない。健康指標は held-out の相関（M1）を主にし、ECE と並べて出す | M1 の表示に calib の ECE を並べる（コンソール）。Brier の分解（reliability / resolution / uncertainty、Murphy 1973）は候補 |
| P2 | 自動の外部計測が `latest.onnx`（動く別名）で打たれ、計測ごとに重みが違って時系列の比較にならなかった | 外部計測を再開するとき（本番で match_games 10）は archive の重みを書き出した ONNX で打ち、結果の行に step を必ず書く。閾値を変えた step も記録する |
| P3 | 学習目標の最大確率 0.99 超が 36〜41% でも corr(v41, z) は 0.2。鋭さ（確信度）を強さの指標と取り違えない | 済（§10 の見立てと ls2-settings.md §2 で、鋭さは σ の比較の物差しにだけ使う） |
| P4 | 損失 1 本ではなく M1〜M4 の生の値を毎日保存し、「続ける／変える／止める」を閾値を持つ 1 つの関数（`libra review`）で出す。閾値を変えても再計測が要らない形 | M6 の実装として `libra review` を作る（metrics の gen、eval の best・anchor・reference を読み、ls2-settings.md §5 の合否を出す） |
| P5 | 固定局面 N 個で旧／新ビルドの方策・価値・根の Q を数値で比べる一貫性ハーネス（「棋譜は同じ」の本番規模の確認、R5・CLAUDE.md 11） | 作る（`libra consistency`: 同じ seed・同じ設定の自己対局の記録と、固定局面の探索結果の一致） |

見送り: 根の子の Q の差で読みを延ばす／高い確信度でだけ投了する（探索・学習則に触れるので CLAUDE.md 8・10 の経緯調査が先）、モデルカードの「既知の弱点」節（公開の準備で扱う）。

