# 実測値

見積もり（docs/libra-local.md）との差を残す。日付、項目、値、条件。

| 日付 | 項目 | 値 | 条件・備考 |
|---|---|---|---|
| 2026-09-11 | libra-sim perft 速度 | 平手 depth 5: 19,861,490 ノード 1.17 s（17 Mnps）。布石 depth 2〜3: 900〜1,400 Mnps | Ryzen 9 9950X3D、g++ 13 -O3 相当（Release）、単一スレッド |
| 2026-09-11 | 合法手の黒箱テスト（desktop wasm 12c4a8d との突き合わせ） | 400 局 × 40 手で合法手集合が全一致。40 手目の制限 75 件、遮断不能（41 手目の裁定）69 件も一致 | seed 0。天秤 300 局・布石将棋 100 局、半数は先手が後手玉に当てる手を優先 |
| 2026-09-11 | libra-search CPU 側（一様方策、GPU なし） | 512 局同時・16 スレッドで 30,000 葉/秒 | Ryzen 9 9950X3D。1 葉 = 選択＋展開＋特徴書き出し |
| 2026-09-11 | 推論速度 | d=256・6 層（5.0M）: batch 512 fp16 で 10.0 ms（51,000 局面/秒） | RTX 5070 Ti、PyTorch 2.11 cu128、torch.compile なし |
| 2026-09-11 | L-S 構成のベンチ（10.2M、512 局同時、12 スレッド、学習なし、5.5 分） | 20,600 評価/秒、756 局、平均 149 手、41.3 sim/手、局/日換算 197,851（直近 1 時間換算） | 初期ネット（乱数）。GPU 使用率 77%、241 W。実行計画 §3.2 の理想値 16 万・現実値 8 万に対し、学習の時分割込みでも 10 万超の見込み |
| 2026-09-11 | L-S 起動 | 12:36 に `~/libra-run/ls` で開始（Task Scheduler「LibraShogi run」登録済み） | 1 週間後に局/日を記録する |
| 2026-09-11 | 証明探索の CPU 負荷（一様方策、512 局、3500 ラウンド） | 有効 30,222 葉/秒、無効 31,748 葉/秒（約 5% 減）。有効時は根の詰み探索が 475 件、布石終盤の証明 2,442 件（呼び出し 143,895 回、平均 4.2 節点） | mate_nodes_root 200、proof_nodes 1000、proof_min_ply 36。L-S 稼働中の測定なので絶対値は低め |
| 2026-09-11 | L-S 稼働 1 時間（学習込み） | 9,172 局 / 0.93 h → 236,385 局/日換算。平均 131.7 手、先手 4,863・引き分け 1,113・後手 3,196、ruling41 2,060、詰み 5,999、256 手 1,113。学習 130 ステップ 25 秒（約 190 ms/ステップ、バッチ作成が律速） | 12:36〜13:32、証明探索なし。13:40 に証明探索を有効にして再開（step 5,696、10,601 局から） |
| 2026-09-11 | 計測ハーネスの煙試験（`libra match --games 2 --go "movetime 1000"`） | Libra（1 時間学習、step 5,440）は 2 局とも詰まされて負け（47・50 手）。1 局 30 秒 | 相手 Tenbin Fuseki Engine 0.1（既定設定、cuda）＋やねうら王 V9.00 相当 Linux ビルド＋vendor の nn.bin |
| 2026-09-11 | 世代間 Elo（`libra eval`、100 局、96 sim） | step 0（乱数）対 step 5,696（1 時間学習）: 得点 0.18、Elo 差 −263（95% CI −373〜−186）。先手番 17-0-29、後手番 1-0-53。終局理由 ruling41 41・詰み 59、平均 55.6 手。較正: step 5,696 は 0.9〜1.0 の予測で実勝率 0.94、0.4〜0.6 で 0.72〜0.92（楽観） | L-S と計測ハーネスの稼働中に測定（262 秒） |
| 2026-09-11 | 基準値マッチの初回（`libra match --games 20 --go "movetime 1000"`、13:41〜13:52） | Libra step 5,696（学習 1 時間）: 0 勝 20 敗。置く側・選ぶ側交互（Libra が置いたのは奇数局）。全局が本将棋の詰み（41〜58 手、平均 50 手）。相手が選ぶ側だった 10 局は先手 7・後手 3 を選択、Libra が選ぶ側の 10 局は先手 4・後手 6 | 相手 Tenbin Fuseki Engine 0.1 既定（`Fuseki_Method=value`、cuda）＋やねうら王 Linux ビルド（vendor nn.bin）。1 局約 30 秒。パイプライン検証が目的で、実行計画の基準値（10 月中旬）ではない。棋譜 `~/libra-run/ls/matches/20260911-134142.jsonl` |
| 2026-09-11 | L-S 14:21 に手数上限を 256→320（41 手目起点）に変更して stop → run（step 9,685、19,926 局） | 直前の 1 時間（証明探索あり、バッチ先読みあり）は 352,411 局/日換算、平均 73.4 手、上限到達 16 局 / 6,917 局。上限変更で局/日への影響はほぼ無い見込み（上限到達は 0.2%） | 以後の局/日は 320 手規定での値 |
| 2026-09-11 | C++ エンジン `libra`（ONNX Runtime 1.30.0、fp32、1 葉ずつ評価）の思考速度。L-S を pause して GPU を空けて計測 | CUDA EP: 布石 1,600 ノード 5.9〜6.2 秒（3.7〜3.9 ms/評価、約 265 評価/秒）、本将棋 1,600 ノード 6.2 秒。Python 版（PyTorch fp16）: 1,600 ノード 7.0 秒（4.4 ms/評価）。CPU EP（4 スレッド）: 400 ノード 2.4 秒（6 ms/評価）。Windows 版 `libra.exe`（CPU）: 100 ノード 0.67 秒 | バッチ 1 の遅延律速。複数葉の同時評価と fp16 で 5〜10 倍の余地。L-S 稼働中は 10〜22 ms/評価に落ちる |
| 2026-09-11 | 玉配置表 v0 の探索値（L-S step 12,284、492 ペア × 1,600 sim、512 並列、L-S 稼働中） | 約 5 分。V̂ は最小 0.222・中央値 0.487・最大 0.619、V̂ ≥ 0.5 は 198/492。自己対局の一様サンプル（ペアあたり中央値 43 局）の実測勝率との相関 0.70。|V̂−0.5| ≤ 最小＋0.02 の釣り合い集合は鏡映込み 263 | 若いネットで値が 0.5 付近に密集している。世代が進めば作り直す |
| 2026-09-11 | GPU 共有時の L-S の局/日（15:05〜15:20、搾取者 lx 64 局＋玉配置表の検証対局 256 局と同居） | 約 16.3 万局/日（単独時 32〜35 万の半分） | 1 週間の実測はこの共有を含む値になる。lx と検証は本体の約 1/10〜1/3 の計算を使う |
| 2026-09-11 | 搾取者 lx（1.9M、ゼロから、64 局同時）の対本体（L-S step 13,021 凍結）勝率 | 起動 0.2 h・308 局: 9.1% → 1.34 h・2,583 局（学習 1,370 ステップ）: 27.8% | 設計書 §4.2 の指標。頭打ちの水準を見る。本体を差し替えたら測り直す |
| 2026-09-11 | 玉配置表 v0 の検証対局（|V̂−0.5| 上位 48 ペア、各 100 局以上、96 sim、256 並列、L-S・lx と GPU 共有） | 6,247 局 / 101 分。先手勝率は最小 0.390・中央値 0.543・最大 0.703、95% 区間の半幅 ±0.085。釣り合い集合（区間が最小値と重なる）は鏡映込み 72。V̂ と検証勝率の相関 −0.07、自己対局の一様サンプル（30〜60 局）との相関 0.08 | 上位 48 の V̂ は 0.493〜0.507 に密集しており差は雑音以下。この世代では探索値でペアを絞れず、検証対局の局数が物差し。最終世代では 1 ペア 10^4 局規模（設計書 §5）が要る。先手が平均 54% 勝つ |
- 2026-09-12 自動自己評価の初回（ls、archive step 0 = 乱数初期化 vs step 98,368、100 局、96 sims、同時 64、GPU は自己対局と共有）: step 98,368 側の勝率 93%、Elo +449（95% 区間 +346〜+676）、所要 164 秒。終局は ruling41 27 / 詰み 73、平均 49.1 手。自己対局は評価中も継続（局/日の低下は数分間のみ）。
- 2026-09-12 自動外部計測の初回（有効分。ls step 約 99,000 の latest.onnx、C++ エンジン、movetime 1000、相手 fuseki_usi_server.py Threads=2、10 局、GPU/CPU は自己対局と共有）: 0 勝 10 敗（先手 0/8、後手 0/2）、全局 詰み、46〜89 手、1 局 50〜70 秒、全体 11 分。12:02 の同設定 10 局は latest.pt 指定の不具合で全局 resign（無効）。
- 2026-09-12 23:20 の ls 稼働 34.5 h 時点: step 146,915、世代 211、累計 455,541 局、局/日（1h）333,000（搾取者 lx 95,563 局/日と GPU 共有）。平均 84.1 手、先手 33,677・引き分け 218・後手 22,073（session）、ruling41 12,703 / 詰み 43,046 / 千日手 1 / 連続王手 1 / 320 手 217。学習 loss 2.71〜2.94（policy_acc 0.37→0.44、v41 0.53〜0.57 で 12 時間ほぼ横ばい）。実行計画 §3.2 の現実値 8 万局/日に対し 4 倍。
- 2026-09-12 基準比 Elo の 1 点目（archive step 105,669 = 基準 vs step 128,711、100 局・96 sims）: 勝率 0.55、基準との差 +34.9 Elo、累積 +491.2（95% 区間 +423〜+562）。6.4 h 分の伸びが測定幅（±70 Elo）の半分で、想定どおり 1 日分では有意差にならない。
- 2026-09-12 搾取者 lx の凍結相手を step 127,235 に作り直した直後からの対本体勝率（15 分窓、各約 1,000 局）: 6.6%（18:51）→ 19%（20:00）→ 30%（21:00）→ 41%（22:00）→ 52.6%（23:19）。4.5 h・約 17,000 局で 50% を超えた（前の周期は凍結 step 13,021 に対し 97.9% まで到達）。1.9M のネットがゼロから 5 時間で互角になるので、本体にはまだ突ける穴がある。
- 2026-09-12 自動外部計測 2 回目（ls step 約 128,000、latest.onnx、movetime 1000、相手 Threads=2、10 局）: 0 勝 10 敗（先手 0/8、後手 0/2）、全局 詰み負け、52〜100 手（1 局目 12:12 の 10 局は 46〜89 手）。勝ちの兆しは無いが手数は伸びている。
- 2026-09-13 07:21:30 搾取者 lx が異常終了（前回起動 00:04 から 7.3 h、ls と GPU 共有）: Windows System ログに nvlddmkm Id 13 ×3（`Graphics Exception`、`Graphics SM Global Exception: Multiple Warp Errors`、`Graphics SM Warp Exception: MMU NACK Errors`）と Id 153（`Error occurred on GPUID: 100`）。WSL の dmesg で python が signal 6、クラッシュ ダンプ（2.8 GB）に `CUDA error: an illegal memory access was encountered`。ls は影響なし（07:27 以降もチェックポイントを継続）。タスク スケジューラの「失敗時に再起動」は働かず、12:05 にユーザーが起動するまで 4.7 h 停止。失ったのは最終チェックポイント 07:15:45（step 115,209）以降の約 400 局と、停止中の約 2.3 万局（11.5 万局/日 × 4.7 h）。nvlddmkm のエラーは過去 14 日で 2 回（もう 1 回は 9/7 05:51、ラン開始前）。停止中の ls は 40.4 万局/日（lx と共有時は 33〜35 万局/日）。
- 2026-09-13 自動自己評価 2 回目（ls、12:49 の archive、100 局・96 sims・同時 64、GPU は ls・lx と共有）: 鎖 step 128,711 vs 209,429 は新しい側の勝率 0.65、+107.5 Elo（95% 区間 +39〜+185）、808 秒。基準比 step 105,669 vs 209,429 は勝率 0.62、基準との差 +85.0、累積 +541.3（95% 区間 +474〜+616）、298 秒。鎖（128,711 から +107.5）と基準比（105,669 から +85.0、前回の鎖 +34.9）は区間の内側で食い違うだけで、1 日で有意に伸びた（前回の累積区間 +423〜+562 と重なりは小さい）。値の較正は新しい側で予測 0.9〜1.0 の実勝率 0.86〜0.90 と過信気味。所要時間の 2.7 倍差の原因は未確認。
- 2026-09-13 13:59 の稼働状態: ls は 49.1 h・step 213,965・世代 297・累計 662,955 局、局/日（1h）311,847（lx 93,331 と共有）、平均 85.4 手、session の ruling41 44,848 / 詰み 152,922 / 千日手 4 / 連続王手 6。lx の対凍結本体（step 127,235）勝率は 12:05〜13:56 の 6,401 局で 74.5%（凍結からの累計 59,069 局で 59.6%）。本体が step 21 万まで進んだので凍結相手は 9 万 step 古い。外部計測は前回 9/12 17:34 から every_hours 24 のため 9/13 17:34 以降に積まれる予定で、この時点では未実施。
- 2026-09-13 二飛香（Issue #1）に当たる陣の採用率（旧ルールの自己対局の sfen41 から数えた。陣＝1 局の先手・後手それぞれ、分類は 3連 > 香香 > 飛香）: ls 7,100 チャンク（71 万局）を 10 等分して、当たる陣は 22.0% → 44.3%（3/10）→ 65.2%（8/10、最大）→ 61.4%（直近 1/10。3連 7.3・香香 42.0・飛香 12.1）。lx は直近 1/5 で 85.3%（3連 14.9・香香 63.4・飛香 7.0）。ls の直近で初めて当たるのは、自分が置いた駒（玉を除く）の中央値 3 枚目・早い 1 割で 2 枚目。片方だけ組んだ局の組んだ側の勝率差は、組まない同士（先手 83.4% 勝ち）と比べた先後平均で +0.4pt だが、組まない同士の局に玉配置の偏りが混ざっており、この比べ方では支配戦略かどうかは言えない（Issue の布石将棋AI は +11.4pt、主な形は 3連）。Libra の主な形は 3連ではなく香香。ls・lx は 17:35 に停止し、以後は二飛香で学習し直す。
- 2026-09-13 19:04 二飛香が稼働中のランで効いていることの確認（新系列の games_*.jsonl の sfen41 で筋ごとに自分の飛・香を数えた）: ls 13,500 局・lx 6,200 局で違反の陣 0/27,000・0/12,400。同じ走査で旧系列の直近 50 チャンクは ls-v0 59.3%・lx-v0 84.7%（前回の 61.4%・85.3% とほぼ一致し、走査が働くことの対照）。40 手以内の no_legal_move は ls 34 局・lx 197 局（3.2%、旧 lx は 50 チャンクで 5 局）で、libra-sim で再生するとすべて 40 手完了後の先手が王手・合法手 0（41 手目の詰み、後手勝ち）。布石中に打つ手が尽きた局は 0。ls が読み込んでいる _sim.so（17:45 ビルド）で Issue の確認用の手順を再現、scratchpad の別ビルドで test_rules 367 件・perft 全通過、CI は ebcdd8e で緑、libra.exe は 17:56 のビルド（コミットより後）。
- 2026-09-13 19:19 合法手の黒箱テスト（`blackbox_wasm.py 400 <seed>`、seed 0〜2、ls・lx と CPU 共有）を desktop 0.8.0 の wasm（`~/tenbin-shogi-desktop` ece8310、wasm は 1b5b59c）と二飛香の旗付きで: 1,200 局（天秤将棋 900・布石将棋 300）× 40 手の合法手集合がすべて一致、40 手目の制限 257 局、41 手目の裁定 231 局も一致。旗を渡さない（`fw_reset()`）と天秤将棋の布石 6〜9 手目で wasm だけが二飛香の飛・香打ちを返して食い違う（3 seed とも）ので、比較が二飛香を見分けている。
- 2026-09-13 20:07 の律速の確認（ls 512 局・12 スレッド、lx 64 局・4 スレッドと共有）: GPU 使用率 84〜85%、237〜241 W（電力上限 250 W、既定 300 W）、CPU は 32 スレッドのうち約 91% が空き（使用は 3 コア弱）。自己対局は GPU が律速で、CPU は GPU 3〜4 枚分を賄える余裕がある。推論は PyTorch fp16 で torch.compile・CUDA Graphs なし、注意は `attn_mask` 付き SDPA のため Flash Attention のカーネルに乗らない。
- 2026-09-14 01:28 自己対局の推論ベンチ（ls・lx を一時停止、RTX 5070 Ti 単独、ls step 260,795 の重み、fp16、1 ラウンド = 固定形状の H2D → forward → D2H と softmax）: eager 20.8 ms（同時 512、24,662 評価/秒）。同時 768・1024 でも 24,177・24,198 評価/秒で、512 の時点で GPU の演算が飽和している（同時局数を増やしても伸びない）。CUDA Graphs だけ ×1.01、torch.compile（default）＋CUDA Graphs ×1.07、compile（max-autotune-no-cudagraphs）＋CUDA Graphs ×1.14（28,073 評価/秒、起動時の autotune 5〜25 秒）。出力差は fp16 と fp32 の差（方策ロジット最大 0.11、範囲 ±52）と同程度。forward の GPU 時間の内訳（eager、同時 512）: 行列積（cutlass の sm80 用 gemm）57%、注意（mem-efficient、バイアス付き）15%、要素ごとの演算（GELU・加算）17%、LayerNorm 10%。SDPA は efficient 19.9 ms・cuDNN 19.5 ms・math 42.9 ms。CPU 側（collect＋apply）は 1 ラウンド約 2.9 ms（別途 CPU で計測）。予定（1.3〜2 倍）に対し、演算の融合で得られるのは 14% 程度で、残りは行列積が律速。
- 2026-09-14 01:43 搾取者の形（64 局、自分 192/4・凍結相手 ls の 320/8、fp16、ls・lx は一時停止中で GPU 単独）の 1 ラウンドの推論（評価と D2H、手番側の振り分けは毎回乱数）: 旧経路（枠を分けて eager）3.28 ms、新経路（両ネットで全枠を CUDA Graphs、max-autotune、行を選ぶ）2.87 ms（×1.14）。凍結相手を通る枠は 2 倍になるが、捕獲による呼び出しの手間の削減が上回る。CUDA Graphs のメモリ（ls の形、同時 512）: forward のピーク 327 MiB（eager 470 MiB）、捕獲後のプールの予約 412 MiB、release と empty_cache の後は 22 MiB に戻る。
- 2026-09-14 02:37 本番での compile＋CUDA Graphs の初期値（ls・lx とも GPU 共有・学習込み、metrics.jsonl の games_total の差）: 変更前（9/13 22:00〜9/14 01:25、eager、約 3.3 h）ls 323,112・lx 118,987 局/日（計 44.2 万）、変更後（02:20〜02:37、10 分のみ）ls 317,902・lx 151,667 局/日（計 47.0 万、+6%）。平均手数は ls 88.4・lx 96.7（変更前の lx は約 86）で、10 分では手数の揺れと起動直後の影響を区別できない。GPU 単独の 1.14 倍より小さいのは GPU 共有と学習の時分割を含むため（見込み）。悪化は無いのでこのまま運用する（ユーザーの判断）。
- 2026-09-14 ワーカー用の重みの配布（`publish_weights`、ls の形 10.23M、CPU のみ・2 スレッド・nice 10、ls・lx 稼働中、WSL の ext4）: ファイル 19.6 MiB、書き出し 22 ms（中央値、5 回）、読み込み 59 ms。学習のたび（ls で約 69 秒ごと）に書いても学習側の時間への影響は無視できる。GPU 上のモデルからの書き出しは、これに GPU から CPU への転送が加わる（未計測）。
- 2026-09-14 05:14 vast.ai の RTX 3090 1 台での自己対局ワーカーの実測（アルゼンチンのホスト、オンデマンド $0.155/h、CPU 32 コア相当を他の利用者と共有・負荷平均 24〜32、torch 2.11.0+cu128、`vastai/pytorch` イメージ、ls の step 272,502 の重み、本番と同じ探索設定、同時 512 局・12 スレッド、推論は compile(max-autotune)＋CUDA Graphs、20 分のうち起動 300 秒後からの 856 秒で 2,000 局）: **191,830 局/日**、平均 85.2 手、GPU 使用率 98%・341 W（上限 350 W）。100 万局あたり **$19.4**。予定（9/14 の検討: 3090 で 42〜54 万局/日、100 万局あたり $6.0）の 0.36〜0.46 倍。1 手 39.4 回の評価として約 7,400 評価/秒・1 ラウンド約 69 ms（手元の 5070 Ti は GPU 単独の forward 20.8 ms＋CPU 2.9 ms）。1 ラウンドの転送は H2D 5.1 MiB・D2H 4.4 MiB。遅い原因（GPU の演算、PCIe、CPU の取り合い）は未切り分け。借りるまでの失敗 4 回（接続確認の不具合、pytorch/pytorch イメージの取得停滞、sshd の authorized_keys の権限、vast.ai の中継への逆向きトンネルの失敗）を含めた支出は約 $0.11。
- 2026-09-14 05:54 vast.ai の RTX 5070 Ti 1 台での自己対局ワーカーの実測（カナダ・ケベックのホスト、オンデマンド $0.195/h、Xeon E5-2680 v4 ×2・28 コア相当・動作 約 1.7 GHz、ホスト全体の CPU は 93% 空き、PCIe Gen3 x16、電力上限 300 W、ほかは 3090 と同じ条件）: **351,162 局/日**（起動 300 秒後からの 861 秒で 3,600 局、平均 84.7 手）、100 万局あたり **$13.3**。GPU 使用率 43%・158 W。ワーカーの段ごとの時間（定常、60 秒ごと）: 約 26 ラウンド/秒・13,500 評価/秒、1 ラウンド collect 2.2 ms・eval（H2D・forward・D2H）16.8 ms・apply 17.6〜18.8 ms。**GPU は手元より速い（手元の forward 20.8 ms）が、CPU の apply で頭打ち**。起動（イメージの取得）に 737 秒。3090 も同じ世代のサーバー CPU（EPYC 7532、2.4 GHz）で、同じ原因の可能性が高い（3090 では段ごとの時間を取っていない）。支出は 3090 と合わせて約 $0.18。
- 2026-09-14 05:47 自己対局エンジンの collect / apply のスレッド数による差（手元の CPU のみ・nice 10・ls・lx 稼働中、ls の探索設定、512 局、乱数のロジット、布石の後半まで回した定常状態で 300 ラウンドの平均）: apply は 1 スレッド 9.95 ms、2 スレッド 6.03 ms、6 スレッド 3.69 ms、12 スレッド 3.55 ms、24 スレッド 3.70 ms。collect は 4.84 / 2.62 / 1.29 / 1.14 / 1.75 ms。6 スレッド以上では速くならず約 3.5 ms で下げ止まる（1 ラウンドは最も重い 1 局の df-pn 証明探索を待つか、呼び出しごとのスレッド生成の手間の見込み、未確認）。乱数のロジットなので本番の探索木とは形が違う。01:28 の「CPU 側 2.9 ms」は定常状態ではなかった可能性がある。
- 2026-09-14 06:05 同じ条件で df-pn を切った比較（`proof_nodes = 0`・`mate_nodes_root = 0`、手元の CPU のみ）: apply は 1 スレッド 5.16 ms、2 スレッド 2.85 ms、6 スレッド 1.31 ms、12 スレッド 1.23 ms（collect は 4.97 / 2.77 / 1.28 / 1.19 ms）。証明探索なしではスレッド数にほぼ比例して下がり 約 1.2 ms で下げ止まる。証明探索ありとの差は 1 スレッドで 4.8 ms、12 スレッドで 2.3 ms で、スレッドを増やしても縮まない。**1 ラウンドは重い証明探索を抱えた少数の局を待っている**（`parallel_for` は 512 局を静的に 12 分割し、全ブロックの join を待つ）。この待ちは 1 スレッドの速さで伸びるので、遅い CPU のホストで apply が 18 ms になった説明と合う（未確認）。
- 2026-09-14 06:26 vast.ai の RTX 5070 Ti を速い CPU のホストで実測（スペイン、オンデマンド $0.190/h、Ryzen 5 5600（6 コア 12 スレッド、最大 4.47 GHz）、ドライバー CUDA 13.2、ワーカー 8 スレッド、ほかは 05:54 と同じ）: **476,019 局/日**（起動 300 秒後からの 889 秒で 5,000 局、平均 84.2 手）、100 万局あたり **$9.6**。GPU 使用率 58.5%・196 W。段ごとの時間（定常）: 約 35 ラウンド/秒・18,000 評価/秒、collect 1.3 ms・eval 16.5 ms・apply 9.4〜10.5 ms。Xeon E5-2680 v4 のホスト（apply 18 ms、351,162 局/日）より 1.36 倍だが、まだ CPU の apply で GPU が 4 割遊ぶ。平均手数が手元の ls（89.0 手）より 5% 短いので、手元と局/日で比べるときは 5% 割り引く。同じ日の前の試行: CUDA 12.8 までのドライバーのホスト（ベトナム、Threadripper PRO 5955WX）では CUDA 12.9 のイメージが GeForce で前方互換を使えず「Error 804」で GPU を初期化できなかった（$0.007）。vast.ai の支出はここまで合計 約 $0.26。
- 2026-09-14 07:02 自己対局エンジンの 1 ラウンドの内訳（手元の CPU のみ・ls・lx 稼働中、ls の探索設定、512 局、乱数のロジット、定常状態で 300 ラウンドの平均、一時的な計装ビルド）: 12 スレッドで apply 2.9 ms のうち、最も遅い 1 局 1.95 ms、2 番目 1.15 ms、全局の合計 ÷ スレッド数 0.70 ms、静的に等分した組のうち最も遅い組 2.50 ms。最も遅い局はほぼ毎回、手番の初めの本将棋の詰み探索（200 ノードを使い切って不詰、40〜51 手目、1 ノード約 9 µs）で、布石の証明探索のノードは 0。1 ms を超える局は 1 ラウンドに平均 1.7 局。常駐スレッドと動的な割り当てに変えた版との比較（同じ条件で続けて測定）: 12 スレッド collect 1.06 → 0.57 ms、apply 3.10 → 2.22 ms（CPU 側 4.16 → 2.79 ms、1.49 倍）、24 スレッド apply 2.90 → 2.47 ms、6 スレッド 3.17 → 2.57 ms。apply は最も遅い 1 局（約 2 ms）で下げ止まる。GPU の評価が 16.5 ms の vast.ai の 5070 Ti（Ryzen 5 5600、apply 約 10 ms）でどれだけ局/日が増えるかは未計測。
- 2026-09-14 07:16 本将棋の根の詰み探索（自己対局が本将棋の手番の初めに毎回行う `mate_nodes_root`、tt 2^12）のノード上限ごとの効果（ls の直近 300 局の棋譜の本将棋の全局面 15,817、CPU のみ・1 スレッド・nice 10、ls・lx の起動と重なった可能性あり）: 上限 200 で詰み 787（5.0%）、不詰の確定 9,609（60.8%）、上限到達で不明 5,421（34.3%）。詰みが見つかるノード数は 10 以下 156・25 以下 324・50 以下 496・100 以下 662・200 以下 788・400 以下 871・1000 以下 1,004（上限 50 にすると 200 で見つかる詰みの 37%、100 にすると 16% を失い、1000 にすると 28% 増える）。時間は 1 局面平均 1.35 ms、うち 84% が上限到達の不明局面（平均 3.3 ms、p99 5.2 ms）。本将棋に入ってからの手数ごとの不明の割合は 0〜19 手 18%・20〜39 手 37%・40〜59 手 43%・60〜79 手 47%。1 ラウンドに本将棋の手番が約 7 回（512 局 ÷ 39.5 評価/手 × 本将棋の割合 55%）とすると、詰み探索は 1 ラウンドに合計約 9 ms の CPU を使い、遅い 1 局を待つだけでなく CPU の総量でも apply の大半を占める見込み（未確認）。
- 2026-09-14 07:45 根の証明探索の先送り（`defer_root_proof` ＋ `proof()`）の効果を、GPU の評価を 16.5 ms の待ちで模して比較（手元の CPU を 6 コア 12 スレッド（CPU 0〜11）に絞る・エンジン 8 スレッド・ls・lx 稼働中、ls の探索設定、512 局、乱数のロジット、60 秒の定常化の後 60 秒）: 先送りなし 48.4 ラウンド/秒・使われた評価 24,784/秒（collect 0.74 ms・apply 2.53 ms）→ 先送りあり 53.1 ラウンド/秒・27,186/秒（collect 0.71 ms・proof 1.93 ms（評価の待ちと重なる）・apply 0.83 ms）。**使われた評価 +9.7%**、評価枠の使用率 99.94%（証明できた手で捨てた根の評価）。9950X3D の 1 コアは vast.ai の Ryzen 5 5600（apply 約 10 ms）より速いので、そこでの伸びはこれより大きい見込み（未計測）。手元の本番は GPU の評価が約 20 ms で、CPU 側が縮む分だけ伸びる（未計測）。
- 2026-09-14 08:05 vast.ai で根の証明探索の先送りの off / on を同じホストで比べようとして借りられなかった: /bin/bash.25/h 以下の RTX 5070 Ti は韓国の 1 貸し手（host 154152、4 GPU 機 2 台、Xeon E5-2630 v3 / E5-2620 v3、実効 3〜4 コア、/bin/bash.122/h）だけで、検索に出るオファー（38692155・38692157・45957834・45957837）を 1〜2 秒後に借りても毎回「410 no_such_ask」（08:01〜08:03 に 5 回、費用 /bin/bash）。前回の Ryzen 5 5600 のホストは一覧に無く、ほかの 5070 Ti・4080・5080 は /bin/bash.54/h 以上。検索結果に借りられないオファーが残ることがある（原因は vast.ai 側で未確認）。
- 2026-09-14 08:50 C++ エンジン `libra` の複数葉の同時評価（`DNN_Batch_Size`）の思考速度（ls・lx を停止して GPU を空けて計測、RTX 5070 Ti、ONNX Runtime 1.30.0 CUDA EP、fp32、ls の 08:43 の latest.onnx、`go nodes 1600`・3 回の中央値、Threads 4、Mate_Nodes 2000）: ノード/秒は 布石 2 手目 / 布石 30 手目 / 本将棋 41 手目の順に、1 葉ずつ 449 / 434 / 413、8 葉 3,427 / 3,377 / 3,167、16 葉 5,872 / 5,632 / 5,376、32 葉 7,999 / 7,599 / 6,984、64 葉 8,531 / 8,899 / 8,840（1 葉ずつの約 20 倍、1,600 ノード 0.18 秒）、128 葉 9,119 / 8,549 / 8,508。64 より大きくしてもほぼ伸びない。疑似ネット（CPU）での 1 回の推論に入った葉は上限 8 / 16 / 32 / 64 に対し平均 8.0 / 15.9 / 30.8 / 59.3。9/11 の見込み（5〜10 倍）を上回った。1 葉ずつの値は 9/11 の 265 評価/秒より速い（ネットは同じ形。ドライバーや ORT のキャッシュの差とみるが未確認）。
- 2026-09-14 08:51 同じ持ち時間での新旧の対局（上と同じ条件、`go movetime 300`、20 局、ハーネス `libra match`、両玉の配置は交互）: 32 葉まとめて評価する新エンジン vs 1 葉ずつの旧エンジン（main の build）は **20 勝 0 敗**（先手 9-0、後手 11-0、全局 詰み、56〜146 手、8 分 51 秒）。仮の負けによる探索の質の低下より、読める量が約 17 倍になる効果がはるかに大きい。
- 2026-09-14 09:25 Windows 版 `libra.exe`（mingw クロスビルド、WSL の相互運用で起動）の思考速度を実行プロバイダごとに比べた（ls・lx を停止、RTX 5070 Ti、09:17 に写した ls の latest.onnx、fp32、Threads 4、Mate_Nodes 2000、3 回の中央値。exe は同じで隣の DLL だけ差し替え）。ノード/秒は 布石 2 手目 / 布石 30 手目 / 本将棋 41 手目の順。**CUDA**（ORT 1.30.0 gpu_cuda12 ＋ CUDA 12.9・cuDNN 9.26 の Windows 用 DLL、`go nodes 1600`）: 1 葉 484 / 486 / 433、16 葉 6,591 / 6,719 / 6,000、64 葉 10,013 / 9,138 / 9,525、128 葉 9,927 / 9,479 / 8,886（WSL の CUDA EP と同等かやや速い）。**DirectML**（ORT 1.24.4 DirectML ＋ DirectML 1.15.4）: 1 葉 474 / 471 / 428、16 葉 988 / 1,025 / 939、64 葉 2,244 / 2,063 / 2,524、128 葉 3,225 / 2,305 / 3,470（1 回の推論に約 16〜28 ms かかり、まとめても伸びにくい。入力を常に上限の行数に揃えても 64 葉 2,256 / 1,961 / 2,381、128 葉 2,152 / 1,360 / 2,892 で改善せず、揃える変更は入れない）。**CPU**（配布物の ORT 1.30.0、`go nodes 400`・2 回）: Threads 4 で 1 葉 371 / 373 / 335、16 葉 440 / 453 / 420、64 葉 369 / 379 / 357、Threads 16 で 16 葉 584 / 581 / 565、64 葉 515 / 512 / 497。まとめた評価は CPU では 16 葉で 1.2 倍程度しか効かない。対 CPU 配布物（Threads 4・64 葉）で CUDA は約 26 倍、DirectML は約 6〜8 倍。起動時の暖機を含む最初の `go nodes 64` は CUDA 0.016 秒、DirectML 0.27 秒。
- 2026-09-14 11:45 vast.ai の RTX 5070 Ti を Ryzen 9 7950X3D（16 コア 32 スレッド）のホストで、根の証明探索の先送り（`defer_root_proof`）の off → on を同じホストで 20 分ずつ比較（米国ミネソタ、オンデマンド $0.268/h（ストレージ込み）、信頼度 0.943 のため `--min-rel 0.94` で選択、ドライバー CUDA 13.2、電力上限 300 W、ワーカー 12 スレッド、ls の 11:00 の latest.pt（step 約 296,000）、本番と同じ探索設定、同時 512 局、compile(max-autotune)＋CUDA Graphs、各回の起動 300 秒後からの約 890 秒）: **先送りなし 581,609 局/日**（平均 86.4 手、100 万局あたり $11.1）→ **先送りあり 669,605 局/日**（平均 87.2 手、100 万局あたり **$9.6**）、**×1.15**（手数込みでは ×1.16）。段ごとの時間（定常）: なし 44.6 ラウンド/秒・22,800 評価/秒、collect 0.6 ms・eval 16.3 ms・apply 5.5 ms → あり 51.7 ラウンド/秒・26,500 評価/秒、collect 0.5 ms・eval 11.8 ms・proof 4.5 ms（評価の待ちと重なる）・apply 2.4 ms。GPU 使用率の平均 74% → 82%、電力 243 W → 270 W。手元の 6 コア模擬（+9.7%）より大きい。Ryzen 5 5600 のホスト（先送りなし 476,019 局/日・$9.6）と比べ、CPU を速くして局/日は 1.22 倍、時間単価が 1.4 倍で 100 万局あたりは同程度だったが、先送りを入れると同じ単価で 1.41 倍の局/日になる。起動（イメージの取得）122 秒、セットアップ 10 秒。支出 $0.18（vast.ai の合計 約 $0.44、残高 $9.55）。
- 2026-09-14 12:05 ワーカーの局の再生による検査（`verify_game`、CPU 1 スレッド・nice 10、ls・lx 稼働中、疑似ネット（乱数のロジット）の自己対局 200 局・平均 92.3 手・full の割合 0.27・方策 topk 32）: 合計 140 ms、**1 局 0.70 ms**（1 手 8 µs、ls の平均 87 手で約 0.66 ms）。vast.ai の 5070 Ti ＋ 7950X3D の 669,605 局/日（7.75 局/秒）ぶんで CPU 約 5 ms/秒。
- 2026-09-14 13:05 手元の GPU の状態（ls 512 局＋vast.ai ワーカーの局の取り込み、lx 64 局と共有、nvidia-smi 5 回）: 使用率 98〜99%、249〜250 W（電力上限 250 W、抑制の理由 SW power cap）、PCIe Gen5 x16。ls の学習は新規 256 局ごとに 88 steps・19.2〜21.9 秒、lx は 51 steps・3.6 秒。クラウドの 5070 Ti（7950X3D、先送りあり 669,605 局/日、GPU 82%・270 W・上限 300 W、学習なし・専有）が手元の ls（ワーカーなしで約 32.5 万局/日）の約 2.06 倍になる内訳の推定（未検証）: 学習の時分割（256 局に自己対局 約 47 秒＋学習 約 21 秒）×1.45、lx との GPU の共有（lx は 1 ラウンド 2.87 ms を毎秒約 100 回で、ls の自己対局の GPU 時間の約 3 割に当たる）×1.3、1 ラウンドの GPU 時間（手元の GPU 単独・compile で約 18.2 ms（01:28 の eager 20.8 ms ÷ 1.14）、クラウド 16.3 ms。電力上限か WSL2 の GPU 仮想化か、未切り分け）×1.1、平均手数（手元 87〜90、クラウド 86〜87）×1.02。ワーカーの局を取り込む間は学習の回数が増え、手元の自己対局の割合はさらに下がる。
- 2026-09-14 14:10 クラウド試運転（RTX 5080、インドのホスト）でブリッジの回収が 14:04:52 から止まった: 重み（fp16、20,501,387 バイト）を送る scp が 783,360 バイトで進まないまま残り（ServerAliveInterval=30 では切れない）、600 秒の打ち切りまで同じ周回の回収も止まった。ホストのワーカーは動き続け（47〜49 ラウンド/秒、GPU 57〜79%）、ホストの inbox に 24 ファイルが溜まった。同じ時刻に別の接続で同じ重みを scp すると 10.5 秒（約 2 MB/s）で送れたので、回線の遅さではなく 1 本の TCP 接続の詰まり。送信の打ち切りを 60 秒＋200 kB/s（20 MB で約 163 秒）にし、送信に失敗しても回収は続けるようにした（decisions.md 同日）。
- 2026-09-14 14:50 本番の ls に vast.ai のワーカーを足した最初の試運転（管理コンソールのクラウドタブから起動、2 時間）: RTX 5080 ＋ Threadripper PRO 5975WX（16 コア割り当て、インド、信頼度 0.994、オンデマンド $0.251/h（ストレージ込み）、ドライバー CUDA 13.2、ワーカー 12 スレッド、同時 512 局、先送りあり、重みは ls の step 302,312 から 324,121 まで 160 回受け取り）。**回収 48,842 局（489 ファイル）、弾いた局 0、検査 1.14 ms/局（計 55.8 秒）**、ls 側の取り込みで古すぎて捨てた局 0。**局/日（ブリッジ起動 15 分後〜終了 2 分前の回収から）592,681**（100 万局あたり $10.2）。ワーカーの段ごとの時間（定常）: 47〜49 ラウンド/秒・約 24,500 評価/秒、collect 1.2 ms・eval 3〜5 ms・proof 10〜12 ms・apply 4.4〜5.5 ms。GPU 使用率の平均 69.2%・232 W（2 分ごと 122 点）で、7950X3D のホスト（669,605 局/日、82%）より根の証明探索に時間がかかり CPU 律速。借りてから ssh まで 334 秒、セットアップ 21 秒、借りた時間 2.12 h、費用の見積もり $0.533（残高 $9.04、インスタンス 0 台を確認）。途中 14:04:52〜14:14:35 に重みの scp が詰まり回収が止まった（同日 14:10、修正済み。局はホストに溜まり後で回収したので局数への影響はない）。ls の games/day(1h) はワーカーの局を含めて 750,508（14:50）。
- 2026-09-14 13:15 ネットの出力のキャッシュの当たり率の見積もり（CPU のみ・nice 19・ls・lx とクラウドのブリッジ稼働中、ls の探索設定・512 局・8 スレッド、布石 lx の openings.json を 10%、評価は特徴量のハッシュから決まる疑似ネット、collect が出した行を特徴量のバイト列でハッシュ、8,000 ラウンド・410 万評価）: 重みの入れ替えで区切る窓 25 万・75 万・130 万評価のどれでも **当たり 15.5〜16.3%**、うち同じ局の中が 16.1%、別の局との重複は 0.1%。同じ局の当たりの距離（4,000 ラウンドのうち後の 2,500）: 1 ラウンド以内 0.02%、40 ラウンド以内（前の手、1 手 41.8 評価）11.39%、100 ラウンド以内（2 手前）5.54%、300 以内 0.14%、それ以上 0。疑似ネットは方策が平らで、実際のネットでは探索が偏るので当たりはこれより多い見込み（未確認）。
- 2026-09-14 14:20 ネットの出力のキャッシュ（`eval_cache`）の効果（上と同じ条件、4,500 ラウンドのうち後の 3,000、同じラウンド数＝同じ回数の評価で比べる）: 1 ラウンドに進む手数 なし 12.39 → あり 14.47（**×1.168**）、キャッシュから展開した葉 15.9%。CPU の時間（1 ラウンド）: collect 0.69 → 0.77 ms、proof（GPU の評価中）1.78 → 2.12 ms、apply 0.78 → 1.06 ms、1 手あたり 0.263 → 0.272 ms。GPU を待たせる CPU の時間（collect＋apply）の増加は 0.37 ms で、手元（1 ラウンド 約 27 ms）とクラウドの 5070 Ti（約 19 ms）ではどちらも局/日 約 1.15 倍の見込み（未計測）。途中の版: 特徴量のハッシュを鍵にし、当たった根の証明探索を collect の中で解いた版は 1.195 倍だが collect 2.62 ms（証明探索が GPU の評価中から外れた）。
- 2026-09-14 15:20 搾取者 lx の対本体成績の推移と CPU で動かす場合の見積もり。**成績**（state.json・metrics.jsonl）: 1 回目の区切り（ゼロから、相手 step 229,590）60,895 局で勝率 18.4%。06:54 に相手を step 280,014 で作り直した後、1 時間ごとの区間勝率は 67.6% → 75.3% → 80.1% → 84.8% → 86.8% → 86.9% → 87.9% → 88.2%（8.4 時間で 40,464 局、累計 82.5%。累計 50% は 0.1 時間・660 局、70% は 1.6 時間、80% は 5.8 時間）で、作り直し直後の 1 時間から 67.6% と互角から始まらず、5 時間目から約 87〜88% で頭打ち。15:16 の本体との step 差 45,353（作り直しは 50,000）、ls の step の速さはワーカーありの直近 3 時間 8,268 step/h（12 時間平均 4,874）。lx の布石 4,424 行を ls の新規対局の 10% に使用。**CPU**（torch 2.x の CPU・float32・64 局面の一括・nice 19・ls・lx 稼働中、CPU の使用率は 15:19 に 90.7% が空き）: lx のネット（1.9M）4 スレッド 793 評価/秒・16 スレッド 1,336 評価/秒、凍結した本体（10.2M）4 スレッド 183・16 スレッド 398 評価/秒。lx の必要量は 123,447 局/日 × 1 局 4,578 評価 ≈ 6,540 評価/秒で、手番で半分ずつとすると本体側 約 3,270 評価/秒に対し CPU 16 スレッドは 398（約 1/8）、lx 側 約 3,270 に対し 1,336（約 1/2.4）。CPU だけにすると局/日は本体側で律速されて約 1.5 万局/日（学習の CPU 化のコストは未測定）。
- 2026-09-13 19:50 自動外部計測（二飛香の系列の初回。ls step 236,203 の latest.onnx、C++ エンジン、movetime 1000、Declare_Win、相手 fuseki_usi_server.py Threads=2・Fuseki_Rules=2、10 局、GPU/CPU は ls・lx と共有）: 0 勝 10 敗（先手 0/5、後手 0/5）、全局 詰み負け、58〜89 手、533 秒。二飛香の手による非合法手の負けは無く、計測として有効。記録漏れを 9/14 に追記した。
- 2026-09-14 04:57 自動自己評価（二飛香の系列の初回。基準 step 230,210 vs step 274,739、100 局・96 sims・同時 64、GPU は ls・lx と共有）: 新しい側の勝率 0.69、基準との差 +139.0 Elo（95% 区間 +70〜+221）、累積 +139.0。新しい側は先手 37/48・後手 32/52。終局は詰み 70・41 手目裁定 30、平均 78.8 手、385 秒。新しい側の値の較正は予測 0.4〜0.6 で実勝率 0.67〜0.78（控えめ）、予測 0.9〜1.0 で 0.935（やや過信）。10.7 h・約 45,000 step の伸び。記録漏れを 9/14 に追記した。
- 2026-09-14 16:27 搾取者 lx を `refresh_steps` 25,000 で起動し直した直後の成績（15:46 に凍結相手を step 280,014 → 326,969 に作り直し、lx の step 約 71,000。`lx/games` の chunk 1018 以降）: **40 分で 4,400 局、lx の勝率 82.4%**（前回の作り直し直後の 1 時間は 67.6%）。lx 先手 2,104 局 81.8%、後手 2,296 局 83.0% で手番の偏りはない。終局はほぼ詰み（no_legal_move 4,393、max_ply 7）。手数 80 未満 463 局 81.6%、80〜139 は 2,923 局 85.2%、140 以上は 1,014 局 75.0%、平均 118.8 手。凍結相手は lx の探索設定（full_prob 0.25・full 96 回・fast 24 回・候補 16 / 8）で、自己対局と同じく根の最終選択に Gumbel ノイズが入る（`finish_move` の g + log π + σ(q̂)）ので、この勝率は手を抽選で選ぶ本体に対する値で、エンジンとしての本体（ノイズなし）に対する値ではない。lx の重みは作り直しをまたいで学習を続けるので、作り直し直後の勝率の前回比には lx の成長が混ざる。
- 2026-09-14 17:00 搾取者 lx（step 73,944）対 凍結した本体（lx/main.pt、ls の step 326,969）を、手の抽選（根の Gumbel ノイズ）の有無で 200 局ずつ（`libra eval`、lx の探索設定（後手玉四段目を除く玉配置）で全読み 96 回・候補 16、同時 64 局・8 スレッド、seed 11、fp16、ブランチ gumbel-noise-switch のワークツリーのビルド（`gumbel_noise` と `--no-noise`、main には未反映）、ls・lx 稼働中で GPU を共有、2 本を同時に約 21 分）: **ノイズあり lx 90.5%**（181 勝 19 敗、Elo +392、95% 区間 +322〜+496、先手 87-0-12・後手 94-0-7、平均 116.9 手）、**ノイズなし lx 86.3%**（172 勝 1 分 27 敗、Elo +319、区間 +257〜+402、先手 84-0-15・後手 88-1-12、平均 120.0 手、千日手 1）。ノイズなしは玉配置と先後が同じ対局が同じ手順になり、200 局に重複が 10 局前後ありうる。lx の中の成績（手の 75% が 24 回の軽い読み、82.4%）より全読みのほうが lx の勝率が高く、ノイズを外しても 4 ポイントしか下がらないので、lx の勝ちは本体の手の抽選の隙ではなく、1.9M の搾取者が 10.2M の本体を系統的に上回る穴。計測中の lx の games/day(1h) は 105,727（15:15 は 122,556）、ls は 360,884。
- 2026-09-14 17:46 搾取者 lx（step 73,944）対 凍結した本体（ls の step 326,969）を、両者とも読みを 96 回から **400 回** に増やしてノイズなしで 200 局（同日 17:00 と同じスナップショット・lx の探索設定・候補 16・同時 64 局・8 スレッド・seed 11・fp16、ブランチ gumbel-noise-switch のワークツリーのビルド、ls・lx 稼働中で GPU を共有、途中 17:27〜17:34 に ls・lx の停止と起動を挟む、2,787 秒）: **lx 87.5%**（175 勝 25 敗、Elo +338、95% 区間 +274〜+426、先手 80-0-14・後手 95-0-11、平均 116.6 手、全局詰み）。96 回（86.3%、Elo +319）から誤差の範囲で変わらず、読みを 4 倍にしても本体は穴を読みで補えない（lx も同じだけ深く読む条件）。[Wang23] の KataGo（10⁷ visits でも 72% 負け）と同じ傾向で、穴は評価（方策・価値）そのものにある。
- 2026-09-14 21:55 布石 20%（`openings_prob` 0.2、15:46 から）と作り直し 25,000 step の後の本体に対する、同じ搾取者のスナップショット（lx step 73,944、17:00 と同じ）の成績（凍結相手を 20:29 に作り直した ls の step 351,978、ノイズなし・読み 96 回・lx の探索設定・同時 64 局・8 スレッド・seed 11・fp16、main のビルド（opponent_prior は評価ハーネスでは使わない）、ls・lx 稼働中で GPU を共有、914 秒）: **lx 82.5%**（165 勝 35 敗、Elo +269、95% 区間 +212〜+342、先手 82-0-13・後手 83-0-22、平均 116.2 手、全局詰み）。step 326,969 の本体（17:00、86.3%・Elo +319・区間 +257〜+402）から 3.8 ポイント下がったが、差の標準誤差は約 3.6 ポイントで有意ではない（区間が大きく重なる）。本体は 25,009 step（約 5 時間、布石 20% のうち lx の布石は 16:45 ごろから）進んでおり、下がった分には通常の学習による強さの伸びも含まれる。lx の中の成績は、作り直し直後の 1 時間が 83.1%（15:36 の区切りは 82.5%）で、lx の成長と 17:34 の opponent_prior の反映が混ざる。
- 2026-09-15 00:11 リーグ（ls の [league]、64 局を過去の lx と、22:15 起動）の最初の約 2 時間と、同じ間のクラウド 2 回: **本体 対 lx step 99,178 は 733 局で本体 32.6%**（238 勝 1 分 494 敗、ls の自己対局の読み設定の本体 対 lx の探索設定の lx）。プールは 23:58 の lx の作り直しで step 105,311 が加わり 2 体（それまで 1 体なので相手は替わっていない）。lx の中の成績は、opponent_prior あり（17:34 から）の区間の相手 step 351,978 が 15,142 局で 82.7%、23:58 に作り直した相手 step 377,768 の最初の 486 局が 81.0%。**クラウド 1 回目**（22:17、RTX 5080 ＋ Ryzen 9 7900X、オーストラリア、$0.262/h、1 時間、費用の見積もり $0.276）: ブリッジの回収 36,293 局・弾いた局 0・誤り 0 だが、**ls が古すぎて捨てた局 8,900（24.5%）**（重みの step 365,387 の 7,300 局が 2,059〜2,150 step 遅れ、364,077 の 1,000 局が 3,369、362,946 の 600 局が 2,441。`max_lag_steps` 2,000）。重みの送信は 1 時間 28 回（14:50 のインドのホストは 2 時間 160 回）、ls のチェックポイントの間隔 10 分の step の進みは 1,310〜2,059。原因（オーストラリアへの送信の遅さか）は未検証。**2 回目**（23:41、Ryzen 9 7900、オーストラリア、2 時間、実行中）: 00:11 までに 16,500 局を回収し捨てた局 0。ls の games/day(1h) はワーカーの局を含むため、リーグの局/日への影響はワーカーなしの時間で別に測る。
- 2026-09-15 08:30 クラウド 2・3 回目の結果、リーグを入れた ls のワーカーなしの局/日、lx の対本体勝率の推移。**クラウド 2 回目**（23:41〜00:20、ユーザーが途中で停止）: 回収 21,406 局・弾いた局 0・誤り 0・捨てた局 0、重みの送信 58 回、費用の見積もり $0.17。**3 回目**（00:27〜03:31、RTX 5070 Ti ＋ Threadripper 3970X、アイオワ、$0.184/h、3 時間、ブリッジは送信時間を記録する版 9d01ba8）: 回収 70,468 局・弾いた局 0・**捨てた局 0**、誤り 3（00:37〜00:38 に ssh の接続が約 40 秒切れ、次の周回で回復）、費用の見積もり $0.564。**重みの送信 325 回、1 回平均 13.2 秒・最大 34.9 秒（20 秒超 2 回）、ls が書いてからホストに届くまで最大 46.6 秒（30 秒超 7 回）**。1 回目（オーストラリア、捨てた局 8,900）の原因は記録が無く未確定のまま。**ls のワーカーなしの局/日**（03:45〜08:30、4.61 時間、metrics.jsonl の games_total の差、リーグ 64 局を含む、lx と GPU を共有）: **325,522 局/日・4,915 step/h**（05:30〜08:30 は 324,890）。リーグ前のワーカーなしの約 32.5 万局/日（2026-09-14 13:05）と同じで、局/日は下がっていない（22:15〜08:30 の ls の局 90,783 のうちリーグ 8,441 局（9.3%）なので、自己対局だけでは約 29.5 万局/日）。lx は 132,113 局/日。**リーグの本体の成績**（22:15〜08:30、相手ごと、本体の勝率）: lx step 99,178 が 3,833 局で 38.2%、105,311 が 1,792 局で 36.9%、116,230 が 2,048 局で 40.7%、135,997 が 768 局で 46.8%（相手ごとに打った時期の本体が違うので、相手どうしの比較はできない）。**lx の中の対本体勝率**（凍結相手ごとの区間の累計）: ls step 351,978 が 82.7%（15,142 局、00:04 まで）→ 377,768 が 79.6%（11,431 局、02:24 まで）→ 402,854 が 73.1%（20,760 局、06:20 まで）→ 428,113 が 63.6%（12,266 局、08:30 時点で途中）。作り直しごとに下がっており、リーグ（22:15 から）と opponent_prior（17:34 から）の後で本体の穴が塞がりつつあるか、lx の成長が追いついていないかは、この数字だけでは分けられない（固定した lx のスナップショットとのノイズなし 200 局で見る）。
- 2026-09-15 09:00 本体 ls の step 441,102 に対する搾取者のノイズなし 200 局を 2 本（2026-09-14 17:00 と同じ条件: 読み 96 回・lx の探索設定・同時 64 局・8 スレッド・seed 11・fp16、評価ハーネスは opponent_prior を使わない。ls・lx をユーザーが停止して GPU を専有、2 本を同時に各 233 秒）: **固定した lx（step 73,944）79.0%**（158 勝 42 敗、Elo +230、95% 区間 +176〜+297、先手 74-0-26・後手 84-0-16、平均 118.0 手、全局詰み）。同じ lx の成績は本体 step 326,969 で 86.3%、351,978 で 82.5% だったので 7.3 ポイント下がった（差の標準誤差 約 3.8 ポイント、約 1.9σ で 95% では有意でない）。本体は 114,133 step 進んでおり、通常の学習の伸びも含む。**今の lx（step 149,576）66.0%**（132 勝 68 敗、Elo +115、区間 +66〜+169、先手 71-0-30・後手 61-0-38、平均 115.9 手、全局詰み）。lx の中の成績（凍結相手 step 428,113、63.6%）と近い。今の lx は固定した古い lx より本体に 13 ポイント勝てていない（差の標準誤差 約 4.4 ポイント、約 2.9σ）。古い lx が突いた穴の一部は本体に残ったまま、今の lx はそれを使わなくなっている（今の lx は凍結相手の作り直しのたびに新しい本体に合わせて学習し、古い穴を忘れた可能性がある）。リーグのプールは 2026-09-14 22:14 の step 99,178 からで step 73,944 は入っておらず、新しい 5 体から選ぶので、古い lx の穴がリーグで学習の対象になるかは今のプールの lx がその穴を使うかによる。
- 2026-09-15 11:10 クラウドのワーカー 1 台が手元の GPU に与える影響（複数台の検討のため。ls の metrics.jsonl の 2026-09-14 22:15〜09-15 11:10、隣り合う行の差をクラウド稼働中（ブリッジ起動の 1 分後から bridge_h の間、5 セッション）とそれ以外に分けて集計、区間をまたぐ行と再起動の行は捨てる。ls の手元の自己対局は `engine.games`（リーグ 64 局を含まない）、全体は `games_total`（リーグ・ワーカーの局を含む）。lx と GPU を共有）: **稼働中 5.28 h は手元の自己対局 98,511 局/日・全体 766,908 局/日・11,434 step/h、それ以外 6.82 h は手元 298,377・全体 326,266・4,919 step/h**。1 時間ごとでは、手元の自己対局は RTX 5070 Ti のホスト（00〜02 時）で 14.2 万〜16.1 万、RTX 5080（10 時台）で 9.5 万、クラウドなし（04〜09 時）で 29.4 万〜30.0 万。学習量は取り込んだ局数に比例する規則（replay_ratio）なので、ワーカーの局の分だけ手元の GPU が学習に回り、手元の自己対局が 2/3 減った。lx も稼働中 105,990 局/日・4,180 step/h、それ以外 127,946・5,021 で 17% 減った。**クラウド 1 台で全体に足された局はホストの回収（56 万〜86 万局/日）より少ない約 44 万局/日で、手元で失った約 20 万局/日を差し引いた実質の費用は $0.26/h なら 100 万局あたり約 $14**（履歴の $8〜11 は手元の減りを数えていない）。見積もり（未検証）: 手元の GPU が学習だけをすると 135 steps・30.7 秒から約 15,800 step/h、1 局あたり 0.362 step（クラウドなしの 4,919 step/h ÷ 13,594 局/h）なので、取り込める局の上限は全体で約 105 万局/日。1 台で約 77 万〜80 万に来ており、**2 台目で増やせるのは多くて約 25 万〜30 万局/日**（2 台目の費用なら 100 万局あたり約 $20〜25）。上限を超えると取り込みが学習に追いつかず、`max_lag_steps` で捨てる局が出る見込み。重みの送信は 3 回目のセッションで 325 回 × 20.5 MB ≈ 6.7 GB（3 時間、転送料の上限 $0.020/GB なら最大 $0.13、履歴の費用に含まない）で、`max_lag_steps` 2000 は約 24 分の余裕に当たる。
- 2026-09-15 12:10 同時局数（n_games）を増やすとクラウドのホストの 1 秒あたりの手数が増えるかの模擬（CPU が律速のホストで GPU が遊んでいたため。RTX 5070 Ti ＋ Threadripper 3970X の 3 回目は 512 局で eval 3.4 ms・proof 14.0 ms・apply 5.6 ms・GPU 63%）。手元の CPU のみ（ls・lx 稼働中、nice 10、taskset）、ls の探索設定、乱数のロジット（前もって作った 8 組を回す）、GPU の評価は 512 局あたり 3.4 ms に比例する待ちで模して根の証明探索と重ね、90 秒の定常化の後 60 秒、`stats()` の moves の差: **16 論理 CPU・12 スレッドで 512 局 2,684 手/秒 → 1,024 局 2,901（+8%）→ 1,536 局 2,991（+11%）**、ラウンドの内訳は collect 0.66 → 1.09 → 1.53 ms・proof 2.20 → 2.78 → 2.98 ms・apply 0.81 → 1.49 → 2.22 ms。**4 論理 CPU・4 スレッドで 512 局 2,258 → 1,024 局 2,301（+2%）**（collect 1.19 → 2.37、proof 2.14 → 3.30、apply 1.40 → 2.94 ms）。証明探索は局数を増やしてもあまり伸びないが、collect と apply が局数にほぼ比例して伸びるので、遅い 1 局を待つ時間を多くの局で割っても手数はほとんど増えず、CPU が弱いほど増えない。ホストごとに同時局数を自動で調整する案は見送る（decisions.md 同日）。最初の試行では乱数のロジットの生成（n × 方策の大きさ）を毎ラウンド評価の待ちに含めてしまい、評価が 10.5 / 22.2 ms になって手数が同じに見えたので捨てた。乱数のロジットなので本番の探索木（証明探索の重さ）とは形が違う。
- 2026-09-15 12:20 ブラウザで Libra のネットを通す速さ（tenbin-shogi-web に載せる検討。ls の latest.onnx 12:05 の写し＝step 約 473,000、onnxruntime-web 1.29.0、4 スレッド、1 条件 5 回の空回しの後 30 回の中央値、入力は 0/1 の乱数（sq 10%・glob 30%）、ls・lx 稼働中で CPU（負荷平均 2.5〜3.5）と GPU を共有。**GPU の計測は ls を止めて測る決まりだが共有のまま測った**ので WebGPU の値は下限）。重みは fp32 41.1 MB・fp16 20.6 MB（`convert_float_to_float16`、入出力は fp32）・int8 10.6 MB（`quantize_dynamic`、QInt8）。fp32 との差（CPU、ls の直近の自己対局 20 ファイルから 1 局 1 局面を無作為に 600 局面＝布石 312・本将棋 288、方策は合法手に絞った softmax、勝率は W＋D/2）: **fp16 は合法手中の最善手 99.8% 一致・方策の全変動距離 平均 0.0005・勝率の差 最大 0.004**、**int8 は最善手 76.8% 一致・全変動距離 平均 0.069・勝率の差 平均 0.026／99% 点 0.24／最大 0.30 でこのままでは使えない**（per_channel にしても 81.3%・0.053・0.021／0.21／0.26）。最初に乱数入力 64 局面で比べた値（int8 の最善手 88%・勝敗の確率の差 最大 0.38）は分布の外の入力なので捨てた。ms/局面（バッチ 1 → バッチ 8）: **WSL の headless Chromium 153 の wasm** fp32 14.4 → 11.3（形の固定 `freeDimensionOverrides` で 12.4 → 11.7）、int8 9.6 → 5.6（固定 7.0 → 5.8）、fp16 53.8 → 39.7（固定のバッチ 1 は 3,022 で中断）。**Windows の Edge 153 headless の wasm** fp32 13.3 → 15.6、int8 11.4 → 9.7。**Edge の WebGPU（RTX 5070 Ti、D3D12）** fp32 20.3 → **2.6（388 局面/秒）**（固定 18.7 → 3.0）、fp16 57.4 → 7.9、int8 194 → 31.7（量子化の演算が GPU に無く遅い）。WSL の Chromium は WebGPU のアダプタが出ない。まとめ: wasm の fp32 は約 80 局面/秒でネイティブの CPU EP（6 ms）の約 2 倍遅いだけ、WebGPU はバッチ 1 では wasm より遅く、まとめ評価で 5 倍になる。WebGPU の fp16 は速くならない（変換で入る Cast が多いためと見るが未確認）。形の固定は wasm のバッチ 1 で 1〜3 割速い程度。セッション作成は 70〜760 ms。
- 2026-09-15 12:38〜14:25 Libra v0.1（step 477,636、`releases/v0.1/libra-v0.1.onnx`、C++ エンジン、`go nodes 96` で読み 96 回固定、CPU EP・Threads 2、Mate_Nodes 2000、Declare_Win）対 tenbin-shogi-web の今の AI の強さ 5（両玉の価値表＋布石の方策ネット温度 0.4＋Sunfish4 の WASM を Node で movetime 10,000 ms・1 スレッド。サイトのコードのまま USI で包んだもの）を `bin/libra match` で 40 局（4 本を並べて 10 局ずつ、置く側は交互、裁定はハーネス、ls・lx 稼働中で CPU を共有、GPU は使わない）: **Libra 0 勝 40 敗**（Libra 先手 0/12・後手 0/28）、全局 詰み（no_legal_move）、53〜111 手（平均 75.3）、1 局平均 157 秒。選択は 置く側 Libra で先手 17・後手 3、置く側 サイトで先手 9・後手 11。直前の配線の確認（強さ 1、1 局）も Libra の 51 手負けで、Libra は勝率 0.6〜0.8 と見ていた局面から本将棋の読み抜けで詰まされた。96 回はブラウザの wasm で約 1.3 秒の見積もりで、相手の Sunfish4 は 1 手 10 秒（約 600 万節点）。読みの回数を増やした場合は測っていない。**同日の追記**: この 40 局はサイトの AI を Libra に置き換えられるかを見る計測で、Libra の強さの指標ではない（decisions.md 同日）。Libra の 1 手はログの 384 手で中央値 477 ms（90% 点 646 ms）で、相手の 10 秒と時間で約 21 倍の差があった。「約 600 万節点」は出所の記録が無く未確認（USI のラッパーが Sunfish4 の info を捨てるのでログにも無い）。
- 2026-09-15 14:40 本体 ls の自己対局の両玉の置き方（`ls/games` の 77 万局、9/13 18:05〜9/15 14:40、500 ファイルごと）: 線対称（同じ筋で段が逆）2.6〜3.1%・点対称 2.6〜2.9%（一様なら各 36/1296＝2.78%）で、どの区間も一様と同じ。自己対局は玉 2 手を一様に置いてから記録を始める（selfplay.cpp、玉の手に方策ターゲットは無い）ので、玉を置く手はネットが学習しない。
- 2026-09-15 14:50 自己対局の実際の先手の得点（引き分け 0.5、2000 ファイルごと）: 後手玉四段目を除く対称な組 0.540 → 0.530 → 0.523 → 0.518（各約 8,000 局）、四段目を除く全組 0.487 → 0.482、後手玉四段目の対称な組は 0.999〜1.000（3 手目の桂打ちの裁定）。`K*1i K*9a` は全期間 675 局で約 0.50。1 組あたりの局数は、直近 2000 ファイル（06:50〜、約 9 時間）で中央値 157、全期間で 618。直近 3 万局のうち少なくとも 3.2% は搾取者の布石（`lx/openings.json`、棋譜に印なし）から始まっていた。
- 2026-09-15 15:10 学習の時点ごとのネットの両玉の評価（CPU・fp32、softmax した勝率、後手玉四段目を除く）: 全組の平均 V は step 98,368 で 0.492、229,590 で 0.474、274,739 で 0.487、422,358 で 0.462、v0.1（477,636）で 0.459、498,735 で 0.465（実際の得点は 0.48 台）。対称な 51 組のうち V < 0.5（後手を選ぶ）は 8・10・4・19・27・22 組。step 0〜229,590 は ls-v0（二飛香の前）。読み 1 回・ノイズありで玉を 100 回置かせると、対称な置き方（線／点）は v0.1 で 11／10 回、498,735 で 46／42 回（学習しない出力が世代で変わる）。ls-v0 の scale.json（読み 1,600 回）では対称な 27 組の V̂ が 0.466〜0.559（平均 0.506）で、10 組が 0.5 未満だった。
- 2026-09-15 15:20 サイト（tenbin-shogi-web）と同じ探索で v0.1 に両玉を置かせたときの偏り（ONNX CPU、wdl を softmax、置く手はノイズあり、V は v0.1 の生の勝率）: 置いた組の |V − 0.5| の平均は読み 1 回 0.023・8 回 0.037・32 回 0.050（選ぶ側の見込み 0.523・0.537・0.550）。|V − 0.5| ≤ 0.02 の組は 972 組中 224。1 手目は先手、2 手目は後手の得を最大にする探索なので、読むほど偏る。サイトが wdl の logit をそのまま渡していた版では、読み 1 回の 100 回中 1 回、後手玉を四段目に置いた。対外対局（ハーネス、相手は常に `K*1i K*9a`）で Libra が選ぶ側のとき、先手／後手は ls-v0（9/11〜12）18／8、ls 9/13 1／4、9/14 0／5（勝率 0.482〜0.519）。
- 2026-09-15 同じ秒数で読める回数の差（既存のログから集計。新たな対局はしていない）。対象は `ls/matches/auto-20260914-195659.log`、自動の外部計測 10 局で、両者に `go movetime 1000`。Libra は C++ エンジン、latest.onnx、DNN_Provider は既定の auto、GPU は ls・lx と共有。相手は fuseki_usi_server.py の Threads=2 で、41 手目以降はやねうら王＋水匠5 の評価関数。**Libra は 1 手（453 手）で中央値 2,988 ノード**（10% 点 2,320・90% 点 4,774、time 約 1,010 ms）。**相手の info は中央値の行で 238 万ノード・292 万 nps、90% 点の行で 327 万 nps**（布石の方策ネットの行を含めて並べた値）。1 秒あたりのノード数の差は約 1,000 倍。1 ノードの重さが違う: Libra はノードごとにネットを通し、相手は軽い評価の αβ 探索。前日の 20260913 のログは Libra が中央値 66 ノードで比べていない。複数葉の同時評価（9/14 08:50）を入れる前だからと見られるが、未確認。GPU を専有したときの Libra は 64 葉で 8,500〜10,000 ノード/秒（9/14 08:50）。
- 2026-09-15 15:15〜17:36 サイト（tenbin-shogi-web）用の玉配置表 `ls/scale/scale-v0.1.json` を libra-scale で作った（v0.1＝step 477,636 の .pt、fp16、runbook §玉配置表の手順どおり）。**build**（読み 1,600 回、492 組）は 39 秒で、暫定の釣り合う組は 242 組（鏡映込み、margin 0.02）。**verify**（|V̂ − 0.5| の小さい 48 組、1 組 100 局以上、読み 96 回、full_prob 1.0、policy_topk 8、同時 256、8 スレッド）は **9,653 局 / 138 分**。GPU は 15:18〜15:57 が専有（ls・lx とクラウドを停止）、15:57 からは ls・lx と共有した。速さは専有で 4,000 局 / 2,073 秒（116 局/分）、共有で 4,600→9,600 局が 5,778 秒（52 局/分、専有の約 0.45 倍）。先手の得点（引き分け 0.5）は最小 0.399・中央値 0.504・最大 0.608、全体 0.509。95% 区間の半幅は ±0.062〜0.098。**釣り合う組は 84 組**（鏡映込み。検証した 48 組のうち規則「|w − 0.5| − 半幅 ≤ min |w − 0.5|」を満たす組を鏡映で広げた数）。**予定と実測の差**: ログの「target 4800」の 2 倍の局数になった。48 組のうち 4 組（`5h 5b`・`5i 5b`・`5g 5a`・`5g 5c`）は両玉が 5 筋で、鏡映が自分と同じなので、鏡映込みの一覧からの一様な抽選で他の組の半分しか当たらない。verify は全組が 100 局に届くまで回すので、全体が約 1 万局に延びた（模擬の見込みは 9,300〜11,100 局）。2026-09-11 の v0 の 6,247 局より多いのもこのためと見られる（未確認）。区間の広さは組ごとの局数で決まるので、表の中身には影響しない。ls・lx の自己対局と学習は 15:14〜15:57 の 43 分止まり、クラウドも 15:12 に止めた。
- 2026-09-15 18:40 v0.1 のネットが両玉を置いた局面を後手寄りに見る原因（学習データの集計。すべて先手から見た得点、引き分け 0.5、後手玉四段目の組を除く）。**布石の価値の学習目標は 0.5×実際の結果 z ＋ 0.5×v41**（replay.py の `sample`、`lambda_z` 0.5）で、**v41（41 手目の局面＝40 手完了・先手の番の探索値 `root_q`）が実際の結果より後手寄り**。ls の棋譜の直近 2 万局（リーグの局を除く自己対局 14,043 局）で z 0.475・v41 0.439・目標 0.457。ネットの両玉の局面の V（v0.1 0.459、step 498,735 0.465。同日 15:10）はこの目標とほぼ同じ。v41 と z の差は ls の全期間（`games_000000`〜`008000`、500 ファイルごとに 10 分の 1 を集計）で −0.017〜−0.063、平均約 −0.04 で、学習が進んでも縮んでいない。41 手目の局面 2,779 局（直近 40 ファイル）で比べると、実際 0.476、記録された v41 0.432、v0.1 のネットの wdl 0.459、ネットの v41 の出力 0.507。**探索値は手番側を低く見る**: リプレイの直近 60 チャンク（リーグの局を除く）で、本将棋（42 手目以降）の各手の `root_q` は手番側から見て実際の結果より、先手の番で −0.017〜−0.036、後手の番で −0.025〜−0.034（読みの多い手・少ない手とも）で、先後によらない。`root_q` は根の訪問の平均（wsum / visits、selfplay.cpp）なので、Gumbel の逐次半減で読んだ悪い手の値も混ざると見られる（未確認: 最も訪問した手の値との比較はしていない）。41 手目の局面の手番はいつも先手なので、この手番側の低さがそのまま先手に不利な v41 になり（−0.042〜−0.044）、布石の目標の半分に入る。布石の各手の `root_q` も先手の番 −0.011〜−0.023、後手の番 +0.021〜+0.030 で、どちらも先手に不利（布石の価値は v41 を学ぶので、ずれが布石全体に広がっている）。
- 2026-09-15 19:10 前行の「`root_q` が根の訪問の平均なので低くなる」の確認（CPU、v0.1 の ONNX、wdl を softmax）。**この説明だけでは足りない**。v0.1 のころの棋譜（`games_007100`〜`007159`、12:20〜12:34）から 41 手目の局面（40 手完了・先手の番）800 局を、ls の [search] と DEFAULTS の設定（Gumbel ノイズあり）で読み直した。先手の得点で、実際 0.493、記録された v41 0.446、**ネット単体 0.398**、読み 24 回の `root_q` 0.415・最も訪問した手の値 0.460、読み 96 回の `root_q` 0.468・最も訪問した手の値 0.529。この局面ではネット単体がすでに実際より大きく低く、探索は読むほど実際に近づける（24 回で +0.017、96 回で +0.070）。最も訪問した手の値は 24 回で実際より低く、96 回で高く（選んだ手の値は高めに出る）、v41 をこれに替えてもずれが消えるとは言えない。直近の棋譜の 41 手目の局面 2,779 局ではネット単体 0.459・実際 0.476 で、ネットの低さの大きさは時期で違う（理由は未確認）。一方、直近 40 チャンクから取った 24,000 局面では、ネット単体は布石で +0.002（先手の番）・+0.015（後手の番）、本将棋で −0.003 と実際に近く、記録された `root_q` は本将棋で手番側 −0.028（先後とも）。41 手目の局面は本将棋の扱い（`replay_features` の fuseki 印が 0、目標は z だけ）。`Position.set_position` ＋ `features(False)` と学習の `replay_features` の特徴量は 2・20・40・41 手目で 200 局とも一致した（集計の経路の違いではない）。**確かなのは**、v41 が実際より約 0.04 低いことと、布石の目標にそれを半分混ぜていること（両玉の局面で実際 0.48・v41 0.44 前後・ネット 0.46）。v41 が低くなる理由（41 手目の局面のネットの低さと、自己対局の探索値の手番側の低さ）は突き止めていない。
- 2026-09-15 20:00 v0.1 のネットの引き分けの確率（CPU、`releases/v0.1/libra-v0.1.onnx`、wdl を softmax）。ls の直近 40 ファイル（`games_008282`〜`008321`、リーグの局を含む）から 1,500 局を抜き、手順の途中の局面を `Position.set_position` ＋ `features(False)` で作った。実際の引き分けは 0.7%（11 局）。ネットの D の平均は 2・10・20・30・39 手目で 0.338・0.343・0.361・0.366・0.352、45・80 手目（本将棋、1,145・981 局面）で 0.007。布石の学習目標を `soft_wdl`（引き分け = 1−|t|、t = 0.5·z ＋ 0.5·v41）で作ったときの D の平均は 0.324（本将棋は目標 z だけで 0.010）。布石のネットの D は実際の引き分けでも探索の値でもなく、目標の作り方をそのまま学んでいる。探索（`wdl_value` = W−L）と表示（(q＋1)/2）は期待値だけを使うので、推論ではこの D は勝率の見立てを変えない（学習への影響は未確認）。docs/method-evidence.md §1.3 (b)。
- 2026-09-15 20:05 釣り合う組での v0.1 の先後の判断のずれと、選ぶ AI の得点（CPU、`releases/v0.1/libra-v0.1.onnx`、wdl を softmax、Gumbel ノイズなし、`mate_nodes_root` 2000。`scale-v0.1.json` の検証済み 48 組の、検証対局（読み 96 回、1 組 100〜244 局）の先手の得点 w（平均 0.508）と比べた。選ぶ AI の得点は、V ≥ 0.5 なら w、そうでなければ 1 − w）。**ネット単体（読み 1 回）**: 平均 V 0.502、V − w −0.006（標準偏差 0.043・標準誤差 0.006）、先手を選ぶ組 29、得点 0.502。**読み 128 回**: V 0.4995、V − w −0.009（0.039・0.006）、先手 24 組、得点 0.509。build の V̂（読み 1,600 回）は V̂ − w −0.008（標準偏差 0.039）。いつも先手を選ぶと 0.508、組ごとに w の大きい側を選ぶと 0.532（同じ対局で選んで測った値なので上に偏る）。区間が 0.5 を含まない 5 組で逆を選んだのは、読み 1 回で 4 組、128 回で 1 組。**釣り合う組の近くでは、95% で有意なずれは見えない**。15:10 の「全組の平均 V 0.459、実際の得点 0.48 台」は世代の混ざった自己対局の結果との比較で、18:40 の目標のずれ（v41 − z 約 −0.04）は同じ局の中の比較。選ぶ AI の得点は 18:22 に同じ条件で測り、サイトの `KINGS_SIMS` 128（tenbin-shogi-web の levels.ts）の根拠に引いていたが、ここに記録していなかった。docs/method-evidence.md §4。
- 2026-09-15 20:30 居玉同士（▲5九玉 △5一玉）で v0.1 が先後をどう選ぶかと、自己対局の実績（ユーザーの問い「サイトの強さ 5 で後手を選んだが正しいか」。サイトの先後の選択は強さに関係なく読み 128 回）。**v0.1 の先手の勝率**（CPU、wdl を softmax、`mate_nodes_root` 2000）: 読み 1 回 0.454、128 回 0.481（Gumbel ノイズありの 5 通りで 0.479〜0.484）、400 回 0.482、1,600 回 0.480（`scale-v0.1.json` の V̂ 0.4796）。どの回数でも後手を選ぶ。この組は検証対局をしていない（|V̂ − 0.5| 0.020 は上位 48 組の外）。**自己対局の実績**（ls の棋譜、リーグの局を除く。世代が混ざり、搾取者の布石から始まる局も含む）: `scale-v0.1.json` の集計で 640 局、先手の得点 0.520（95% 区間 0.481〜0.558）。1,000 ファイルごと（各 70〜93 局）では 0.474〜0.577 と揺れ、v0.1 のころの `games_007000`〜`007999` は 70 局 0.543。実績の点推定は先手寄りで、v0.1 の 0.481 と 約 0.04 違う（釣り合う 48 組の平均のずれ −0.008 より大きい）が、区間が 0.5 を含むので、どちらを選ぶのが正しいかは決まらない。区間の半幅を ±0.03 まで縮めるには、同じ組で約 1,000 局の検証対局が要る。
- 2026-09-15 21:10 全組の検証対局の速さ（玉配置表の作り直しの見積もり。v0.1 の .pt、読み 96 回・full_prob 1.0・policy_topk 8、剪定後の 492 組から一様、ls・lx は停止して GPU を専有、RTX 5070 Ti、定常の 90〜120 秒の `stats()` の差）: **今の verify の経路**（eager fp16、同時 256、8 スレッド）226.7 手/秒・20,399 評価/秒。**自己対局と同じ `SelfPlayLoop`**（compile(max-autotune)＋CUDA Graphs、評価のキャッシュと根の証明探索の先送りあり。棋譜は変わらない。同時 512、12 スレッド）**386.5 手/秒（×1.70）**・26,418 評価/秒（キャッシュの当たり 8,353/秒）。同時 1,024・16 スレッドは 365.2 手/秒で、評価が 26,454/秒で頭打ちになり、512 より遅い。v0.1 の verify の専有時の 116 局/分と eager の手数から 1 局平均 約 117 手とすると、512 は約 198 局/分・約 28.5 万局/日（平均手数は組の構成で変わるので未確認）。
- 2026-09-15 21:55 上と同じ `SelfPlayLoop`・同時 512 の計測を、Windows 側のアプリと GPU を共有した状態でやり直した（計測前の GPU 使用率 91%・5.0 GB。ls・lx と WSL の他の GPU の処理は無し。Windows の nvidia-smi に Chrome・Edge・ChatGPT・Claude などが並ぶが、どれが使っているかは見えない）: **116.6 手/秒・8,422 評価/秒（21:10 の専有の 0.30 倍）**。同じ時間帯の `libra-scale seq run` の試運転は、居玉 1 組で 280 秒に 1 局、1f 1a で 100 秒（compile を含む）に 0 局だった。検証対局を回す間は GPU を使う Windows のアプリを閉じる必要がある。
- 2026-09-15 23:22 全組の検証対局（`libra-scale seq`、v0.1 の .pt、読み 96 回・同時 512・12 スレッド、対称な組 27 組の段）の局の速さ。**vast.ai の RTX 5080**（Ryzen 9 9900X 実効 12 コア、実効 $0.226/h）: ブリッジの回収で 23:12:23〜23:22:28 に 2,601 局、**264 局/分**（ワーカーの起動 23:08:33 から約 4 分後から。回収は 100 局ずつのファイルなので ±10 局/分ほどの粗さ）。100 万局あたり約 $14.3。**手元の RTX 5070 Ti**（ls・lx は停止、GPU を使う Windows のアプリは閉じた状態）: 同じ時間帯（23:11:57〜23:22:28）の status の手元の局数 11,419→13,519、**約 200 局/分**。合計 約 467 局/分で、5080 は手元の 1.3 倍。1 段目の約 33 万局（模擬の見積もり。実際の局数は未確認）は、この速さなら残り約 11 時間（9/16 の 10 時ごろ）。平均手数は組で違うので、対称な組の段の後で速さは変わりうる。
- 2026-09-16 01:15 全組の検証対局の、対称な置き方の段の結果（v0.1、読み 96 回、組ごとに半幅 0.02 まで打つ。手元の 5070 Ti ＋ vast.ai の 5080。9/15 21:59〜9/16 01:15。PC の再起動で 00:37〜00:45 は手元が止まった）。**群の合算**（先手の得点、95% 区間）: 5 筋の同じ高さ 3 組 7,419 局 0.502（0.491〜0.514）、線対称 15 組 36,717 局 0.529（0.524〜0.534）、点対称 15 組 36,540 局 0.519（0.514〜0.524）。どの群も後手に傾いていない。**組で後手寄りに確定したのは ▲3g △3c（線対称）だけ**: 2,458 局 0.463（0.443〜0.482、z 約 3.7、27 組ぶんの Bonferroni をかけても片側 p 約 0.003）。00:48 に早期、01:10 に確定のアラートが出た。左右の鏡映で分けると ▲3g △3c 1,286 局 0.477、▲7g △7c 1,172 局 0.447 で、どちらも 0.5 未満。同じ 3 筋の ▲3h △3b は 0.520、▲3i △3a は 0.539、点対称の ▲3g △7c は 0.520。ほかに点推定が 0.5 未満の組は ▲4g △4c 0.495 と ▲2i △8a 0.493 で、どちらも区間が 0.5 を含む。最も先手寄りは ▲2g △8c 0.583 と ▲1i △1a 0.578。左右の鏡映の置き方同士の差（5 筋を除く 24 組）は z の二乗和 22.1（自由度 24）で、ネットの左右の非対称による差は見えない。手元と vast.ai の局の違いは ▲3g △3c の周りの 4 通りだけ見て、系統的な差は無かった。いずれも v0.1 が読み 96 回で自分と打った値で、ゲームそのものの値ではない。
- 2026-09-16 09:54 v0.1 の全組の検証対局が終わった（`libra-scale seq`、492 組、読み 96 回、9/15 21:59〜9/16 09:54 の 11 時間 55 分、**303,677 局**。途中 00:37〜00:45 は PC の再起動で手元が止まった）。打ち切りは**有意 419 組・半幅 0.02 が 73 組**で、上限 3,000 局に達した組は無い。先手の得点は最小 0.155、25% 点 0.393、中央 0.496、75% 点 0.571、最大 0.814。**95% 区間が 0.5 を含む組 87、後手が有利で有意 217 組、先手が有利で有意 188 組**。選ぶ側は先手 238 組・後手 254 組（ほかに後手玉が四段目の 164 組は規定で先手）。置く側の釣り合う組は代表 56 組（鏡映込み 109 通り）。探索の見立てとの差 V̂ − 得点は平均 **−0.0136**（標準誤差 0.0022）で、9/15 に 48 組で測った −0.008 と同じ向き。9/11 の v0 の表（同じ 48 組、1 組 100〜244 局）と比べると、同じ組の得点は平均 +0.009 で、最大の差は ▲3g △4c の +0.097。打った内訳は手元 146,269 局、vast.ai の 4 台 157,408 局。速さは手元（5070 Ti）約 200 局/分、クラウド（5080）224〜264 局/分。手元の run が終わった後にクラウドから届いた 177 局は取り込んでいない（表には入っていない）。
- 2026-09-16 10:25 全 492 組の検証対局から出した、**表を使うことの価値**（同日の表 `scale-v0.1-seq.json` の得点を真の値として計算。対局はしていない）。**選ぶ側**（492 組から一様に置かれたとき、選んだ側から見た得点）: いつも先手 0.477、**表で得点の高い側を選ぶ 0.604**、探索値 V̂（読み 1,600 回）の符号で選ぶ 0.599。**置く側**（選ぶ側が最善を取る前提での、置いた側の得点）: 釣り合う 56 組から一様に置く **0.493**、全 492 組から一様に置く 0.396。95% 区間が 0.5 を含まない 405 組で手番を間違えたときの損は平均 0.241（得点差）。組ごとの先手の得点は最小 0.155・最大 0.814 と開きが大きいので、置く・選ぶの両方で表の有無が大きく効く。
- 2026-09-16 10:30 **サイト（tenbin-shogi-web）が使っている玉配置表の点検**（サイトのコードを読み、新しい表の得点と突き合わせた）。サイトは `src/data/libra-kings.json` に 9/15 15:17 の**旧表**（検証 48 組 × 100 局）の釣り合う 84 通りだけを持つ（取り込みは `scripts/kings/import-scale.mjs` が `balanced` だけを写す）。置く役は表から一様に選び（`src/engine/libra/kings.ts` の `pickKing`）、選ぶ役は両玉を置いた局面を**読み 128 回・ノイズ無し**で読んで先手の勝率 ≥ 0.5 なら先手（`src/app/game/controller.ts`、`KINGS_SIMS`）。新しい計測（303,677 局）で見ると、**サイトの 84 通り（代表 44 組）のうち 45 通り（代表 23 組）は 95% 区間が 0.5 を含まない**＝釣り合っていない。最も外れるのは ▲3g △6c の 0.619（区間 0.527〜0.711）、後手寄りでは ▲5h △3b の 0.459。置く側の得点（選ぶ側が最善を取る前提）は、**サイトの 84 通りから置くと 0.467、新しい 109 通りなら 0.4925**。旧と新で共通する代表の組は 17 で、旧のみ 27・新のみ 39。**後手玉が四段目の組は、表が無くても探索で先手を選べる**（v0.1、CPU、読み 128 回で 6 組を評価し、先手の勝率 0.990〜1.000）。サイトは `forced` を持たないが、この点は問題にならない。
- 2026-09-16 11:06 **▲3g △3c を読み 384 回で測り直した**（対照に ▲5i △5a。`~/libra-run/ls/scale/seq-3g3c-s384`、10:11〜11:06、4,822 局、約 122 局/分。手元 2,788 局＋vast.ai のオンデマンド RTX 5080 2,034 局）。**▲3g △3c は 2,410 局で 0.4846（95% 区間 0.4647〜0.5046）**、対照の ▲5i △5a は 2,412 局で 0.5031（0.4832〜0.5230）。読み 96 回の 0.4620（0.4432〜0.4808）と比べると +0.023（差の標準誤差 0.014、z 1.6 で有意ではない）が、**読み 384 回では区間が 0.5 を含み、後手寄りは確定しない**。探索値 V̂（読み 1,600 回）は 0.4928 で、これも 0.5 に近い。読みが浅いほど後手寄りに出る向きで、ゲームの前提が崩れている証拠にはならない。
- 2026-09-16 15:50 vast.ai のホストごとの自己対局ワーカーの費用対効果を、`~/libra-run/cloud` の 7 セッションのブリッジのログから**定常状態の局/日**（最初の 10 分を除いた回収の傾き）で並べ直した。**直近の 09/16 14:59 のホスト（RTX 5070 Ti ＋ Ryzen 7 7800X3D、8 コア 16 スレッドを専有・5.05 GHz、米国ニュージャージー、信頼度 0.998、入札 $0.1613＋ストレージで実効 $0.168/h、offer 50488949）が最良で 674,932 局/日・100 万局あたり $5.98**（転送料を除く。`libra-vast history` の $6.47 は準備時間と転送料込み）。以下、同じホストの 12:43 のセッション 664,615 局/日・$6.06、Ryzen 9 5950X ＋ 5070 Ti（実効 $0.157/h）576,000・$6.54、Ryzen 9 7900 ＋ 5080（オンデマンド $0.262/h）877,037・$7.17、Threadripper 3970X ＋ 5070 Ti（$0.184/h）566,040・$7.80、7950X3D ＋ 5080（$0.294/h）848,760・$8.32、Threadripper PRO 5975WX ＋ 5080（$0.251/h）646,405・$9.32。**同じ 5070 Ti で CPU だけが違う 3 台の差（566k → 576k → 675k）が最も大きく効いており**、9/14 のベンチ（同じ 5070 Ti で Xeon E5-2680 v4 351k → Ryzen 5 5600 476k → 7950X3D 669,605）と向きが一致する。675k は 9/14 の 7950X3D の 669,605 とほぼ同じで、5070 Ti ＋ 速い CPU の頭打ちに見えるが、本番のセッションでは GPU 使用率と段ごとの時間を記録していないので**未確認**。5080 のホストは局/日が 1.3 倍でも $/h が 1.6 倍で、CPU 律速のぶん 100 万局あたりでは 5070 Ti に負ける。`history` の表で直近が特に良く見えるのは準備時間の差もあり、**同じホスト・同じ価格・同じ定常速度**の 12:43（借りた 26 分・ssh まで 143 秒）と 14:59（46 分・35 秒）で $7.47 → $6.47 と動く。平均手数はこの間に伸びていて（手元の ls の metrics.jsonl の差分で 9/15 00:30 台 93.2 手 → 9/16 15:05 台 96.2 手、+3.2%）、局/日での比較は直近に不利な側。入札の値引きはホストによる: 9/15 の 3970X のホストはオンデマンドの実効 $0.184 に対し入札の実効 $0.183 で、最低入札の 10% 増し（`--bid-margin 0.1`）が値引きをほぼ食った（2026-09-15 の決定の例は同じホストで $0.248 → $0.182）。直近のホストのオンデマンド価格は記録が無く**未確認**。入札の 4 セッションはいずれも 19〜47 分で止められて借り直している。
- 2026-09-16 19:00 自己対局の CPU 側の速度改善（decisions.md 同日）の前後（手元の CPU のみ・nice 10・ls・lx 稼働中で負荷平均 2.5〜7、scratchpad の計測プログラムを libra-sim / libra-search の源と g++ 13 `-O3 -DNDEBUG -fPIC`（CMake の Release の静的ライブラリと同じ）で作る。`-fPIC` を付けないと変更前が 1.5〜1.8 倍速く出るので揃えた）。**実棋譜の局面での 1 呼び出し**（ls の `games_009546`〜`009549` の 400 局、布石 15,200・本将棋 24,113 局面、1 スレッド）: `write_features` 7.8 → 0.75 µs、本将棋の `legal_moves` 5.3 → 0.50 µs（平均 89.7 手）、本将棋の `outcome`（合法手の有無）5.4 → 0.08 µs、根の詰み探索（200 節点、平均 77.8 節点）1,419 → 105 µs（1 節点 18.2 → 1.3 µs）、布石終盤の証明探索（36 手目以降、1,000 節点 × 2 問題）85.6 → 33.9 µs。**疑似ネットの自己対局**（512 局、ls の [search] と同じ既定値＋根の証明探索の先送りと評価のキャッシュ、評価は特徴量のハッシュからの乱数のロジット、4,500 ラウンドのうち後の 3,000、平均 71.5 手、棋譜は前後で一致）の 1 ラウンドの CPU（ms、前 → 後）: 1 スレッド collect 4.52 → 0.92・apply 4.55 → 2.42・proof 4.40 → 0.60、4 スレッド collect 1.30 → 0.35・apply 1.68 → 1.07・proof 2.10 → 0.34、8 スレッド collect 0.76 → 0.31・apply 0.97 → 0.74・proof 2.03 → 0.36、12 スレッド collect 0.63 → 0.30・apply 0.79 → 0.59・proof 2.04 → 0.37。**GPU を待たせる collect＋apply** は 1 スレッド 9.07 → 3.34（×2.7）、4 スレッド 2.97 → 1.42（×2.1）、8 スレッド 1.73 → 1.05（×1.6）、12 スレッド 1.41 → 0.89（×1.6）。proof は GPU の評価と重なるので、局/日に効くのは GPU の評価より長いホスト（9/15 の Threadripper 3970X の 14.0 ms など）だけ。複数スレッドの apply は約 0.6 ms で下げ止まり（1 スレッド 2.4 ms ÷ 12 より大きく、遅い 1 局か待ち合わせの手間、未切り分け）。**局/日は未計測**: 手元の ls は GPU 律速（1 ラウンド約 27 ms）なので 12 スレッドの差 0.5 ms は約 2% の見込み、CPU 律速のクラウドのホスト（9/14 11:45 の 7950X3D で apply 2.4 ms、9/14 05:54 の Xeon で 18 ms）ではもっと大きい見込みだが、本番のネットの木（平均 96 手、布石の証明探索が多い）は疑似ネットと形が違うので、どちらも未確認。旗の比較（1 スレッド、変更前のコード）: `-mpopcnt` で根の詰み探索 242 → 151 µs、`-march=x86-64-v3` は棋譜が変わった（`-ffp-contract=off` を付ければ一致）、LTO は `-mpopcnt` と同程度。変更後のコードではどの旗も目立った差が無い（ばらつきの範囲）。
- 2026-09-16 20:00 X3D の L3 を自己対局に活かせるかの計測（手元の CPU のみ・nice 10・ls・lx 稼働中、scratchpad の計測プログラム）。**CCD の見え方**: WSL2 の `lscpu` は L3 96 MiB × 1（全 32 論理 CPU で共有）、L2 は論理 CPU 0-1・2-3… の組。64 B の節点を乱順に辿る遅延（THP）を論理 CPU 0〜31 に 1 つずつ固定して 2 回: 作業領域 20 MB で 77〜119 ns、64 MB で 102〜122 ns、どの CPU も同じ幅で 2 群に分かれない（固定なしでは 0.5 MB 3 ns、16 MB 42〜65 ns、32 MB 以上 95〜125 ns で、稼働中の ls・lx と並べると 32 MB でほぼ DRAM の遅延）。**自己対局の段ごとの性能カウンタ**（`perf_event_open`、1 スレッド、疑似ネットで 512 局、4,500 ラウンドのうち後の 3,000）: collect 1 ラウンド 500 万サイクル・命令/サイクル 0.97〜1.05・LLC ミス 7.7〜8.5 万、apply 1,280〜1,340 万サイクル・0.82〜0.85・11〜12 万、proof 4.4（計算律速）。区間ごと（計装版で 1 ラウンドあたり）: collect の特徴量の書き込み 3.2 万ミス（特徴量の行 512 × 10 KiB）、キャッシュ引き 0.8 万、apply の展開（合法手・方策の行の読み・辺の確保）のうち合法手以外で 2.1 万、キャッシュへの書き込み 0.9 万、逆伝播 0.6 万、次の葉への選択 1.3 万、着手 0.8 万。**df-pn の表をスレッドごとに**（前 → 後、棋譜は 1・4・8・12 スレッドで一致）: 常駐メモリ（VmRSS、12 スレッド）645 → 452 MiB、12 スレッド collect 0.312 → 0.319・proof 0.371 → 0.364・apply 0.601 → 0.618 ms、1 スレッド collect 0.999 → 1.010・proof 0.610 → 0.567・apply 2.749 → 2.774 ms（どれもばらつきの範囲。同じ版を続けて回しても apply が 5〜8% 動く）。`clock_` を solve ごとに 0 に戻した最終版と交互に 3 回ずつ（12 スレッド）: collect 0.357 / 0.365 / 0.363 → 0.361 / 0.364 / 0.363 ms、apply 0.630 / 0.644 / 0.642 → 0.643 / 0.650 / 0.656 ms、棋譜は 1・4・8・12 スレッドで一致。glibc の `hugetlb=1`（1 スレッド、同じ版で 2 回ずつ）: dTLB ミス apply 1.6 → 1.3 万、apply 2.64 / 2.51 → 2.77 / 2.30 ms で差は読めない。局/日は未計測（手元は GPU 律速で CPU 側は 1 ラウンド約 27 ms のうち約 0.9 ms）。
- 2026-09-16 20:10 自己対局と学習の GPU の内訳（ls・lx は停止して GPU を専有、RTX 5070 Ti・電力上限 250 W、ls の形 d 320・8 層、乱数の重みと入力、scratchpad の計測プログラム）。**推論 1 ラウンド**（同時 512、fp16、compile(max-autotune)＋CUDA Graphs、300 回 × 3）: 18.0〜18.4 ms（28,000 評価/秒）＝ CPU→GPU 0.30〜0.32 ms ＋ 順伝播 17.7〜18.6 ms ＋ GPU→CPU 0.09〜0.10 ms（ロジットを fp16 で送って CPU で戻すと 0.14〜0.16 ms で遅い）。**学習 1 ステップ**（バッチ 1,024、bf16 autocast、fused AdamW、今の `Trainer.step`、60 ステップの平均、2 回）: eager 169.3〜171.5 ms、compile(default) 143.5 ms（×1.20）、**compile(max-autotune-no-cudagraphs) 130.8〜131.3 ms（×1.30）**、compile(max-autotune、CUDA Graphs あり) 127.8 ms（×1.33、autotune 込みの立ち上げ 43 秒）。compile の有無の損失の差は 20 ステップで最大 2.1e-5〜3.6e-5。学習の GPU メモリの最大 eager 8,136 MiB・compile 7,925 MiB。バッチの CPU→GPU 0.74〜0.83 ms（pin すると 0.54〜0.58 ms。1 ステップの 0.2 ms なので変えない）。学習のバッチ作り（`ReplayBuffer.sample`、CPU、実際の窓 10 万局、30 回 × 3、ls 稼働中）: 8.8〜11.4 ms で、学習は GPU が律速。**本番の配分**（19:50 の status から）: ls の 1 周（新規 256 局）67 秒のうち学習 22.1 秒（95 ステップ、1 ステップ 233 ms で GPU 単独の 1.36 倍。lx との共有による）で約 33%。学習が 1.30 倍になると 1 周 約 62 秒で、ls の局/日は **約 +8% の見込み**（lx との共有下では未確認。反映後に測る）。
- 2026-09-16 21:10 学習の速度改善の候補（ls・lx は停止して GPU を専有、RTX 5070 Ti、ls の形 d 320・8 層、scratchpad の計測プログラム）。**学習 1 ステップの変種**（バッチ 1,024、乱数の重みと入力、60 ステップの平均、いずれも損失の式は同じ）: 今の Trainer（model だけ compile(max-autotune-no-cudagraphs)）124.9 ms、損失まで compile に入れる 123.8、＋毎ステップの `.item()` を最後の 1 回だけに 124.5、注意機構を FlexAttention（score_mod で相対位置バイアス）124.5、inductor coordinate_descent_tuning 124.0、CUDA Graphs 128.8。base との損失の差は 20 ステップで最大 2.5e-5〜3.0e-5。GPU メモリの最大 7.9〜8.0 GiB。（プロファイラのカーネル別の割合、ls・lx 稼働中の概算: GEMM 56%、注意機構 16%、要素ごと 13%、縮約 5%、その他 9%。5.4 TFLOP/ステップ ÷ 125 ms ≈ 43 TFLOPS、GEMM 部分だけなら ≈ 77 TFLOPS で理論値 87.9 の約 88%。）**自己対局と学習を重ねる**（実際の SelfPlayLoop 512 局・12 スレッド・ls の latest.pt の重み・compile＋CUDA Graphs、Trainer 95 ステップ、1 周の分量、2 回）: 逐次では自己対局 1,000 ラウンド 19.2〜19.6 ms/round ＋ 学習 127〜128 ms/step で合計 31.4〜31.6 秒、学習を別スレッドで重ねると学習が 161〜171 ms/step に伸び、その間に 179〜233 ラウンド（70〜85 ms/round）しか進まず、同じ分量の推定 31.0〜31.3 秒で **1.01 倍**（GPU が両方の段で飽和）。推論を高優先度ストリームに載せる変種は 15 分の打ち切りで未計測。稼働中の ls の GPU メモリ 14.7 GB（ls・lx・自動計測の対局）の下では計測プログラムが 8 GB を取れずステップが 11〜14 秒に落ちるので、共有下の数値は使っていない。lx の学習は 64 局ごとに 60 ステップ 3.5 秒（1 周約 65 秒の 5%）で、学習の compile の効果は lx では小さい。
- 2026-09-17 08:40 同じネットの自己対局の較正、最初の値（`bin/libra [--run lx] calib --games 20000`、書き出し済みのチャンクの最新 20,000 局、全読みの手、ls・lx 稼働中に CPU で約 1 秒）。**ls**（step 636,016、リーグの対局 1,814 局を除き 18,186 局、382,824 局面）: 全体 ECE 0.053・MCE 0.128・偏り（予測 − 実際）−0.019・Brier 0.189（基準 0.241）。3 手目（両玉の直後）ECE 0.017・偏り −0.009（4,130 局面）。布石・先手の番 0.046／−0.015、布石・後手の番 0.052／+0.019、本将棋・先手の番 0.056／−0.027、本将棋・後手の番 0.058／−0.035。曲線は中央（予測 0.4〜0.6）で実際とほぼ一致し、**両端で自信過剰**（全体: 予測 0.028 → 実際 0.083、0.150 → 0.278、0.849 → 0.774、0.967 → 0.922。布石・後手の番の 0.9〜1.0 は 0.932 → 0.710）。証明済みの手 80,239 は全部結果と一致。**lx**（step 287,097、20,000 局、289,860 局面。記録は全部本体との対局で、使うのは搾取者の手だけ）: 全体 ECE 0.283・偏り **+0.283**（予測 0.547、実際 0.263）・Brier 0.273 で基準 0.192 より悪い。区間の順序は保たれている（予測 0.034 → 実際 0.021 … 0.951 → 0.507）。status の搾取者勝率は 0.306（3,611 局）。これは自己対局の較正ではなく「本体に対する搾取者の勝率の予測」の当たり方。コードで確かめた事実: 使った手（搾取者の手番、`full=1`）の `root_q` は搾取者のネットだけの値（`selfplay.py` の `evaluate` は根の手番の持ち主のネットで価値を取り、根が搾取者なら木の価値は全部搾取者のネット）で、価値の学習は本体の手番の局面（手番側の勝率 約 0.7）も含む全局面。偏りの理由は未確認（推測: ネットの入力は手番側から見た局面だけで、手番側が搾取者か本体かを区別できないので、価値が両者の平均の 0.5 付近に寄る）。GUI が使う ls の 3 手目の偏りは小さい。
- 2026-09-17 10:00 ls・lx の処理時間のうち、段ごとの計測を入れる前の記録から分かる分（ls・lx 稼働中・GPU 共有、02:34 の起動し直しから 7.1 時間、log.txt と metrics.jsonl と status.json の 1 回の読み）。**学習**: ls は 5 分あたり 469 ステップ、status の直近の 1 回 94 ステップ 17.8 秒（189 ms/step、バッチ待ちを含む）から、**実時間の約 30%**（9/16 20:10 の見積もり 33% と同程度）。lx は 5 分あたり 444 ステップ、直近の 1 回 59 ステップ 2.4〜3.0 秒（41〜51 ms/step）から**約 6〜8%**。どちらも直近 1 回の秒数を 7 時間に広げた概算（未確認）。**チェックポイント**（10 分ごと、9/16 以降）: ls 127 回 中央値 0.3 秒・最大 0.4 秒、ONNX 書き出し 中央値 0.9 秒・最大 1.8 秒。lx 129 回 中央値 0.1 秒・最大 11.9 秒（9/17 09:26:01 の 1 回だけ）、書き出し 中央値 0.7 秒・最大 1.9 秒。合わせて実時間の 0.2〜0.3%。**自動計測**（ls、別プロセスで GPU を共有）: 9/16 の自己評価 402 秒・基準比 390 秒・外部計測 786 秒（9/15 は 378・352・884 秒）。自己対局の 1 ラウンドの内訳（収集・評価・証明・反映）はランナーが記録していなかったので出せない。同日の計測の追加（decisions.md）の反映後に `timing` から取る。
- 2026-09-17 10:40 搾取者モード（lx）の自己対局でネットの出力のキャッシュ（`eval_cache`、今は搾取者モードで切っている）を使った場合の当たり率の見積もり（CPU のみ・nice 15〜19・ls・lx 稼働中、lx の [search] の設定・64 局・`defer_root_proof`、キャッシュは切ったまま走らせ、collect が出した行を特徴量のバイト列で対局ごとにハッシュして、前に出た行を当たりと数える。最初の 1/3 のラウンドは数えない。scratchpad の計測プログラム）。**実際のネット**（lx の latest.pt step 約 296,000 と凍結した本体 main.pt、CPU・fp32、3,000 ラウンド・12.8 万行、終わった局は 9 局で布石と本将棋の前半に寄った標本）: 鍵に根の手番を足して同じ持ち主の探索の評価だけを使う形 **5.1%**、両方のネットの出力を持ち根の手番で選ぶ形 **16.8%**。**疑似ネット**（特徴量と使うネットの組のハッシュからの乱数、6,000 ラウンド・25.6 万行）: 根の手番込みの鍵 1.4%、両方のネットの出力 6.7%、同じ条件の自己対局（キャッシュの鍵は特徴量だけ）14.6%。当たりの多くは直前の手（相手の持ち主の探索）から来るので、根の手番で分けると残らない。本番の ls の実測は 19.1%（status の engine、`cache_hits / sims`）。
- 2026-09-17 11:18〜11:40 GPU の評価の効率と ls・lx の分け合い（ls・lx は停止して GPU を専有、RTX 5070 Ti・電力上限 250 W、ls の latest.pt・lx の latest.pt と main.pt・プールの最新の lx、fp16・compile(max-autotune)＋CUDA Graphs、学習なし、布石の読み込みなし、scratchpad の計測プログラム。lx は同日の両方のネットの出力を持つ評価キャッシュ入り）。**ネット単体**（固定バッチ、100 回 × 3 の最小）: ls の形（d 320・8 層）バッチ 64 / 128 / 256 / 512 で 2.16 / 4.33 / 9.20 / 18.71 ms（29,651 / 29,537 / 27,827 / 27,364 評価/秒）、lx の形（d 192・4 層）0.69 / 1.14 / 2.13 / 4.62 ms（92,620 / 112,723 / 120,481 / 110,935 評価/秒）。**本体のネットはバッチ 64 で既に演算が律速**（1 評価あたりは 512 と同じ）で、小さいバッチの非効率は無い。**実際の自己対局を単独で 120 秒**（1 秒あたりの手）: lx 同時 64 局・4 スレッド 549.0（292 ラウンド/秒、キャッシュの当たり 15.96%）、128 局・8 スレッド 604.4（+10%）、256 局・8 スレッド 611.0（+11%）。ls（自己対局 512 局・12 スレッド＋リーグ 64 局・4 スレッドを交互）自己対局 667.1・リーグ 67.0（42 ラウンド/秒、当たり 19.65%）。**2 プロセスで同時に 150 秒**（単独との比）: ls ＋ lx 64 局 → ls 466.1（**0.699**）・lx 211.6（**0.385**）、合計 1.08。ls ＋ lx 256 局 → ls 357.2（0.535）・lx 306.5（0.502）、合計 1.04。**1 プロセスで交互**（ls の 1 ラウンドごとに lx 64 局を 4 ラウンド）: ls 413.9（0.620）・lx 195.9（0.357）、合計 0.98。まとめ: 2 プロセスの共有による損は無く（CPU の段が重なるので合計は 1 を超える）、1 プロセスにまとめても増えない。lx の同時局数を増やすと、効率は変わらずに lx が GPU を多く取る（WDDM の時分割で、1 回に出す仕事の大きい側が長く使う）。今の lx 64 局は GPU の約 3 割を取っており、lx が無ければ ls の自己対局は約 1.43 倍（学習の時間は局数に比例するので、局/日の伸びはこれより小さい。未計測）。
- 2026-09-17 11:55〜12:18 過去と今の搾取者 lx 対 本体 ls のノイズなし 200 局を 4 本（`bin/libra --run lx eval --games 200 --sims 96 --concurrent 64 --threads 8 --seed 11 --no-noise`、9/14 17:00・9/15 09:00 と同じ条件。lx の探索設定、評価ハーネスは opponent_prior を使わない、fp16。lx はプールの保存を学習用の形に直した写し、本体は `checkpoints/archive`。前半の 2 本は ls・lx 稼働中で GPU を共有して各 1,147 秒、後半の 2 本は 12:14:57 に ls・lx が停止された後で各 216〜219 秒。9/14・9/15 に使った lx step 73,944 は残っていなかった）。lx から見た得点: **lx step 116,230（9/15 02:24 保存）対 本体 step 422,358（9/15 05:06）63.5%**（127 勝 73 敗、Elo +96、95% 区間 +48〜+149、平均 119.8 手）、**同じ lx 対 本体 step 646,699（9/17 10:19）79.0%**（158 勝 42 敗、Elo +230、区間 +176〜+297、先手 80-0-16・後手 78-0-26、平均 121.6 手）、**lx step 283,910（9/17 08:00 保存）対 本体 646,699 29.75%**（59 勝 1 分 140 敗、Elo −149、区間 −206〜−99、平均 106.6 手、千日手 1）、**lx step 300,492（今の lx）対 本体 646,699 32.0%**（64 勝 136 敗、Elo −131、区間 −186〜−82、平均 106.6 手）。2 日前の lx は、同じ時期の本体より今の本体に 15.5 ポイント多く勝つ（差の標準誤差 約 4.5 ポイント、約 3.5σ）。今の lx と今朝の lx は今の本体に 3 割しか勝てず、学習中の成績（凍結相手に 28〜29%）と同程度。**今の本体には古い lx が突く穴が残っており（または学び直しで開いた）、今の lx はそれを使っていない**。step 116,230 はプールの 10 体のうち最も古く、ls のリーグが選ぶ新しい 5 体（`recent`）に入っていない。
- 2026-09-17 13:15〜13:45 始め直した搾取者 lx（9/15 の step 116,230 の重み、step の数えは 301,826 から続く、最適化の内部状態なし、リプレイの窓は空、布石は空、凍結相手は main.pt step 633,193、ls と GPU 共有、eval_cache 有効）の対凍結相手の勝率を metrics.jsonl の 5 分ごとの累積から区間に直した値: 最初の 561 局（学習の開始 13:19:47 より前、重みは step 116,230 のまま）0.800、以降 523 局 0.406・532 局 0.346・504 局 0.317・537 局 0.281・538 局 0.287。始め直す前の lx は同日 08:00〜12:15 に累積 0.29〜0.30。約 3,200 局・2,900 学習 step で始め直す前と同じ水準に戻った。最初の 0.80 は 11:55〜12:18 のノイズなし 200 局の 79% と合う（重み自体は今の本体に強い）。重みの相対変化（2 次元以上の重みの ‖Δ‖/‖w‖、CPU）: step 116,230→304,724（2,898 step）0.181、283,910→301,826（17,916 step）0.221、116,230→135,997（19,767 step）0.322。学習率のウォームアップ（`lr_at(step_count)`、1,000 step）は step の数えを引き継いだため効かず、始めから lr 3e-4・AdamW のモーメント 0 で学習した（metrics の lr 0.0003）。原因の切り分けは未確認（ウォームアップなし・モーメントなし・小さい窓のどれか、または組み合わせ）。比べる前の 0.29 は布石 10% を含み、始め直した後は布石なし（openings 0 件）なので厳密に同条件ではない。lx の局/日（1 時間の移動平均）は 13:45 に 151k、ls は 354k だが窓が埋まっておらず、キャッシュの効果としては未確認。lx のキャッシュの当たり率は metrics に出していないので未確認
- 2026-09-17 14:05〜14:27 本体 ls の保存済みの重みどうしの 1,000 局を 2 本（`bin/libra eval --games 1000 --sims 96 --concurrent 64 --threads 8`、根のノイズあり＝毎日の自動評価と同じ条件、seed 21・22、ls・lx は停止して GPU 専有、1 本 約 10.5 分）。**525,172 対 646,699: 525,172 の得点 0.5265、Elo +18.4（95% −3.1〜+40.1）**。**422,358 対 646,699: 422,358 の得点 0.568、Elo +47.5（95% +26.0〜+69.5）**。9/15 05:06 に保存した 422,358 より 9/17 10:19 の 646,699 が弱い（約 22 万 step・約 57 万局の学習で後退）。自動評価（各 100 局）は 274,739→422,358 +123、422,358→525,172 +49、525,172→646,699 −35、基準 230,210 比 +139・+220・+147・+173 で、100 局では区別できなかった。較正（得点の予測と実際、評価側の探索値）: 646,699 は予測 0.55〜0.6 で実際 0.36〜0.40、0.75 で 0.43〜0.48 と勝ちを高く見積もる。422,358 は 0.55 で 0.54、0.75 で 0.59。終局は裁定 25%・詰み 75%、平均 98〜100 手。原因は未確認（この間の ls の設定の変更は log.txt に無く、リーグは 9/14 22:15 の step 361,203 から、学習の compile は 9/16 20:22 から）
- 2026-09-17 14:40〜14:51 本体 ls の 422,358 対 525,172 の 1,000 局（同日 14:05 と同じ条件、seed 23、ls・lx 停止中で GPU 専有）: **422,358 の得点 0.540、Elo +27.9（95% +6.3〜+49.6）**。同日の 2 本（422,358 が 646,699 に +47.5、525,172 が 646,699 に +18.4）の差 +29 と合う。後退は 422,358→525,172 の区間から始まっていた。この区間の ls の設定は 2 つのチェックポイントに保存された config で全項目同じ、学習の経路のコードの変更は統計を出すだけの 33394a5（9/16 00:38 の起動から、目標の値は同じ）。区間の運用の違いは vast.ai のワーカーの局（9/15 10:00〜15:30 に約 13.4 万局、区間の局の約 56%。274,739→422,358 は約 17 万局・約 39%、525,172→646,699 は約 5 万局・約 14%、log.txt の `workers: ingested` から）。9/17 の自動評価（100 局）は 422,358→525,172 を +49（95% −19〜+121）としていて、向きが逆だった
- 2026-09-17 15:00〜16:20 学習率の確認（オフライン、ls・lx 停止中で GPU 専有、本番の run には書かない）。646,699 のチェックポイント（重み＋AdamW の状態）から、そのときの窓（chunk_index 11,743 までの 10 万局、読むだけ）で 5,000 step 学習（バッチ 1,024、`replay.sample` と `Trainer` を本番と同じ設定で使う scratchpad のスクリプト、学習の torch.compile max-autotune、1 本 約 11 分）し、`bin/libra eval --games 1000 --sims 96 --concurrent 64 --threads 8 --seed 31`（根のノイズあり）で比べた。**学習率 2e-4（今と同じ）: 対 646,699 −38.0（95% −59.9〜−16.5）、対 422,358 −65.0（−87.2〜−43.3）。学習率 2e-5: 対 646,699 −38.4（−60.2〜−16.8）、対 422,358 −70.8（−93.1〜−49.1）**。重みの相対変化（2 次元以上の ‖Δ‖/‖w‖）は 2e-4 で 0.067、2e-5 で 0.011。学習率を 1/10 にしても同じだけ弱くなり、学習率の高さでは説明できない。同じ窓を繰り返し学ぶ条件で、本番の自己対局とは違う。compile を使わない学習・さらに小さい学習率・422,358 からの同じ確認は、ユーザーの判断「追加の実験は不要」で行っていない
- 2026-09-17 16:30〜17:20 **本体の保存済みの重みが、リプレイの窓の中の局面だけに当てはまっている計測**（CPU 推論だけ、GPU 不使用、ls・lx 停止中。`replay.sample` で 10 チャンク＝1,000 局から 2,000 局面を鏡映なしで取り、`lambda_z` 0.5 の学習目標 t と、ネットの V＝W−L・方策の交差エントロピー（上位 32 の目標）を比べる。リーグの局は除く。スクリプトは scratchpad の memprof.py）。**646,699（窓＝チャンク 10,743〜11,742）**: 窓の中（10,800）は本将棋の corr(V,t) 0.943・二乗誤差 0.110、布石 0.888・0.107。窓の外は、古い側 1,343 チャンクで本将棋 0.828・0.308、2,143 で 0.624・0.627、3,343 で 0.568・0.698、6,143 で 0.466・0.887、10,543 で 0.434・0.914。**自分の直後の世代が打った新しい側 257 チャンク（12,000）でも本将棋 0.427・0.907、布石 0.660・0.298**。窓の中でも最も新しい 11,700（学習した回数が少ない）は 0.691・0.500。方策の交差エントロピーも同じ形（窓の中 1.74〜2.05、外 2.13〜2.42）。**422,358（窓 4,624〜5,623）**: 窓の中 5,000 は本将棋 0.929・0.137、新しい側 276 チャンク（5,900）で 0.501・0.800、6,076 チャンク（11,700）で 0.458・0.867。**274,739（窓 392〜1,391）**: 窓の中 600 は 0.928・0.141、新しい側 108 チャンク（1,500）で 0.409・0.914。本将棋の目標はほぼ ±1 なので、常に 0 を出す予測の二乗誤差は約 1.0。**窓の外では価値の予測がほとんど情報を持たず、これは 9/14 の 274,739 から同じ**。
- 2026-09-17 17:00 上と同じ窓（チャンク 10,743〜11,042 の 27,284 局・255 万局面）で局面（SFEN）の重複を手数ごとに数えた（CPU）: 異なる局面 97.3%。他の局にも現れる局面は 5 手目まで 83.0%、6〜10 手目 9.9%、11〜15 手目 2.9%、16〜20 手目 0.2%、21 手目以降 0.0%。**本将棋の局面は一局ごとに全部違う**ので、窓の中の本将棋での当てはまり（corr 0.94）は同じ局面の記憶ではなく、その局の結果の記憶。
- 2026-09-17 16:40 保存済みの 4 つの重み（274,739・422,358・525,172・646,699）を同じ局面（チャンク 5,600〜5,619 と 11,700〜11,719 から各 3,000 局面）で比べた（CPU、scratchpad の netdiag.py）: 方策のエントロピーは布石 2.63〜2.71・本将棋 2.07〜2.26、最大確率は布石 0.18〜0.19・本将棋 0.34〜0.37で、世代によらずほぼ同じ（鋭くなっても平らになってもいない）。世代間の方策の KL は 422,358→525,172 が布石 0.37〜0.45・本将棋 0.36、422,358→646,699 が 0.56〜0.64・0.44〜0.46（約 10 万 step ごとに 0.4 nat 動く）。同じ局面での価値の相関は、それを打った世代の重みだけ高い（5,600 では 422,358 が 0.744、646,699 は 0.628 で 274,739 の 0.646 と同じ水準。11,700 では 646,699 が 0.705、422,358 は 0.599）。
- 2026-09-17 16:20 学習目標の鋭さの推移（ls の replay の全チャンク、400 チャンクごとに 20 チャンク、全読みの手だけ、CPU）: 目標の最大確率の平均 0.73〜0.75、0.99 超の割合 0.36〜0.41（うち証明済みの one-hot 0.13〜0.15）、エントロピー 布石 1.24〜1.41・本将棋 0.38〜0.47 で、9/13 から 9/17 まで変わらない。全読みの手の割合 0.255〜0.277。
- 2026-09-17 16:20 探索値と結果の相関（replay、各時点 3,000 局、リーグ除く、CPU）: corr(root_q, z) は布石 0.46〜0.53・本将棋 0.56〜0.59、**corr(v41, z) は 0.18〜0.23**（9/13〜9/17 で変わらず）。v41 の平均は −0.07〜−0.13、同じ局の z（先手から、41 手目に達した局）は −0.00〜−0.06。
- 2026-09-17 16:10 起動の前後で学習の損失・一致率に段差は無い（metrics.jsonl の起動前後 4 行ずつ、9/14 12:18〜9/17 13:15 の 19 回）。自己対局の推論の重みがセッション内で古いままになる不具合なら段差が出るはずで、その候補は否定。
- 2026-09-17 16:00 棋譜 JSONL の 500 チャンクごとの集計: リーグ（対 lx）の局の割合は 9/15 00:28 の 1.7% から上がり、9/16 12:03 以降は **7.9〜9.1%**（status 文書の「約 3.7%」は vast.ai の局が入っていた区間の値）。終局理由・平均手数・先手勝率・布石の手順の異なり（12 手で 0.93〜1.00）は 9/13 から変わらない。
- 2026-09-17 16:00 lx の metrics.jsonl の 3 時間ごとの平均: 方策の損失は 9/14 21 時の 1.80 から 9/17 00 時の 2.39 へ上がり続け、一致率は 0.54 から 0.33 に下がった（ls は 9/14 以降 2.17〜2.23・0.41〜0.42 で平ら）。
- 2026-09-17 18:05〜19:20 **窓の大きさの確認実験**（ユーザーの指示「確かめる」。ls・lx 停止中で GPU 専有。本番の run には書かない。重み・結果・スクリプトは `~/libra-run/experiments/2026-09-17-window/`）。646,699（重み＋AdamW の状態）から、§6（同日 15:00）と同じ手順・同じ設定（学習率 2e-4、バッチ 1,024、compile(max-autotune)、5,000 step）で、**リプレイの窓だけを 10 万局（本番と同じ）から 100 万局（ディスクのチャンク 1,743〜11,742、約 9,200 万局面、読み込み 34 秒、RAM 約 8 GB）に変えて**学習した。学習の損失（500 step ごとの平均）: 100 万局の窓では 3.37 → 3.31（価値 0.62 → 0.60）とほとんど下がらず、10 万局の窓では 2.83 → 2.74（価値 0.36 → 0.32）。**記憶の輪郭**（同日 16:30 と同じ手順）: 100 万局版は本将棋の価値の相関が窓の中 0.65〜0.75、直後の世代の局 0.58（二乗誤差 0.62）、6 時間前 0.61、2 日前 0.56 と、中と外の差が消えた。10 万局版は窓の中 0.95（0.10）、直後の局 0.39（1.02）、2 日前 0.39（1.02）と、学習前（0.43）より外が悪化。**1,000 局（読み 96、根のノイズあり、同時 64、seed 51〜53、1 本 約 8 分）**: 100 万局版の得点は **対 646,699 が 0.847（+297 Elo、95% +269〜+329、先手番 484-0-13・後手番 363-0-140）、対 422,358（最強の保存済み）が 0.838（+286、+258〜+317）、対 10 万局版（§6 の lr 2e-4）が 0.854（+306、+277〜+339）**。平均手数 76〜80（相手の 95〜103 より短い＝早く詰ましている）。終局理由の割合は変わらない（裁定 25〜26%）。**同じ学習量で窓だけを 10 倍にすると、記憶の型が消えて 300 Elo 上がる。原因の見立て（status-2026-09-17.md §10）を支持**。
- 2026-09-17 21:53 やり直しの ls（20:34 に `[workers] enabled` で起動、step 約 2,600〜6,600、乱数初期化から 2 時間弱で平均 69 手）に vast.ai を 1 台（21:05 起動、RTX 5090 ＋ Ryzen 7 7800X3D、韓国、オンデマンド $0.547/h、3 時間）。ls の metrics.jsonl の 21:15〜21:50（35.7 分）: **全体 約 242 万局/日・6,670 step/h、手元の自己対局 約 57.5 万局/日**。ブリッジの回収は 21:12〜21:53 で 53,300 局（約 185 万局/日）、弾いた局 0・誤り 0・ls が古すぎて捨てた局 0、検査 0.53 ms/局。**重みの送信は 5 分に 1 回（9/15 の決定の `--min-push-seconds` 300）で 42 分に 8 回、1 回 5.8〜120.1 秒**（韓国への 21:47 の回が 120 秒）。ワーカーの重みは最大で約 7 分（この速さで約 800 step）古く、`max_lag_steps` 2,000 の内。9/15 の上限の見積もり（全体 約 105 万局/日、lx と共有・平均 87 手）を超えているのは、局が短く lx が無いためとみられる（未確認）。起動前の 20:44〜21:00 の手元だけは約 26 万局/日（再起動直後の区間で、原因は見ていない）。
- 2026-09-17 22:53 やり直しの ls の 12 万局の archive（step 8,144、総局数 120,098）の自動計測（途中）: **最強比 step 1,416 に +359.7 Elo（区間 +327.9〜+396.8、1,000 局）**、**固定の参照 ls-v1 step 646,699 に +17.4 Elo（新しい ls の勝ち 52%、200 局）**（step 1,416 では −322.7）。win1m と基準比は実行中。20 万局の archive（step 13,807、22:44）は順番待ち。計測 1 組（最強比 1,000 局・参照 200 局 × 2・基準比 1,000 局）は約 1 時間で、クラウド 2 台で総局数が 1 時間に約 14 万局（games/day(1h) 3,488,720）増えるため、10 万局ごとの archive に計測が追いつかず積み上がる。**一般化の差**: `gen` の本将棋の価値の相関（窓の中 → held-out）は 22:05 の 0.557 → 0.555 から 22:52 の 0.634 → 0.572 へ差が開いた（方策の交差エントロピーの差も 0.24 → 0.27〜0.37）。held-out は起動からの全期間のチャンク（20 に 1 つ）で、窓（最新の約 11 万局）より古い局を含む。同じ時期の held-out と窓のチャンクを 1 つずつ並べて CPU で測ると（step 13,807、各 100 局・2,000 局面、`genprof --chunks`）、本将棋の相関は held-out 1,200・1,600・1,800・1,980 が 0.748・0.658・0.659・0.572、隣の窓のチャンク 1,201・1,601・1,801・1,981 が 0.667・0.571・0.684・0.583 で、**同じ時期なら held-out が窓の中より悪くはない**（1 点 100 局で幅は大きい）。差が開いたのは held-out の局の古さによるとみる（記憶なら同じ時期の窓のチャンクだけが良くなる）。
- 2026-09-18 00:12 煙テストのクラウド 2 台の結果（どちらも RTX 5090 ＋ Ryzen 7 7800X3D、韓国、オンデマンド $0.547/h）: **1 台目**（vast1、21:05〜00:11、借りた 3.11 h）回収 217,444 局・弾いた 0・誤り 0・捨てた 0、費用の見積もり $1.71、**1,736,080 局/日・100 万局あたり $7.87**、重みの送信 34 回（最大 120 秒）。**2 台目**（vast2、`--root ~/libra-run/cloud2`、22:26〜00:00、1.56 h）回収 106,794 局・弾いた 0・誤り 0・捨てた 0、$0.86、**1,706,429 局/日・$8.03**、送信 18 回（最大 10.9 秒）。2 台の間（22:30〜23:59）の ls は games/day(1h) 約 335 万〜363 万局で、捨てた局 0（取り込みは学習に追いついた）。総局数は 20:19 の起動から 00:12 に 412,366 局（うちワーカー 324,238 局）。2 台目のセッションは終了後に `~/libra-run/cloud/` へ移して履歴に入れた。vast.ai の残高 $1.28、9 月の費用 $5.82。
- 2026-09-18 01:15 ls の学習の時間の割合と取り込みの関係（metrics.jsonl の 5 分ごとの timing、2026-09-17 21:50〜09-18 01:12、スケールの検討 docs/scaling-2026-09-18.md のため）: **クラウドなし**（00:16〜01:12）全体 約 62 万局/日・学習 8〜10%・約 2,000 step/h・157〜177 ms/step（自動計測と GPU を共有）、**1 台**（21:50〜22:31）約 240 万局/日・24〜33%・約 6,700 step/h・130〜160 ms/step（22:10 からは計測と共有で 159 ms）、**2 台**（22:36〜23:56）約 374 万局/日・49〜54%・約 11,700 step/h・155〜160 ms/step、1 step あたり 12〜13 局。学習だけに使えば約 22,600 step/h ≈ 85 手の局で約 650 万局/日（計測の共有なしで約 790 万）が手元の学習の上限という見積もり（直線で延ばした値、未確認）。自己対局の timing の割合は手元の局だけを表す。ls-v1 のリプレイは 120 万局で 6.0 GB・棋譜 0.77 GB（1 局 約 5.6 KB、古いチャンクは消さない）。
- 2026-09-18 01:17 **煙テストの判定（やり直しの ls、総局数 30 万局の archive まで）**。10 万局ごとの archive（区切りの変更前は 18,700 局のずれ込み）の自動計測: **最強比** step 1,416 → 8,144（120,098 局）+359.7 [+327.9, +396.8]、→ 13,807（200,532 局）+216.8 [+192.0, +243.9]、→ 21,270（約 30 万局）+271.8 [+244.7, +301.9]（各 1,000 局）。**参照 ls-v1 step 646,699**（200 局）: −322.7（step 1,416）→ +17.4 → +143.1 → **+225.0**（勝ち 78%）。**参照 win1m**: −301.3 → −275.5 → −130.9 → **−81.4**（勝ち 38%）。**基準比**の累積 +965.9（基準は 402 → 8,144 → 13,807 と置き換え）と記録したが、offset の誤りで、正しくは +626.3（8,144 対 21,270 の +271.8 ＋ 8,144 の +354.5）、基準は 8,144（decisions.md 2026-09-18）。**M1**: held-out の本将棋の価値の相関 0.5724 → 0.6396（直近 20 万局）、窓の中との差 +0.026。`bin/libra review` は全項目「続ける」。ls2-settings.md §5.1 の合格条件（M1 差 0.1 未満で上昇、M2 20 万・30 万局の archive が前の最強に区間の下限 > 0 で勝つ、M4 参照に対して上がる）をすべて満たした。起動（20:19）から約 5 時間、うちクラウド 2 台ぶんの 32.4 万局を含む。
- 2026-09-18 02:23 **40 万局の archive（step 28,908）の自動計測**（本番の区切りへの変更前、10 万局ごとの最後）: **最強比** vs step 21,270 勝率 79.7%・+237.6 [+211.9, +265.7]（1,000 局、最強を置き換え）、**固定の参照** vs ls-v1 646,699 勝率 83%・+275.5 [+217.8, +349.5]（20 万局の点 +225 から）、vs win1m 勝率 54.5%・+31.4 [−16.7, +80.6]（20 万局の点 −81.4 から。区間は 0 を含む）（各 200 局）、**基準比** vs step 8,144 勝率 85.2%・+304.1、累積 +658.6 [+629.9, +691.0]（1,000 局。85% 超で基準を 28,908 に置き換え）。走っていた古いコードは累積を +998.2（相手 13,807）と誤記し、02:29 の起動で修復（`anchor.jsonl.bak-20260918-022919` に旧行）。起動後の設定は every_games 400000、次は 80 万局（クラウドなし 約 62 万局/日なら約 13 時間後、見込み）。
- 2026-09-18 07:16〜07:29 **80 万局の archive（step 62,426、総局数 800,229）の自動計測**（本番の区切り 40 万局ごとの初回。数値の出所は progress ブランチの `progress/ls.json`、09:48 の書き出し）: **最強比** vs step 28,908 得点 0.832・**+277.9 Elo [+250.6, +308.5]**（1,000 局、最強を 62,426 に置き換え、足踏み 0）、**基準比** vs step 28,908（offset +658.6）+277.9、**累積 +936.5 [+909.2, +967.1]**（1,000 局）、**固定の参照** vs ls-v1 646,699 得点 0.9025・**+386.6 [+317.4, +489.4]**（40 万局の +275.5 から +111.1）、vs win1m 得点 0.800・**+240.8 [+185.8, +308.9]**（40 万局の +31.4 から +209.4。ここで初めて区間の下限が 0 を超えた）（各 200 局）。**外部計測**（09:23、相手 Tenbin Fuseki Engine 0.1 ＝ fuseki-shogi-ai の方策ネット＋水匠5、movetime 1000、10 局）: **Libra の得点 7.0（勝率 70%）**、終局は 10 局とも詰み。旧系列の同じ計測は 9/12〜9/16 の 6 回で得点 0・0・0・1・1・0（docs/status-2026-09-17.md §2.3）で、**初めてはっきり勝ち越した**。ただし 10 局と少なく、この対局の Libra 側の重みは節目に固定した 62,426 ではなくその時点の `latest.onnx`（step 74,627）で、節目固定にする修正（commit 1f6105b）はこの run にまだ入っていなかった。**M1 一般化**: held-out の本将棋の価値の相関は直近 20 万局で 0.6543 → 0.6539（−0.000、上がっていない）で `bin/libra review` の判定は「注意」。窓の中との差は −0.0023（held-out のほうがわずかに良い）なので窓の記憶ではなく、物差しが頭打ち。40 万局（02:23 ごろ）から 80 万局（06:30 ごろ、archive の時刻は未取得）まで約 4 時間で、クラウドなしの見込み（約 62 万局/日なら 13 時間）より速い。理由は未確認。
- 2026-09-18 **伸びの曲線（100 万局に達した時点。`bin/libra scaling`、数値の出所は progress ブランチの `progress/ls.json` の 09:48 の書き出し）**: 固定の参照との Elo を総局数の対数に当てはめると、**局を 2 倍にして +185 Elo（帯の中の 5 点、残差 23 Elo）**。区間ごとの 2 倍あたりは 12 万→20 万局 **+192**、20 万→30 万局 **+85**、30 万→40 万局 **+274**、40 万→80 万局 **+209**（100 万局あたりでは +1,770・+494・+1,134・+523。総局数が増えるほど下がるのは局数が対数で効くため）。各点 200 局で 95% 区間が ±50〜70 Elo あり、この散らばりは局数で説明がつく。**伸びが落ちている証拠は無い**。費用は 100 万局 $8（9/17 の実測）なので、80 万局を 2 倍にする 80 万局が $6.4、**1 Elo あたり $0.035（約 5 円）**。80 万局から 100 万局買い足して 180 万局にすると **+216 Elo の見込み**（未確認）。目盛りは win1m を 0 とし、12 万局の点だけ弱いほうの参照（ls-v1 646,699）を +290 でずらして使った（両方が帯の中にある 20 万・30 万局の点の差の平均。ls2-settings §5 の「win1m は旧 ls に +297」と合う）。最強比の積み上げ（基準比の累積 +936.5）は鎖の膨らみを含み、固定の参照で測った同じ区間の伸び（step 402 の −308 → step 62,426 の +387 ＝ +695）より 240 Elo 大きい。**注意**: 80 万局の点で win1m への勝率が 80%、ls-v1 646,699 は 90% で、次の節目からは 2 つとも天井に着く（docs/scaling-2026-09-18.md §6.4）。
- 2026-09-18 13:11 **設定をリポジトリで管理する形の確認**（decisions.md 2026-09-18 の「確かめる計測」）: ユーザーの停止 → 起動のあと、`progress` ブランチの `progress/ls-config.toml` の冒頭が「このファイルは `libra run` が作り直す。設定の正は `origin/main:config/ls.toml`」になり、`[auto] reference_ckpts` が 3 つ（ls-v1 646,699・win1m・**ls の 80 万局の archive `ckpt_000062426.pt`**）になった。**手元のチェックアウトを更新せずに main の設定が効くことを実機で確認**（ランは step 81,919、総局数 1,017,659 で稼働中）。引き継ぎのとき値は 1 つも変えていない（`libra config` の反映待ちの違いは `reference_ckpts` の 1 件だけだった）。なお同日 12:34 の停止 → 起動では起動の記録（`start:` の行）が `log.txt` にも `stdout.log` にも残らず、ランが約 20 分止まった（`bin/libra status` は正常、`restarts:` にも記録なし。コンソールの `schtasks /Run` が届かなかったとみる。2 回目の「起動」で復帰。未確認）。
- 2026-09-18 13:26〜15:45 **布石の価値目標 λ のオフライン比較**（docs/ls2-settings.md §5.2 の「仮」の設定値の比較。`bin/libra abtest --ckpt ~/libra-run/ls/checkpoints/archive/ckpt_000062426.pt --arm lam05:train.lambda_z=0.5 --arm lam10:train.lambda_z=1.0 --steps 5000 --games 1000 --publish`。80 万局の archive（step 62,426）と**その節目の窓**（chunk 8,000 までの 400,114 局・3,643 万局面、読み込み 10 秒）から、腕の間で同じ局面・同じ鏡映を引いて 5,000 step 学習。本番の ls とクラウドのワーカーは動かしたまま GPU を共有（ユーザーの判断。学習は λ 0.5 が 2,519 秒、λ 1.0 が 1,322 秒で、共有の混み具合の差。結果は回数で決まるので変わらない）。結果は `progress` ブランチの `experiments/20260918-132627-abtest.json`）。**1,000 局（読み 96、根のノイズあり）: λ 0.5 対 λ 1.0 は得点 0.4945・Elo −3.8（95% −25.4〜+17.7）で差は出なかった。元の重み（step 62,426）に対しては λ 0.5 が +37.0（+15.4〜+58.8）、λ 1.0 が +49.0（+27.4〜+70.9）**（平均 96.5〜98.5 手）。**一般化（λ 1.0 の目標＝実際の勝敗で測った共通の物差し `gen_z`、窓 → held-out、各 4,000 局面のうち布石 1,687・本将棋 2,313）: λ 0.5 は布石 corr 0.6009 → 0.5572（二乗誤差 0.6328 → 0.6842）・本将棋 0.6606 → 0.6442、λ 1.0 は布石 0.6002 → 0.5596（0.6340 → 0.6821）・本将棋 0.6610 → 0.6448 で、**こちらも差は無い**（差 0.002〜0.004 は 4,000 局面の幅の内）。学習の損失の価値の項（λ 0.5 が 0.569、λ 1.0 が 0.477）は目標そのものが違うので比べられない。λ 0.5 の `gen_own`（自分の混ぜた目標で測った値）は布石 corr 0.82・二乗誤差 0.17 と高く出るが、これは V̂41 を混ぜた目標が自分のネットの予測に近いだけで、実際の勝敗の当たり方ではない。**この比較で分かるのは「5,000 step の範囲で ±25 Elo より大きな差は無い」ことで、長い学習での小さな差は未確認**。
- 2026-09-18 16:08〜16:29 **σ の形のオフライン比較**（docs/ls2-settings.md §2 の「仮」の設定値。`bin/libra eval --a $CK --b $CK --games 1000 --b-set gumbel_rescale=true --b-set c_scale=0.1 --publish`、$CK は 80 万局の archive `ckpt_000062426.pt`。同じ重みで、A 側が今の形（c_scale 1.0・正規化なし）、B 側が mctx 形（[0,1] 正規化・c_scale 0.1・v_mix）。読み 96、根のノイズあり、1,000 局、1,276 秒。結果は `progress` ブランチの `experiments/20260918-160803-sigma.json`）: **今の形が +168.8 Elo [145.4, 193.8]（得点 0.7255）で勝った**。先手側 424 勝 2 分 70 敗（85.4%）、後手側 300 勝 1 分 203 敗（59.6%）で**両方の側で勝ち**、平均 97.0 手、終局は詰み 717・41 手目の裁定 280・千日手 2・320 手 1。**読む手の選び方としては今の形が明らかに良い**ので設定は変えない。**限界**: これは読む手の選び方だけの比較で、σ が方策の目標の形を変える分（学習データ側）は測れない（窓の中の目標は今の σ で作られている）。1 回目（同日 15:50 ごろ）は手元の C++ のビルドが古く `set_side_config` が無くて止まった（案内漏れ。`cmake --build build` のあと成功）。
- 2026-09-18 **120 万局の節目の固定の参照（4 点目）と、伸びの曲線の更新**（`progress` ブランチの `progress/ls.json`、step 98,604・総局数 1,200,126、各 200 局）: win1m に **+281.7（得点 0.835、天井）**、ls-v1 646,699 に **+372.3（0.895、天井）**、**80 万局の archive `ckpt_000062426.pt` に +157.7（0.7125、帯の中）**。win1m の目盛りに直すと 120 万局は **+398.5**（ずらし +240.8。80 万局の点の step が 62,426 ＝ この参照そのものなので「自分対自分 ＝ 0 Elo」で決まり、推定ではない）。**80 万 → 120 万局は +157.7 Elo ＝ 2 倍あたり +270 で、これまでの 5 区間で最も速い**（+192・+85・+274・+209・+270）。帯の中の 6 点の当てはめは **+200 Elo / 2 倍（残差 28）**で、5 点のときの +185（残差 23）から上がった。費用は 120 万局を 2 倍にするのに $9.6、**1 Elo あたり $0.048（約 7 円）**。次の 100 万局の見込みは +175 Elo。**伸びが鈍った証拠は無い。** なお公開されている `scaling` は 80 万局の archive を `dropped` に落としていて 120 万局の点が乗っていない（9/18 の決定の「確かめる計測」が、予測どおり**落ちる側で再現した**: 新しい参照の最初の計測で win1m は既に 0.835 の天井にあり、帯の中で重なる点が無い）。`_self_point` を入れた版は main にあり、次の停止 → 起動で公開値も 120 万局まで伸びる。上の値は同じ計算を手元（`stitch`・`fit`）で回したもの。
- 2026-09-18 19:00〜20:03 **120 万局の archive（step 98,604、総局数 1,200,066）の自動計測**（数値の出所は `progress` ブランチの `progress/ls.json` の 20:03 の書き出し。λ を 1.0 にした直後の節目で、archive の時点で新しい目標で作られた対局は窓 約 60 万局のうち約 11 万局＝2 割弱）: **最強比** vs step 62,426 得点 0.7125・**+166.7 Elo [+143.3, +191.5]**（1,000 局、最強を 98,604 に置き換え、足踏み 0）、**基準比** 累積 **+930.4 [+903.3, +960.5]**（1,000 局。基準 step 28,908 に +271.8）、**固定の参照**（各 200 局）vs win1m +240.8 → **+281.7 [+223.5, +357.0]**（得点 0.835 で天井に着いた）、vs ls-v1 646,699 +386.6 → **+372.3**（得点 0.895、天井。この参照は同日 2e675a5 で外した）、**3 つ目の参照 vs ls の 80 万局 `ckpt_000062426.pt` +157.7 [+107.4, +215.1]（得点 0.7125、帯の中。最強比の +166.7 と一致し、同じ対を別の局数で測って合った）**。**外部計測**（18:19、Tenbin Fuseki Engine 0.1 ＝ fuseki-shogi-ai の方策ネット＋水匠5、movetime 1000、10 局）: **Libra の得点 9.0（勝率 90%）**、終局は 10 局とも詰み。前回 7.0 から上がり、**今回は節目の重み（step 98,604）で測れた**（前回はその時点の `latest.onnx` step 74,627 だった。commit 1f6105b の固定が効いた）。**伸び**: 80 万 → 120 万局は 0.585 倍の倍加で、曲線の見込み（2 倍あたり +185）なら +108 Elo に対し実測 +166.7 で**見込みより上**。曲線そのものは更新できず（固定の参照 2 つが天井で帯の外、3 つ目は点が 1 つ）、次の節目で伸ばせる。**M1 一般化**: held-out の本将棋の価値の相関 0.6393 → 0.6331（20 万局で −0.006、`bin/libra review` は「注意」。80 万局の −0.000 から続く頭打ち）、窓の中との差 −0.047（held-out がわずかに悪いだけで、窓の記憶ではない）。**布石**は held-out 0.5943・窓 0.5782（差 +0.016 で held-out のほうが良い）。**λ の前後は直接比べられない**（目標そのものを変えたので、λ 0.5 のときの布石の数字は混ぜた目標に対する値で 0.8 前後だった）。同じ物差し（結果 z）で比べると 9/18 13:26 の比較の λ 1.0 の腕（step 62,426 から 5,000 step）の 0.5596 から 0.5943 に上がっている。**λ の本当の前後の比較は次の節目（160 万局）**。**局/日 1,689,139**（1 時間平均。クラウドのワーカーが動いている。9/17 のクラウドなし 約 62 万局/日の 2.7 倍）。
- 2026-09-18 **120 万局の節目の強さは「直前の自分」と「古い相手」で食い違う（非推移性 約 170 Elo）**。旅人さんの「前回から Elo が伸びていない」（管理コンソールの Elo のグラフ）を調べた結果。同じ step 98,604 の重みを 4 つの物差しで測った値（`progress/ls.json`）: **最強比 = 80 万局の自分（step 62,426）に得点 0.723・+166.7 Elo [143.3, 191.5]、1,000 局**。**外部の相手**（Tenbin Fuseki Engine 0.1、`movetime 1000`）は step 74,627 の 7.0/10 から **9.0/10**。一方、古い相手には伸びていない: **基準比（step 28,908 が相手、1,000 局）は 0.832 → 0.827（+277.9 → +271.8、鎖で 936.5 → 930.4）**、win1m（200 局）は 0.800 → 0.835（+240.8 → +281.7）、ls-v1 646,699（200 局）は 0.9025 → 0.895（+386.6 → +372.3）。**素直に足し算できるなら** step 98,604 は step 28,908 に 277.9 + 166.7 = +444.6 ＝ 得点 0.928 のはずが実測 0.827 で、**172.8 Elo 足りない**。同じ計算を win1m で 125.8、ls-v1 646,699 で 181.0 の不足。**3 つの古い相手すべてで 126〜181 Elo の不足が揃うので、1 回の測り損ないではない**（基準比は 1,000 局で 95% 区間 [244.7, 301.9]、期待の +444.6 は遠く外）。読み方は 2 つあり、どちらも未確認: (a) 古い相手には既に 83〜90% 勝っていて勝率の上限に近く、Elo が縮む、(b) 直前の自分にだけ効く強さが付いた（自己対局のじゃんけん）。**交絡**: この区間（step 62,426 → 98,604）には布石の価値目標 λ の 0.5 → 1.0 の切り替えが step 約 85,000 で挟まっている（`progress/ls-config.toml` の 13:11 が 0.5、16:05 が 1.0）。λ 1.0 が非推移性を増やしたかは未確認（最強比では +166.7 と大きく勝ち越しているので、少なくとも直前の自分に対しては悪化していない）。
- 2026-09-19 **140 万局の手動計測（step 117,436、総局数 約 1,402,838）: 4 つの物差しがすべて +42〜+94 Elo で一致した**（ユーザーがコンソールの前倒しで archive と計測を回した。数値の出所は `progress` ブランチの `progress/ls.json` の 02:30 の書き出し）。**最強比**は step 98,604 に得点 0.628・**+91.0 Elo [69.0, 113.6]**（1,000 局、更新）。**基準比**（step 28,908 が相手、1,000 局）は 0.827 → **0.859**（+271.8 → +313.9、鎖で 930.4 → 972.5）で**前回の横ばいから +42.1 に戻った**。**win1m**（200 局）は 0.835 → 0.895（+281.7 → +372.3、+90.6）。**80 万局の archive**（200 局）は 0.7125 → 0.81（+157.7 → +251.9、+94.2）。前回（120 万局）は 4 つが +167 / −6 / +41 / −14 とばらばらだったので、**非推移性は 80 万 → 120 万局の区間に固有**だったとみる（λ 1.0 の切り替えを挟んだ区間。因果は未確認）。**`libra rating`（全部の対局を Bradley-Terry で 1 本の目盛りにしたもの）では step 98,604 の +1043.5 [1011, 1076] から step 117,436 の +1115.2 [1081, 1150] で +71.7 Elo**（2 倍あたり +318.4）。**12 万局以降の 7 点の当てはめは +211.4 Elo / 2 倍（残差 21.9）**で、9/18 の「+200、ただし最後の点が直前比で高めに出ている」という但し書きは要らなくなった（今の値は全部の対局から一度に出したもの）。費用は 140 万局を 2 倍にするのに $11.22、**1 Elo あたり $0.053（約 8 円）**、次の 100 万局の見込みは +164 Elo。**じゃんけん度（χ²/自由度）は全期間 37.5、step 8,144 以降 27.0、step 28,908（40 万局）以降 13.3** と、新しい期間ほど小さい（ずれが最大なのは step 402 対 win1m の実測 0.14 対 当てはめ 0.022 など学習の初期）。ただし 13.3 は 1 より大きいので、最近でもじゃんけんは残る。**注意: このとき動いていたコードは 9/18 の `libra rating`・参照の自動入れ替えより前**（`progress/ls.json` に `rating` の節が無い。設定だけは `origin/main` から読むので `match_games = 40` と `reference_rotate = 0.8` は入っていた）。そのため **win1m（0.895）と 80 万局の archive（0.81）が両方 0.8 を超えたのに入れ替わっていない**。リポジトリを更新して停止 → 起動すると入れ替わる。
- 2026-09-18 **管理コンソールの Elo のグラフの横軸を時間と総局数で比べた**（`progress/ls.json`、総局数 108.9 万局の時点。計算はスナップショットの `metrics` と `reference` から、`scaling.games_of_step` と同じ直線の当てはめで行った）: 1 時間平均の局/日は **349,411〜3,792,184（10.9 倍の開き）**、局/日が 0 の観測が 7 回（止めていた時間帯）。固定の参照 win1m.pt の 7 点で、横幅に占める割合は step 402→1,416（4,631→18,731 局）が時間軸 10.4% / 局数軸 1.8% で **5.9 倍の引き伸ばし**、13,807→21,270 が 7.8% / 12.6% で 0.62 倍の縮み。最後の計測（80 万局、8.6 時間前）から右は時間軸で全体の **43% が空白**。→ Elo と対外対局の横軸を総局数にした（decisions.md 同日）。
- 2026-09-19 **一般化の物差し（M1）は丸暗記ではなく、強さと関係が薄いだけだった**（`progress/ls.json` 02:46 の書き出し、step 121,804・総局数 1,449,553。40 万局以降の gen の計測 56 回。詳細は docs/gen-metric-2026-09-19.md）。**丸暗記の指標（窓の中 − held-out）は 4 区間の平均で −0.002 / +0.008 / +0.011 / +0.002**（閾値 0.1 の 10 分の 1 以下。56 回のうち 22 回は held-out のほうが良い）。比較: 2026-09-17 の本物の丸暗記は窓の中 0.94 対 held-out 0.43（差 0.51）。**強さが `libra rating` で +751.6 → +1115.2（+364 Elo、40 万 → 145 万局）伸びた同じ区間で、held-out の相関は +0.008 ± 0.005**（最後の 1/4 の平均 − 最初の 1/4 の平均、t = 1.68 で偶然と区別が付かない）。**1 回の計測のばらつきは sd 0.013**（4,000 局面。相関 0.65・4,000 標本の理論値 0.009 とほぼ一致）で、20 万局離れた**2 点の差のばらつきは sd 0.018**。本当の伸びは 145 万局あたりでは 20 万局につき +0.0008 程度（56 点の当てはめ +0.0037 / 2 倍、95% 区間 [−0.003, +0.010]）なので、**ノイズが信号の 23 倍**。実際に判定は 0.6331「注意」（9/18 の 120 万局の節目）→ 0.6614「続ける」（9/19 02:46）と裏返った。**対処は判定の直し**（decisions.md 同日）で、学習の設定は変えていない。
- 2026-09-19 **160 万局の自動計測（step 135,740、総局数 1,600,085）: 傾きは変わらず、λ 1.0 の悪化も無し**（`progress` ブランチの `progress/ls.json` の 06:25 の書き出し。**`libra rating` と参照の自動入れ替えが入ったコードで動いた初めての節目**）。**最強比**は step 117,436 に得点 0.549・**+34.2 Elo [12.6, 56.0]**（1,000 局、更新。区間の下限が 0 より上）。**基準比**は前回 0.859 で 0.85 を超えたため基準が step 117,436 に置き換わり、鎖で 972.5 → 1,006.7。**固定の参照**は win1m 0.895 → 0.87（+372.3 → +330.2）、80 万局の archive 0.81 → 0.805（+251.9 → +246.3）で、どちらも下がったが 200 局の幅（±60〜90 Elo）の内で有意ではない。**外部の相手**（Tenbin Fuseki Engine 0.1、`movetime 1000`）は **40 局で得点 36.0（90%）**（`match_games` 10 → 40 の初回。step 98,604 の 9/10 と同じ率を 4 倍の局数で確かめた）。**`libra rating`（全部の対局を Bradley-Terry で 1 本の目盛りに）**: step 117,436 の +1106.9 [1073.4, 1140.5] → step 135,740 の **+1135.6 [1097.8, 1173.3]**。**12 万局以降の 8 点の当てはめは +209.1 Elo / 2 倍（残差 19.9）**で 9/19 の +211.4（7 点、残差 21.9）から変わらない。**直近の区間の「2 倍あたり +151」は遅く見えるが、区間が 0.19 倍化しかないので幅が広く、実測 +34.2 [12.6, 56.0] を倍化数で割ると +180 [+66, +295] で、傾き +209 が期待する +40.0 Elo は区間の中にある。伸びが鈍った証拠は無い。** **λ 1.0 の前後**（CLAUDE.md 11 の「次の 2 つの節目で比べる」）: この節目のリプレイの窓（約 80 万局）は λ 1.0 で作った対局が約 7 割（切り替えは総局数 約 105 万局）で、**最強比は区間の下限が 0 より上、一般化の差も noise の範囲**（窓 − held-out: 布石 corr_v −0.0363・本将棋 +0.0107、符号が前回と逆で大きさも同程度）。**悪化の兆候は無い**。じゃんけん度（χ²/自由度）は全期間 33.9（前回 37.5）。**曲線の不具合を 1 つ直した**: 外部計測しか無い step 74,627（10 局、区間 ±287 Elo）が曲線に載り「2 倍あたり −190 Elo」という区間を作っていたので、200 局に満たない点は外すようにした（`rating.CURVE_MIN_GAMES`）。上の値はその修正後。
- 2026-09-19 **200 万局の節目（step 172,923、総局数 2,000,241）: 下がっていない。過去最強を更新**（`progress` ブランチの `progress/ls.json` の 12:54 の書き出し。旅人さんの「Elo さがってませんか？」の確認）。**`libra rating`（全部の対局を Bradley-Terry で 1 本の目盛りに）**: 120 万局 +1029.8 → 140 万局 +1091.9 → 160 万局 +1106.0 → **200 万局 +1151.8 [1112.2, 1191.5]**（160 万局から **+45.8**）。**最強比は step 135,740 に +66.5 Elo [44.8, 88.7]（1,000 局、更新、足踏み 0）で区間の下限が 0 より上**。**下がって見えた線は相手ごとの 200 局の計測**: win1m.pt が step 135,740 の +330.2 [267.4, 416.4]（87%）→ step 172,923 の +288.1 [229.3, 364.6]（84%）、ckpt_000062426.pt が +246.3 [190.9, 315.2]（80%）→ +190.8 [139.0, 251.9]（75%）。**どちらも区間が大きく重なり、下がりは有意ではない**（200 局の 2 点の差のばらつきは約 50 Elo で、実測の −42.1・−55.5 はいずれも約 1 標準偏差）。**しかも 2 つとも得点 0.84・0.75 で、win1m は天井寄り**（進捗の書き出しも「天井・床」と印を付けている）。**目盛りの直前比（+45.8）が直接測った最強比（+66.5）より小さいのは、目盛りが相手ごとの測り直しも一緒に当てはめているため**（じゃんけん度 χ²/自由度 33.19）。**伸びの曲線**: 全部の対局からの当てはめで **+140.1 Elo / 2 倍（11 点、残差 87.9）**、帯の中の 7 点では +186.5（残差 34.3）。9/19 の 160 万局の +209.1（8 点、残差 19.9）より小さいが、**この 11 点には外部計測 10 局だけの点など区間の広いものが混ざる**（`CURVE_MIN_GAMES` は目盛りの曲線にだけ効く）ので、160 万局の値と同じ物差しではない。**局/日は 1,875,827**（1 時間平均）。**外部の相手**（Tenbin Fuseki Engine 0.1）は step 135,740 の 40 局で得点 0.900 のまま（200 万局の点はまだ無い）。
- 2026-09-19 **伸びの曲線は「最近の伸び」で見ないと頭打ちの判定を誤る**（`progress/ls.json` の 200 万局の 11 点で確かめた）。log2(総局数) に対する Elo の直線当てはめを、範囲を変えて比べた: **全 11 点で傾き +140.1／2 倍・残差 87.9**（残差の並びは +132・+18・−146・−154・−82・−20・+12・+43・+74・+61・+62 で、初めの 2 点に引っ張られて傾きが浅くなり、**最新の点が線より +62 上**＝「加速している」と読めてしまう）。**12 万局以降の 9 点で +200.1・残差 20.8**、**直近 4 回の倍化（20 万局以降）の 8 点で +203.1・残差 21.3**（最新の点は線より −23）、**40 万局以降の 5 点で +180.9・残差 10.4**（同 −12）。**「直近 4 回の倍化・最低 4 点」の規則を過去の各節目に当てはめると、40 万局以降の傾きは 213.2 → 207.0 → 204.1 → 205.6 → 203.4 → 203.1 と安定**（20 万・30 万局の時点では 84.9・96.0 と不安定だが、点が少ない時期なので使わない）。→ 目安の線はこの範囲に当てはめることにした（decisions.md 同日）。**読み: 200 万局の点は目安の線の 23 Elo 下で、残差 21 の範囲内。頭打ちの証拠は無い。**
- 2026-09-19 **自己対局に投了を入れたときの効き目**（`bin/libra resign`、ls の最新 20,000 局＝step 約 189,000・総局数 約 218 万。ランは止めずに読み取りだけ。読み 96/24、布石では投了しない）。**素の値**: 平均 92.4 手・3,615.3 評価/局、証明済みの手（探索していない）87,938 手＝4.40 手/局（4.8%）、布石の手順から指した手 0 手（`openings` が空）、引き分け 0.4%。しきい値 T・連続 K ごとの（投了した局／手数の節約／**評価の節約**／局/日の倍率／誤投了＝投了した側が最後まで打てば勝っていた割合）: **0.90・1 → 72.5% / 20.3% / 19.1% / 1.24 倍 / 2.61%**、0.90・2 → 66.0% / 16.5% / 15.2% / 1.18 倍 / 1.30%、0.95・1 → 70.0% / 14.7% / 13.2% / 1.15 倍 / 0.99%、0.95・2 → 60.2% / 11.7% / 10.4% / 1.12 倍 / 0.43%、0.98・1 → 65.2% / 10.1% / 8.6% / 1.09 倍 / 0.32%、0.99・1 → 60.5% / 7.8% / 6.3% / 1.07 倍 / 0.17%。**どの設定も AlphaGo Zero の基準（誤投了 5% 未満）の内側**で、いちばん緩い 0.90・1 でも 2.61%。全局に占める「結果のラベルが逆になる局」は 0.90・1 で 1.89%、0.95・1 で 0.69%、0.95・2 で 0.26%。**重要**: 手数の節約と局/日の伸びはほぼ打ち消し合い、**1 日に学習へ入る局面の数はどの設定でも変わらない**（0.983〜0.987 倍）。増えるのは 1 日に取れる対局の結果（勝ち負け）の数で、価値の学習には効くが方策の学習に使う局面は増えない。したがって曲線（+186.5 Elo / 2 倍）から出る「1.24 倍 ＝ +57 Elo」は**上限**で、実際にどれだけ化けるかは入れて前後を測らないと分からない（未確認）。**この見積もり自体の限界**: 残りの手がそのまま指されたと仮定して数え直した値（AlphaGo Zero が誤投了を測るのと同じやり方）。また棋譜に最良の子の値が無いので根の値だけで判定しており、原典の「両方が下回ったら」より投了しやすいぶん、節約率も誤投了率も上限（decisions.md 同日）。
- 2026-09-19 **稼働中の ls の内訳（`libra status` の `timing`、窓 307 秒。クラウド 2 台と併走、lx は停止中）**: `sp_eval 69.9%` / `sp_apply 8.0%` / `sp_collect 4.4%` / `sp_proof 2.3%` / `train_step 12.2%` / `train_sample 0.7%` / `train_publish 0.2%` / `replay 0.2%` / `checkpoint 1.5%` / `other 0.5%`。1 ラウンド 42.9 回/秒（collect 1.0 ms、eval 16.3 ms、proof 0.5 ms、apply 1.9 ms）、学習 173.5 ms/step。**訂正: 自己対局の CPU 側は約 3% ではなく約 12%**（`sp_collect + sp_apply` = 12.4%、1 ラウンド 19.7 ms のうち 2.9 ms）。「約 3%」は 2026-09-16 に**偽のネットで・lx が GPU を分け合っていた**条件で測った値をそのまま引いたもの。GPU が主（`sp_eval` 69.9%）という結論は変わらないが、collect と apply が GPU の順伝播と**直列**なので、512 局を 2 組に分けて重ねれば 1 割前後の余地がある（**未検証**。棋譜は局ごとに独立なので変わらない）。**メモリ**: 窓 109 万局でランナーの RSS 6.75 GB（≒ 5 KB/局。従来の見積もり 10 KB/局は誤り）、WSL 32 GB のうち空き 21 GB、`free` のスワップ 16 GiB 中 10 GiB 使用（`autoMemoryReclaim=gradual` によるものと**推定**、未確認。`train_sample 0.7%` ≒ 10 ms/step が 2026-09-16 の 8.8〜11.4 ms と一致するので、リプレイの窓はスワップに出ていない）。
- 2026-09-19 **240 万局の節目（step 210,020、総局数 2,399,947）: 中の物差しは順調。外部計測だけ無効**（`progress` ブランチの `progress/ls.json` の 9/20 04:26 の書き出し）。**最強比** vs step 172,923 得点 0.580・**+56.1 Elo [34.4, 78.1]**（1,000 局、更新、足踏み 0。区間の下限が 0 より上）。**基準比** 得点 0.702・累積 **+1,121.3 [1,098.4, 1,145.6]**（1,000 局）。**固定の参照**（各 200 局）win1m 得点 0.82・+263.4 [206.7, 335.3]（200 万局の +288.1 から下がったが 200 局の幅の内）、ckpt_000062426 得点 0.795・+235.4 [180.8, 302.6]。**`libra rating` の目盛り** 200 万局 +1,159.8 → **240 万局 +1,204.0 [1,168.4, 1,239.6]**（+44.2）。**伸びの曲線** 12 万〜240 万局の 8 点で **+181.0 Elo / 2 倍（残差 34.8）**。**外部計測は無効**（下の行）。**局/日 540,512**（1 時間平均。metrics の 2 点から計算し直しても 532,300 で一致）。200 万局の節目の 1,875,827 の約 29% で、2026-09-17 の「クラウドなし 約 62 万局/日」に近い。**自動計測のジョブは走っていない**（`auto.running` は空、待ち行列 0）ので、**vast.ai のワーカーが止まっていると見られる。未確認。**
- 2026-09-19 **240 万局の外部計測（40 局で 40 勝 0 敗）は無効**。ハンデ（Libra だけ 1 手 400 回）を入れた初回だが、**相手も 1 手 400 回にしてしまっていた**。原因は**手元の作業ツリーが古いまま**だったこと: 設定は `config/ls.toml` をリポジトリの `origin/main` から読む（`runconfig.py`）ので `match_go = "nodes 400"` は効いたが、「相手だけ別の `go`」を送るコード（同日の PR #14）が手元に無く、`match_go_opp` が黙って無視されて `--go-opp` が渡らず、`run_match` が両者に `nodes 400` を送った。相手（fuseki_usi_server ＋やねうら王/水匠5）は `movetime 1000` で約 240 万ノード読む（2026-09-15 の実測）ので、400 ノードは別物の弱さ。**古いコードで動いていた証拠**（`progress/ls.json`）: 計測の記録に PR #14 で足した `go_opp`・`opponent_opt`・`libra_opt` が 1 つも無く、`auto_cfg` にも `match_go_opp` が無い。**40 勝 0 敗の原因が相手の 400 ノードであることは確からしいが未確認**（相手が `go nodes` を honor したかは確かめていない）。**始末**: `git pull` して起動し直せば、この行は条件が違うので `libra rating` が別の点（`Tenbin Fuseki Engine 0.1 [Threads=2, nodes 400]`）として扱い、1 局も負けていない点は強連結の条件から外れて**自動で目盛りから落ちる**（手元で再現して確かめた）。棋譜を消す必要は無い。**pull 前の目盛りの 240 万局の点（+1,204.0）は、この 40 勝が正しい相手との対局として混ざっているぶん高め**（`n` 2,440 のうち 40 局なので影響は小さい見込み。未確認）。中の物差し（最強比 +56.1）はこの行を使っていないので影響なし。

- 2026-09-20 15:09〜15:14 **稼働中の ls の GPU 使用率を 5 分間・1 秒ごとに計測**（`nvidia-smi dmon -s pucm`、300 サンプル。ls だけが GPU を使い、lx もクラウドのワーカーも無し。ls 自身が対象なので止めずに測った）。**全体**: SM 使用率 平均 66.5%・p50 81%・p10 20%、メモリ帯域 平均 43.4%、消費電力 平均 235 W・p50 279 W（上限 300 W、`clocks_event_reasons.sw_power_cap` は Active）。**時系列に周期がある**: SM が 50% を切る塊が **33〜40 秒ごとに 9〜10 秒**（9 回。うち 1 回は 14 秒で、同時刻のチェックポイントと `latest.onnx` の書き出し 約 3 秒を含む）現れ、その間は SM 平均 27%・120 W。残りの 216 秒（72%）は SM 平均 82%・279 W。周期と長さは同時刻の `timing`（`train_step 23.8%`、学習 23 step × 425.8 ms ≒ 9.8 秒、`train_every_games 256`）と一致するので、**低い塊は学習のステップの期間**（時刻の直接の突き合わせはしておらず**未確認**）。**CPU はほぼ空き**: ランナーのプロセスの CPU は `/proc` のカウンタの差で **1.20 コア / 32 コア（3.8%）**、うち主スレッド 0.69・自己対局の 12 スレッドは各 0.04（loadavg 1.4）。自己対局の期間は GPU がほぼ飽和していて余地は小さい。
- 2026-09-20 **上の「学習の期間に GPU が空く」の原因は WSL のスワップ（リプレイの窓が追い出されている）。定常の性質ではなく、行ったり来たりする状態**（`metrics.jsonl` の全期間と `/proc` で確認）。**証拠**: (1) 学習 1 step の時間は **132〜180 ms と 380〜455 ms の 2 つの状態を数時間ごとに往復**していて、窓の大きさとは対応しない（9/19 08:14 は窓 93 万局で 358 ms、9/19 13:31 は窓 105 万局で 171 ms）。(2) 遅い状態では**バッチ作りの待ち `train_sample` が 2〜5 ms から 30〜67 ms に増える**一方、**自己対局の順伝播 `sp_eval` は 15.9 ms のまま動かない**。つまり GPU そのものは遅くなっていない。(3) **ランナーのプロセスは 9.36 GB がスワップに出ている**（`/proc/835441/status` の `VmSwap`、RSS 6.97 GB）。**一方で RAM は 23 GB 空いている**（`free`: 31 GB 中 使用 8 GB、スワップ 16 GB 中 12 GB 使用）。`vmstat` のスワップインは起動からの平均 2,114 KB/s。窓（135 万局）から一様に取るので、冷えて追い出されたページを毎回引き戻している。原因は `%USERPROFILE%\.wslconfig` の `[experimental] autoMemoryReclaim=gradual`（＋ `swap=16GB`、スワップファイルは F: ドライブ）で、**空きがあってもページを Windows に返している**。2026-09-19 の行の「リプレイの窓はスワップに出ていない」は**誤り**（`train_sample 0.7%` が小さいのは、バッチ作りを別スレッドで学習と重ねているため待ちとして見えていなかっただけ）。**大きさ**: 9/20 05 時以降の同じ条件（クラウドのワーカー無し）で、速い状態（学習 145 ms/step、20 点）は **局/日 669,171**、遅い状態（417 ms/step、89 点）は **598,014** で、**+11.9%**。学習ブロックが 9.8 秒 → 3.5 秒に縮むぶんの見積もり（+18%）より小さいが、向きと桁は合う。**直し方の案**（未実施。ユーザーの判断が要る）: `.wslconfig` の `autoMemoryReclaim` を外す（`memory` を 32 GB から下げて Windows のぶんを残す）。反映には `wsl --shutdown` が要るので、ラン停止 → 設定変更 → 起動の順になる。`vm.swappiness` は sudo が無いので使えず、`mlock` も上限 64 MB で足りない。
