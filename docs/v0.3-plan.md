# v0.3 の検討リスト

v0.2 を出したあとに手を付ける候補を 1 か所に集める文書。**決まったことは docs/decisions.md に 1 行、実測は docs/measurements.md に 1 行が正**で、ここには「何を・なぜ・入れる前に何を確かめるか」だけを書く。リリースの手順そのものは docs/release.md。

**状態**: 予定（ユーザーの決定済み）／候補（未決。Claude が決めてよいものは decisions.md に 1 行を書いてから入れる）／保留（入れると決まっているが時期を調整中）。

**「学習」の列が ● の項目は、学習データ（探索・目標・リプレイ・設定値）を変える。** CLAUDE.md 11 により**同じ日に 2 つ入れない**。入れた日の前後で、最強比 2,000 局・`gen` の held-out との差・`libra rating` の目盛りを比べて measurements.md に書く。

**最終更新 2026-09-24。**

| # | 項目 | 状態 | 学習 | 出典 |
|---|---|---|---|---|
| 3 | 投了を有効にする | **入れた（2026-09-21）。440 万局で合格** | ● | 2026-09-19 decisions.md |
| 1 | 搾取者 lx を入れる | **起動した（2026-09-23）。課程を入れて立ち上げ中** | ●（本体は変わらない） | 2026-09-21 ユーザー |
| 2 | 自己対局の後手玉四段目を 25% から減らす | 候補 | ● | 2026-09-21 measurements.md |
| 4 | エンジンの推論を fp16 にする | 保留（引き金あり） | ○ | 2026-09-17 ユーザー |

## 入れる順番（2026-09-21 に決めた）

**ユーザーの決定「投了を先」。** 投了は 局/日 を 1.21 倍にするので、GPU を分け合う lx より先に入れると、lx で目減りするぶんを先に稼げる。

| 段 | 変えるもの | 学習データ | 状態 |
|---|---|---|---|
| ① | `[search] resign_threshold` 0.0 → 0.9（§3） | 変わる | **済（2026-09-21 に入れ、440 万局で合格）** |
| ② | lx を単体で起動（`config/lx.toml`。§1） | **本体は変わらない** | **済（2026-09-23 に起動）。2026-09-24 から §1.1 の課程で立ち上げ中** |
| ③ | ls の `[selfplay] openings`（lx の布石を 10%） | 変わる | **②が本番の本体に勝てるようになってから**（下） |
| ④ | ls の `[league] enabled = true` | 変わる | ③ の 1 節目あと |

段のあいだに節目（総局数 40 万局ごと、2026-09-24 の 約 65 万局/日 で約 15 時間）を 1 つ挟み、最強比 2,000 局・`gen` の held-out との差・`libra rating` の目盛りを比べてから次へ進む（CLAUDE.md 11）。② は本体の学習データを変えないので、①の計測を待たずに進められる（ただし GPU を分け合うので ①の 局/日 の比較は ② の起動前に済ませた）。

**③ の引き金が変わった（2026-09-24）。** もとは「② の布石が溜まってから」だったが、**lx が本番の本体にほとんど勝てていないので布石が出ない**（`openings.extract_openings` は lx が勝った局だけを拾う）。さらに §1.1 の課程を入れてからは、**課程の間に勝った局は布石に数えない**（弱くした相手に勝った布石を本体に渡さないため）。よって ③ の引き金は **「課程を終えて、本番の本体への勝率が出るようになること」**。

---

## 1. 搾取者 lx を入れる（②。起動済み、立ち上げ中）

やり直しの系列（2026-09-17〜）に lx を足した。docs/runbook.md 冒頭の「本体が最強比で伸び始めてから足す」という前提は満たしていた（2026-09-21 の最強比は step 317,975 が +37.0 Elo、区間 [21.7, 52.4]、2,000 局）。

**ここまでの経過**

| 日 | できごと |
|---|---|
| 2026-09-22 | `config/lx.toml` と docs/lx-settings.md を書き、main に入れた（ユーザーの「そろそろlxを入れてもいいか？」に Claude が「入れてよい」と答えた。decisions.md 同日）。土台は旧系列 lx-v1 の `config.toml` で、変えたのは 5 つだけ |
| 2026-09-22 | **ゼロから始める搾取者の run は 1 度も起動できない不具合**が見つかり直した（`load_opponent` が凍結相手のファイルの有無を見ずに読み、作り直しの経路は「相手が既に居る」前提だった。**Claude が文書に「起動直後に自動で作られる」と書いた誤りも同時に直した**。decisions.md 同日） |
| 2026-09-23 | ユーザーが起動。**ls の 局/日 は約 87 万 → 約 65 万（−25%）**、lx 自身は約 31 万局/日（見込みは「2 割減」だった。measurements.md 同日） |
| 2026-09-23 | 対本体勝率が `progress` に出ていなかったので出すようにした（decisions.md 同日） |
| 2026-09-24 | **1 日回して対本体勝率 0.04%**（99,462 局で 35 勝）。切り分けの結論は「壊れていない。学習回数が旧型の 39%」（measurements.md 同日） |
| 2026-09-24 | 一次資料の方法の節を読み直し、**課程が無いことが一次資料との差**と分かった。**ユーザーの決定で課程を入れた**（§1.1） |

**§1.1 課程（カリキュラム）— 2026-09-24 に入れた**

凍結した本体の**読みの回数を段で上げる**。`[exploiter] curriculum_sims = [1, 2, 4, 8, 16, 32]`・`curriculum_threshold = 0.75`・`curriculum_games = 2000`。直近 2,000 局の勝率が 75% を超えたら次の段へ進み、最後の段を越えたら本番の読み（96・24 回）に戻る。

- **なぜ**: ゼロの重みで最初から本番の強さの相手と打つ形は、読んだ一次資料（[Wang23]・[Tseng25]・[Timbers22]・AlphaStar）のどれにも無い。[Tseng25] 付録 E.2 は最終の相手の読み 1〜4 回から倍にし（閾値 75%）、64 回からいきなり当てると 3% のまま伸びず、4 回から始め直して進んだ。**1 局も勝てないと結果 z が常に −1 で「どう勝つか」の信号が無い**（[Wang23] は KataGo の補助の損失に頼ったが Libra には無い）。docs/exploiter-literature.md §3 の 7。
- **論文と変えたところ**: 読みを戻す操作（[Tseng25] は人の手で戻した）は入れない（崩れたら段を足す）／相手を弱めるのは相手の手番の根の読みだけで、lx の木の中の本体の手は 2026-09-14 の決定どおり本体の方策の PUCT のまま／**課程の間の局は対本体勝率にも布石にも数えない**（収束の物差しは本番の本体への勝率のため）。decisions.md 2026-09-24、docs/lx-settings.md §5、docs/method-evidence.md §2.14。
- **C++ は変えていない**（相手の読みは既存の `SelfPlay.set_side_config`）ので作り直しは要らない。コンソールの表示を変えたので `tools/windows/install.sh` は要る。
- **効き目**（2026-09-24 09:36 に起動、約 75 分後）: 課程の前は本番の本体に 0.04% だったのが、1 段目（本体の読み 1 回）で**直近 2,000 局 2.3%**。勝つ頻度はおよそ 60 倍。

**見るもの**

- 段ごとに上がるまでの局数（`state.json` の `exploiter.curriculum.history`、progress の `lx.md`、コンソールの「対本体 勝率」の行）。
- **段が上がり始めると速い**: 判定は直近 2,000 局の移動窓で、lx は 1 時間に約 13,000 局打つので 2,000 局は約 9 分ぶん。
- **最後の段 32 → 本番（96・24 回）だけ 3 倍の跳び**（ほかは全部 2 倍）。[Tseng25] は「上げるのが早すぎると崩れる」（128 → 256 で崩れ、128 に戻して回復）と報告しているので、**課程を終えた直後に崩れたら、まず 64 の段を足す**。
- **課程を終えた後の本番の本体への勝率が本来の物差し**。収束の判定は「ゼロから学習した搾取者の対本体勝率が一定以下で頭打ちになること」（docs/lx-settings.md）。
- lx の `ruling41` がほぼ 0%（`prune_gote_rank4 = true` が効いている）、lx のランナーの RSS が 6 GB 前後（窓の上限 50 万局は仮の値）。

**それでも上がらないときの手**（まだ出していない。どれも学習データに関わるので docs/lx-settings.md に出所を書いてから）

- (a) 本体の方策のノイズを強めるか温度を上げて、1 段目より弱い段を作る。
- (b) 過去の弱い本体の重みを 1 段目の前に挟む（[Wang23] 型。[Tseng25] はこれを無駄と見ている）。
- (c) `replay_ratio` を上げる（[Wang23]・[Tseng25] は 1 局面 4 回。今は 1.0。勝ちが出始めた今なら効く見込み）。

**本体 ls 側で変える 2 行。どちらも学習データが変わるので、同じ日に両方は入れない。**

- `[selfplay] openings`（今は空） — lx が見つけた布石を本体の新規対局の 10%（`openings_prob = 0.1`）に混ぜる。**引き金は上の「入れる順番」のとおり課程の終了。**
- `[league] enabled`（今は `false`） — 本体が過去の lx と打つ局。**局数で約 9〜11% を占める**（docs/runbook.md §搾取者）。

**注意**

- 探索の形（`[exploiter] opponent_prior`、既定 true）は変えない。2026-09-14 に、評価ハーネスの形を流用して Wang+ 2023 が誤りとした探索のまま 3 日動いていた（CLAUDE.md 8）。
- GPU を分け合うので ls の 局/日 が落ちる（実測 −25%）。前後の 局/日 は measurements.md 2026-09-23 に書いた。
- lx の玉配置は後手玉四段目を除く 36×27 から一様（`search.prune_gote_rank4 = true`）。本体と違う（2026-09-11 の決定）ので、§2 を入れるときも lx 側は触らない。

## 2. 自己対局の後手玉四段目を 25% から減らす（候補、未決）

自己対局は 1〜2 手目の玉を**方策ではなく 36×36 のマスから一様乱数で置いている**（`libra-search/src/selfplay.cpp` の `start_game`）。後手玉の 36 マスのうち 9 マスが四段目なので**四段目はきっかり 25% 出る**。四段目は 3 手目の桂打ちが docs/rules.md §3.4 の遮断不能になり、40 手完了時の裁定で先手の勝ちが確定するので、41 手目に届かない。

- **もったいなさ**: 学習に入る局面の約 11% が「もう決まっている局面」（2026-09-21 の measurements.md）。0% にすれば同じ GPU で実質の局数が 1 割ほど増える**見込み**。
- **この見込みは過大**: 41 手目以降は根の詰み探索（`mate_nodes_root`・`proof_nodes`）のぶん 1 手が高く、短い布石だけの局を長い局に置き換えると 局/日 はその比ほど戻らない。**未検証**。
- **丸ごと除かない**（§5）。**25% → 5% 程度に減らす**のが案。
- **要る改造**: 今は真偽値（`search.prune_gote_rank4`。2026-09-11 に搾取者用として作ったもの）なので、確率にする小改造が要る。
- **入れたあとに確かめること**: 後手玉四段目の 2 手目の局面でエンジンが返す `winrate` が 0.5 以上のままか（`position fuseki moves K*<先手> K*<後手の四段目>` ＋ `go` を直接送る）。§5 の理由がここに掛かる。
- **入れる時期**: ②（lx）が落ち着いてから。学習データを変えるので ③・④ とも同じ日に入れない。

## 3. 投了を有効にする（2026-09-21 に入れ、440 万局で合格）

`config/ls.toml` の `[search] resign_threshold` を 0.0 → **0.9** にした（**ユーザーの決定「投了を先」**。decisions.md 同日）。`resign_runs = 1`・`resign_disable_prob = 0.1`・`resign_min_ply = 40` は既定と同じだが、決めた値なので明示して書いた。

**結果（2026-09-22 09:40 ごろに効き始め、440 万局の節目で判定）**

- **合格**。同じ相手への最強比が +13.4 → **+44.9 Elo**（差 +31.5、区間 [9.9, 53.1] の下限が 0 より上）。
- 効き目: 投了で終わった局 **66.4%**、1 局 94 手 → **74.8 手**、局/日 約 58 万 → 約 74 万。布石の裁定は 24.97% で投了前と変わらず（予想どおり無関係）。
- **C++ を作り直していなかったため丸 1 日効かなかった**（動いていた `_search.so` が投了の入ったコミットより前で、設定を黙って読み飛ばしていた）。番人を入れた（decisions.md 2026-09-22）。
- **副作用**: 一般化の物差し（M1）の水準が下がり、480 万局で判定が「見直し」に上がった。**丸暗記ではなく物差しのほうがずれた**（本将棋の相関だけが落ち、布石は落ちていない）ことを確かめ、線 `gen_min_corr` を 0.6 → 0.5 に引き直した（decisions.md 2026-09-23）。**自己対局の分布を変える設定（③・④）を入れたら、この線を測り直す。**

出典と数値: docs/measurements.md・docs/decisions.md の 2026-09-19・09-23、docs/method-evidence.md §2.11。効き目は `bin/libra resign` でランを止めずに測り直せる。**計測の対局では投了しない**（`evaluate.play_match` が 0 に上書きする）。

## 4. エンジンの推論を fp16 にする（保留。こちらから持ち出さない）

**2026-09-17 のユーザーの決定で保留**。「今回は報告だけでよい。サイトが WebGPU に対応したり、Windows の配布版で GPU 版を利用できるようにする改修を検討するときに再度提案して」。**引き金が来るまでこちらから持ち出さない。**

- 理由: fp16 が効くのは WSL の CUDA 版エンジンだけで、サイト（onnxruntime-web の wasm ＝ CPU）にも Windows の配布版（CPU 版 onnxruntime）にも効かない。学習の 局/日 にも効かない（自己対局はすでに fp16）。
- 引き金: tenbin-shogi-web の WebGPU 対応、または tenbin-shogi-desktop / Windows 版の GPU 実行（CUDA / DirectML）の話が出たとき。
- 再提案のときに使う実測は **docs/libra-engine-speed-2026-09-17.md**（2026-09-24 にリポジトリへ取り込んだ。§3 の計測プログラムは `tools/bench-cpu-2026-09-17.cpp`）。要点は、エンジンの時間はほぼ全部がネットの推論で探索の CPU 側は 3〜5%（実測）、fp16 の見込みは約 2 倍（幅 1.6〜2.2 倍、**未検証**）、ONNX のファイルは約半分、手を入れるのは書き出しの `libra-net/libra_net/export_onnx.py` だけで **C++ の変更は要らない見込み**。**測り直しは要らない。**
- 同じ調査の §5 に「**読みの回数と強さの曲線はこのプロジェクトで一度も測っていない**」とある。fp16 を再提案するときは、速さが強さにどれだけ変わるかが分からないままなので、先にこの曲線を測る（手順も §5 にある）。
- **複数葉の同時評価は 2026-09-14 に入れ済み**（`DNN_Batch_Size`、既定 64）なので、2026-09-11 の decisions.md が「速度が要るときに入れる」と書いた 2 つのうち残っているのはこれだけ。

## 5. 今は入れないと決めたもの

**先手の得（対称な置き方で +39〜+90 Elo、組ごとに +11〜+197）を v0.3 の項目にすること**（2026-09-21）。v0.2 の玉配置表の検証対局 196,328 局で確定した値だが、**学習の設定を変える話にはならない**（自己対局は 1〜2 手目の玉を一様に置いているので、先手の得は置き方の偏りから出たものではない。ただしこれは「v0.2 が読み 96 回で自分と打ったときの値」で、ゲームそのものの値でも人同士の値でもない）。人同士のハンデの表を作り直す材料なので、そちらで扱う（measurements.md 2026-09-21、tenbin-shogi-web の docs/decisions.md 2026-09-19）。

**自己対局から後手玉四段目を丸ごと除くこと**（2026-09-21）。理由は**「選ぶ」側の評価が壊れるから**。

天秤将棋は片方が両玉を置き、もう片方が先後を選ぶ。相手が置き役のときは、後手玉が四段目の局面を Libra が評価して「先手を持つ」と答えなければならない。そしてその判断は玉配置表ではなく**ネットが出している**: `libra-engine/src/engine.cpp` の `Engine::scale_move` は `pos.ply() > 1` で false を返すので `scale.json` は 1〜2 手目を置くときにしか効かず、選ぶときの `winrate` は探索＝ネットが返す（docs/protocol.md §2。desktop 0.10.0 から GUI が実際にこれを問い合わせる）。人が置き役になるサイトとデスクトップで起きる場面。

2026-09-11 の decisions.md にも「搾取者 lx の玉配置は後手玉四段目を除く 36×27 から一様（`search.prune_gote_rank4`）。**本体 L-S は設計どおり 36×36 一様のまま（選ぶ側が悪い配置も評価できるように）**」とあり、本体で除いていないのは意図したもの。

**2 駒・3 駒関係（KP・KKP・KPP）の表を評価値に足すこと**（**2026-09-24 のユーザーの決定**「やめる」）。既存の表は不変の制約で使えず（ルールも違う）、自前で学ぶ表は Transformer の注意機構がすでに表せる関係の重ね書き。表の強みは CPU での速さで、GPU 律速の Libra では読みが速くならない。KPP は約 1.93 億項目でネット（約 1,020 万）の約 19 倍。検討の全文は docs/two-piece-table-2026-09-24.md。**こちらから持ち出さない。**

## 6. 手を入れていないが分かっている穴

**外部計測（やねうら王＋水匠 5 との 40 局）が 280 万〜480 万局の 5 つの節目とも 1 局も記録を残していない。** 相手が「評価関数を読み込みます」と言った直後に落ちている（プロセスの起動そのものは成功しているので実行権限や共有ライブラリの不足ではない）。**相手側の資産の問題なのでユーザーに確認を依頼している**（2026-09-23）。自分同士の計測（最強比・基準比・固定の参照）は正常なので、学習の判断には困らない。measurements.md 2026-09-23 (h)。
