@echo off
rem generated by tools/windows/install.sh -- do not edit here; edit the .in template
rem applies to both runs: ls (main) and lx (exploiter)
for %%R in (ls lx) do (
  echo == %%R ==
  wsl.exe @DISTRO_ARG@ -- @LIBRA@ --run %%R status
)
pause
